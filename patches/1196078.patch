diff --git a/1196078.orig.txt b/1196078.txt
--- a/1196078.orig.txt
+++ b/1196078.txt
@@ -10,11 +10,11 @@
 <p>The code works Ok... but there are a couple of things still to be worked out:-
 </p>
 
-<ul><b>1.</b> The code allows me to see the sites used within each "dsk" item... but I also want to see the "dsk" items used at each site.  Can I do that with a single hash... or (as I expect) I'll need to maintain at least a couple of hashes?
-</ul>
+<p><b>1.</b> The code allows me to see the sites used within each "dsk" item... but I also want to see the "dsk" items used at each site.  Can I do that with a single hash... or (as I expect) I'll need to maintain at least a couple of hashes?
+</p>
 
-<ul><b>2.</b> Is there a "better"(?) way to do this sort of thing?  I have an inkling there might be something with 'map' that could help.
-</ul>
+<p><b>2.</b> Is there a "better"(?) way to do this sort of thing?  I have an inkling there might be something with 'map' that could help.
+</p>
 
 <p>So... to the code:-
 </p>
