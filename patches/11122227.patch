diff --git a/11122227.orig.txt b/11122227.txt
--- a/11122227.orig.txt
+++ b/11122227.txt
@@ -36,5 +36,5 @@ Why isn't the variable $kards set to 5 as it should be?
 	#output: xtr kards = 5 from eval cmd : AKQJT98765432KKKK =~ tr/K//;
 </code>
 
-<p> but note that the code eval runs must be in double quotes, not in braces. so <code>$kards = eval {$xtr;} ; does NOT work. </p>
+<p> but note that the code eval runs must be in double quotes, not in braces. so <code>$kards = eval {$xtr;} ;</code> does NOT work. </p>
  
\ No newline at end of file
