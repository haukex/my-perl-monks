<?xml version="1.0" encoding="UTF-8"?>
<node id="1181061" title="Re: What are the rules on calling myself?" created="2017-02-04 04:03:10" updated="2017-02-04 04:03:10" _doctext_mode="strict"><type id="11">note</type>
<author id="830549">haukex</author>
<doctext field_name="doctext">&lt;p&gt;Hi [Todd Chester],&lt;/p&gt;
&lt;p&gt;Perl's prototypes are sometimes misunderstood to be something like function type signatures, but that's exactly not what they are. In Perl, [doc://perlsub#Prototypes] influence not only what subroutine arguments are accepted, but how the calls to those subroutines are parsed. They're not necessarily the best thing for validating subroutine arguments, and can sometimes lead to confusing code. I'd recommend Perl's prototypes primarily for cases where one wants to create functions whose syntax imitates Perl builtins.&lt;/p&gt;
&lt;p&gt;In your case, it's the "&lt;c&gt;( $ )&lt;/c&gt;" immediately after &lt;c&gt;sub RmdirAndLoop&lt;/c&gt;, which makes calls to &lt;c&gt;RmdirAndLoop&lt;/c&gt; be parsed like a unary operator. Unless it is your intention to change how calls to &lt;c&gt;RmdirAndLoop&lt;/c&gt; are parsed, as the AM said it's perfectly fine to remove the "&lt;c&gt;( $ )&lt;/c&gt;" prototype, and if desired add code to check the arguments.&lt;/p&gt;
&lt;p&gt;Just for completeness, there is another way to get rid of the warning, from the bottom of the [doc://perlsub#Prototypes] doc:&lt;/p&gt;
&lt;blockquote&gt;
&lt;p&gt;If a sub has both a PROTO and a BLOCK, the prototype is not applied until after the BLOCK is completely defined. This means that a recursive function with a prototype has to be predeclared for the prototype to take effect, like so:&lt;/p&gt;
&lt;c&gt;
sub foo($$);
sub foo($$) {
    foo 1, 2;
}
&lt;/c&gt;
&lt;/blockquote&gt;
&lt;p&gt;Hope this helps,&lt;br&gt;-- Hauke D&lt;/p&gt;</doctext>
<root_node field_name="root_node">1181055</root_node>
<parent_node field_name="parent_node">1181055</parent_node>
<thread id="1181055">
<node id="1181056"><node id="1181057"><node id="1181058"><node id="1181059"><node id="1181062"/>
</node>
</node>
</node>
</node>
<node id="1181061"><node id="1181106"/>
<node id="1181164"/>
</node>
</thread></node>
