<?xml version="1.0" encoding="UTF-8"?>
<node id="11115951" title="Perl beginner's issue with hash" created="2020-04-23 11:38:38" updated="2020-04-23 11:38:38" _doctext_mode="lenient"><type id="115">perlquestion</type>
<author id="1201390">Maire</author>
<doctext field_name="doctext">&lt;p&gt; Hi All, I'm a Perl beginner trying to do something very basic and encountering an issue. I want to find how many times each item in an array appears in a separate .txt document - when an item doesn't appear an error message should be printed. I have cobbled together the code below from various sources and it seemed to work at first glance, but the script only counts one instance of each item. &lt;/p&gt;

&lt;code&gt;
@ids = ("a", "b", "c", "d", "e", "f", "g",);

my %counts;

foreach my $id (@ids)
{
open(FILE, "list.txt" ) || die "couldn't open 2\n";
$/ = undef;

while (&lt;FILE&gt;) {
	if (/$id/g) {

		$counts{$id}++;
		}
		
		else { 
		
		print "$id not found\n";
		
		}
		
}

close FILE;
}

  use Data::Dumper;
  print Dumper \%counts;

&lt;/code&gt;

Sorry I can't work out how to include the contents of list.txt into the above code - I've included it below instead

&lt;code&gt;
a
b
b
c
c
d
d
d
f
f
f
&lt;/code&gt;

&lt;p&gt; This script returns:
&lt;code&gt;
e not found
g not found
$VAR1 = {
          'f' =&gt; 1,
          'c' =&gt; 1,
          'b' =&gt; 1,
          'd' =&gt; 1,
          'a' =&gt; 1
        };
&lt;/code&gt;

When I'm expecting:
&lt;code&gt;
e not found
g not found
$VAR1 = {
          'f' =&gt; 3,
          'c' =&gt; 2,
          'b' =&gt; 2,
          'd' =&gt; 3,
          'a' =&gt; 1
        };
&lt;/code&gt;
&lt;/p&gt;

&lt;p&gt; My uneducated guess is that it is the way I am reading in list.txt that is 'wiping' the hash, but unfortuantely I have no idea how to fix it. Any tips would be very much appreciated. Thank you in advance! &lt;/p&gt;</doctext>
<thread id="11115951">
<node id="11115952"><node id="11115953"><node id="11115969"/>
</node>
</node>
<node id="11115970"/>
<node id="11115975"/>
<node id="11116000"/>
</thread></node>
