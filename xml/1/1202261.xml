<?xml version="1.0" encoding="UTF-8"?>
<node id="1202261" title="Re: Resource management (updated)" created="2017-10-29 02:49:42" updated="2017-10-29 02:49:42" _doctext_mode="strict"><type id="11">note</type>
<author id="830549">haukex</author>
<doctext field_name="doctext">&lt;p&gt;Perl has no exact equivalent of Java's [https://docs.oracle.com/javase/tutorial/essential/exceptions/tryResourceClose.html|"try-with-resources"] mechanism, partly because Perl's exception management is different (e.g. the native [doc://open] and [doc://close] don't normally throw an exception, but return false values, so program flow can be handled differently) and because Perl does not have an equivalent of Java's &lt;c&gt;interface java.io.Closeable&lt;/c&gt;. Although lexical file handles are closed automatically when they are destroyed, this might not be true for some [doc://tie]d file handles, and although Perl's reference counting means that the time at which objects are destroyed is much more reliable than in Java, I would still not recommend relying on &lt;c&gt;DESTROY&lt;/c&gt; for everything*. This is because accidental circular references would prevent objects from being a garbage collected, and also, if one forgets to discard objects early enough, trying to clean up resources during global destruction can cause problems. Another issue is that the different kinds of resources you mentioned have different APIs, so the code to clean them up will be different. So unfortunately the answer to your question is that there is no single idiom built into Perl that applies to all the different resources you mentioned, other than closing them explicitly.&lt;/p&gt;
&lt;p&gt;However, Perl is of course a very flexible language that allows you to implement some kind of idiomatic/unified resource handling, if you like. How you would best do that depends on the details of your code, e.g. is it OO or not and what kind of resources specifically do you want to manage? Another thing that comes to mind is [mod://Try::Tiny], which gives you a nice little &lt;c&gt;try&lt;/c&gt;/&lt;c&gt;catch&lt;/c&gt;/&lt;c&gt;finally&lt;/c&gt; mechanism.&lt;/p&gt;
&lt;p&gt;* &lt;b&gt;Update:&lt;/b&gt; Note that I chose the wording "for everything" carefully. There are of course modules that specifically make use of Perl's reference counting and &lt;c&gt;DESTROY&lt;/c&gt; to perform actions at the end of a scope, such as e.g. [mod://End] or [mod://File::pushd] (although note how careful the latter has to be not to clobber [doc://$@]). So it is in theory possible to implement "auto close" behavior this way. However, I've already mentioned the major caveats to be considered, like accidental circular references, or in this case just accidentally storing a reference to the object somewhere else, will interrupt this mechanism. Because of these possible accidents I'd call it (for lack of a better term) less "theoretically reliable" than Java's &lt;c&gt;try&lt;/c&gt;. So this is why I recommend against &lt;c&gt;DESTROY&lt;/c&gt; for a generalized, idiomatic mechanism - but for situations where the scope is small and you can keep track of the references it may be applicable.&lt;/p&gt;</doctext>
<root_node field_name="root_node">1202253</root_node>
<parent_node field_name="parent_node">1202253</parent_node>
<thread id="1202253">
<node id="1202258"/>
<node id="1202261"><node id="1202266"><node id="1202304"/>
</node>
</node>
</thread></node>
