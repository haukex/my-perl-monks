<?xml version="1.0" encoding="UTF-8"?>
<node id="1193101" title="error variables after IPC::Run" created="2017-06-19 11:47:26" updated="2017-06-19 11:47:26" _doctext_mode="lenient"><type id="115">perlquestion</type>
<author id="979486">CarolinaPerler</author>
<doctext field_name="doctext">&lt;p&gt;
Using IPC::Run.&lt;br/&gt;
Would like to examine all the built-in special error variables: $@, $!, $^E, and $?.  After an IPC::Run 'run', when I try to check $! and $^E, I get 'Illegal seek'.&lt;br/&gt;
&lt;/p&gt;

&lt;code&gt;
run (\@commandArray, \$in, \$error, timeout(3));
print "LIB LEVEL ERROR: $!\n";
print "OS LEVEL ERROR: $^E\n";
&lt;/code&gt;

&lt;p&gt;On some systems in some situations, $^E is not supported.  But, $! should be available.  IPC::Run hands back $error (given the code above).  But, it does not necessarily give back all the detail that can be gleaned from cruising through the built-in error vars.

I like using IPC::Run and would like to continue doing so.  But, need a robust mechanism for catching error info.  It appears that IPC::Run is monkeying with $! and $^E.

Any ideas on how to capture the error detail after an IPC::Run run call?
&lt;/p&gt;

</doctext>
<thread id="1193101">
<node id="1193104"/>
<node id="1193105"/>
<node id="1193106"/>
<node id="1193108"/>
</thread></node>
