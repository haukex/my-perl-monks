<?xml version="1.0" encoding="UTF-8"?>
<node id="1227651" title="New Perl user - help with my homework" created="2018-12-24 05:43:46" updated="2018-12-24 05:43:46" _doctext_mode="lenient"><type id="115">perlquestion</type>
<author id="1227647">Eardrum</author>
<doctext field_name="doctext">&lt;p&gt;
Hey guys!
Edited: 

Write a program that calculates the average number from 1 to the number that the user inputs (in jumps of 2) and prints it to the screen.

&lt;code&gt;
example:

input:
Please enter a number
6

output:
(1+3+5)/3 = 3

&lt;/code&gt;

Here is the fixed code - it works perfectly - but would you guys have done anything differently? 


&lt;code&gt;
print "Please enter a number: \n";
my $limit = &lt;STDIN&gt;;
chomp $limit;
my $sum = 0;
my @array = ();
	for (my $i = 1; $i &lt;= $limit; $i+=2) {
		$sum += $i;
		push(@array,$i);
		
		#Just a note:
		print "\n i: $i, sum: $sum, array: @array, scalar: ", scalar(@array), "\n"; }

my $x = scalar(@array);
my $average = $sum/$x;
my $final = join("+",@array);
print "\n($final)/$x=$average\n";
&lt;/code&gt;



Second question:

 Write a program that prints (in a downgrading order - 10 each time) the numbers, starting with the number that the user inputs (until it reaches 0).

&lt;code&gt;
example:

input:
Please enter a number
52

output:
52 42 32 22 12 2
&lt;/code&gt;

Here's my code (would you guys have done anything differently?):

&lt;code&gt;
print "Please enter a number: \n";
my $limit = &lt;STDIN&gt;;
chomp $limit;
my $number = $limit;
my @array = ();
	for (my $i = 0; $i &lt;= $limit; $i+=10) {
		push(@array,$number);
		$number -= 10; }
print "@array\n";
&lt;/code&gt;
 

Third question:

Please enter a program that receives 4 number from the user. 
The program will put in an array only the numbers that are above 50.
Print to the screen the array variables, seperated by "-". 

&lt;code&gt;
example:

input:
Enter 4 numbers:
20
55
72
100

output:
55-72-100
&lt;/code&gt;

I wrote the code using "if", and it works as it should - but I'm required to use "for" loop. Can't seem to figure it out.

&lt;code&gt;
print "Please enter 4 numbers: \n";
my @numbers = &lt;STDIN&gt;;
chomp @numbers;
my @array = ();
	if ($numbers[0] &gt; 50) {
		push (@array,$numbers[0]) }
	if ($numbers[1] &gt; 50) {
		push (@array,$numbers[1]) }
	if ($numbers[2] &gt; 50) {
		push (@array,$numbers[2]) }
	if ($numbers[3] &gt; 50) {
		push (@array,$numbers[3]) }
my $final = join("-", @array);
print "$final\n";
&lt;/code&gt;


Fourth question:

 Write a program that receives 4 numbers from the user, and print to the screen only the even numbers (use operator % and "for" loop):

&lt;code&gt;
example:

input:
Please enter 4 numbers:
1
2
4
9

output:
2
4
&lt;/code&gt;

-Same as the previous question.

~~~~~~~~~~~~~~~~~~~~~~~

Fifth question: Write a program that receives from the user a number from the command line (@ARGV). The program will print to the screen a "triangle" with the same size as the number from the input:

&lt;code&gt;
example:

input:
6

output:
*
**
***
****
*****
******


input:
3
output:
*
**
***
&lt;/code&gt;

-With this one I don't even know where to start.

Help would be much appreciated.

Thank you!
&lt;/p&gt;</doctext>
<thread id="1227651">
<node id="1227652"/>
<node id="1227653"/>
<node id="1227654"><node id="1227665"/>
</node>
<node id="1227655"/>
<node id="1227670"><node id="1227687"/>
</node>
<node id="1227698"/>
<node id="1227757"/>
</thread></node>
