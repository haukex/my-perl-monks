<?xml version="1.0" encoding="UTF-8"?>
<node id="824281" title="Alternatives to Mixing Alarm and Sleep" created="2010-02-19 18:34:44" updated="2010-02-19 18:34:44" _doctext_mode="lenient"><type id="115">perlquestion</type>
<author id="509638">pileofrogs</author>
<doctext field_name="doctext">&lt;p&gt;Greetings ye Monks.&lt;/p&gt;

&lt;p&gt;I have a loop that sleeps at the end and I want to make the thing bail out if the whole loop takes too long.  The obvious solution:&lt;/p&gt;
&lt;code&gt;
alarm 10
while ( ! $done ) {
    $done = is_it_done_yet();
    sleep 1;
}
&lt;/code&gt;
&lt;p&gt;... is bad because perlfunc says: &lt;/p&gt;
&lt;blockquote&gt;
               It is usually a mistake to intermix "alarm" and "sleep" calls.
               ("sleep" may be internally implemented in your system with
               "alarm")
&lt;/blockquote&gt;
&lt;p&gt;So what's the "right" way to do this?&lt;/p&gt;

&lt;p&gt;Do I want to fork off a watcher process?  Is there a simpler way?  If there isn't a simpler way, is there a good CPAN module that handles this?&lt;/p&gt;

&lt;p&gt;For background: I'm writing an apache log rotater.  I want to compress the files once apache has stopped writing to them, which is what &lt;c&gt;is_it_done_yet()&lt;/c&gt; is analogous to.  My script checks to see if it can start compressing by using lsof ([mod://Unix::Lsof]) and compresses them using gzip if it can.  If lsof or gzip wander off and never return, I want to have some way to abort the whole thing.&lt;/p&gt;

&lt;p&gt;Thanks!&lt;br&gt;--Pileofrogs&lt;/p&gt;</doctext>
<thread id="824281">
<node id="824292"/>
<node id="824306"/>
<node id="824364"/>
</thread></node>
