<?xml version="1.0" encoding="UTF-8"?>
<node id="1180561" title="curl without backticks and system()" created="2017-01-29 11:12:15" updated="2017-01-29 11:12:15" _doctext_mode="lenient"><type id="115">perlquestion</type>
<author id="1123185">urbansumo</author>
<doctext field_name="doctext">Dear Monks,

&lt;p&gt;Someone told me that I should avoid using backticks and system() in my scripts. I therefore come to the gates with a couple of questions:&lt;/p&gt;

&lt;p&gt;1. Is using backticks and/or system() really that bad, ie is it ever appropriate to use backticks and what security issues does it pose if you do, or is it just considered sloppy (ie not purist) programming?&lt;/p&gt;

&lt;p&gt;2. My specific requirement is the use of curl in order to pull out the latest wordpress version from the header information, for example:&lt;/p&gt;

&lt;code&gt;
our $response = `curl -sILk wordpress.org/latest | grep Content-Disposition`;
&lt;/code&gt;

Here is what the $response variable would look like:

&lt;code&gt;
Content-Disposition: attachment; filename=wordpress-4.7.2.tar.gz
&lt;/code&gt;


&lt;p&gt;This response variable is then passed through a regex filter to return the version number only (4.7.2).&lt;/p&gt;

&lt;p&gt;Here is the full subroutine in all its glory:&lt;/p&gt;

&lt;code&gt;
sub get_latest_wordpress_version {
        our $url = "https://wordpress.org/latest";
        our $response = `curl -sILk wordpress.org/latest | grep Content-Disposition`;
        our ($wp_latest_version) = $response =~ /(\d+.\d+(.\d+)?)/;
        return $wp_latest_version;
}
&lt;/code&gt;

&lt;p&gt;This works perfectly, however, I'm looking for a way to not have to run system commands to achieve this, but the kicker is, I'm looking to achieve this using only core perl modules, to ensure maximum portability of the code.&lt;/p&gt;

&lt;p&gt;I've done a lot of searching for ways to achieve this but hit roadblocks that mean the backticks are here to stay for a while.&lt;/p&gt;

&lt;p&gt;Any help you can provide will be gratefully received.&lt;/p&gt;

&lt;p&gt;Urbansumo&lt;/p&gt;



</doctext>
<thread id="1180561">
<node id="1180562"/>
<node id="1180565"><node id="1180664"/>
</node>
<node id="1180578"><node id="1180661"/>
<node id="1180662"/>
</node>
</thread></node>
