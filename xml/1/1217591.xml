<?xml version="1.0" encoding="UTF-8"?>
<node id="1217591" title="PERL: newlines truncated from sqlplus output in perl" created="2018-06-29 01:41:02" updated="2018-06-29 01:41:02" _doctext_mode="lenient"><type id="115">perlquestion</type>
<author id="1217589">harshitb</author>
<doctext field_name="doctext">Hello Folks,

I have been trying to run a perl script to generate the output in .txt file. I am actually executing a sqlplus procedure using system command to write output in a .txt file. The sqlplus gives output in newline separated form, but after executing the system command, it is somehow truncating the trailing newlines from everyline, effectively writing the output to a single large line.

The code I am trying to execute is:
$cmd = "echo 'exec javasplat.create_test_list(\'tests.txt\', \'$startStr\', \'$endStr\');' | $sqlplusconnect" ;
system($cmd);

I am doing the above thing in bash environment. 
After going through few reviews, I came to conclusion that bash command substitution is removing trailing newlines. I tried setting the $IFS variable to empty string, it didn't work as it wasn't there at first place. 

Can you guys suggest me how do I prevent my script from truncating newline characters? 

Thanks,
Harshit
</doctext>
<thread id="1217591">
<node id="1217592"/>
<node id="1217598"/>
</thread></node>
