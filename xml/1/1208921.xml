<?xml version="1.0" encoding="UTF-8"?>
<node id="1208921" title="Is there any (performance) benefit to importing functions?" created="2018-02-10 16:17:25" updated="2018-02-10 16:17:25" _doctext_mode="lenient"><type id="115">perlquestion</type>
<author id="1204428">YenForYang</author>
<doctext field_name="doctext">&lt;p&gt;
(This question is composed of sub-questions)
&lt;/p&gt;
&lt;p&gt;
I've noticed that there are some functions that don't require &lt;code&gt;use&lt;/code&gt;-ing or &lt;code&gt;require&lt;/code&gt;-ing to use: for instance, the &lt;code&gt;Time::HiRes::&lt;/code&gt; functions, the &lt;code&gt;utf8::&lt;/code&gt; utility functions, Cwd::getcwd(), etc.
&lt;/p&gt;
&lt;p&gt;
1st question (sort of offtopic): where do I find a reference/list of these functions which require no &lt;code&gt;require&lt;/code&gt; or &lt;code&gt;use&lt;/code&gt; to use?
&lt;/p&gt;
&lt;p&gt;
I had thought at first that maybe all core modules could be called via long name without require, but then I realized that that's not the case (e.g. &lt;code&gt;Data::Dumper::Dumper()&lt;/code&gt;, &lt;code&gt;File::Copy::&lt;/code&gt;, etc.)...so that's one of the reasons why I'm curious about which (module) functions can be called with require or use.
&lt;/p&gt;
&lt;p&gt;
But that got me thinking also about (2nd question) whether or not it makes a performance difference when using "long" package names (regardless of whether &lt;code&gt;require&lt;/code&gt; is required) vs. &lt;code&gt;use&lt;/code&gt;-ing (&lt;code&gt;require&lt;/code&gt;-ing and &lt;code&gt;import&lt;/code&gt;ing) and calling the subroutine from my namespace directly. I mean does perl have to convert long names into paths or something to locate the function everytime (just a wild guess, because it might make a difference in that case)? In some of my perl scripts I actually load modules that use XS via &lt;code&gt;XSLoader::load()&lt;/code&gt; directly (and &lt;code&gt;XSLoader&lt;/code&gt; doesn't require a &lt;code&gt;require&lt;/code&gt;, a surprise to me) directly, and then I have to call the functions I want to use via long names each time.
&lt;/p&gt;
&lt;p&gt;These things might seem trivial to most, but I'm just really curious and would probably do things a little differently if I knew what was going on. Sometimes I try to avoid conveniences if it leads to benefits in other areas.&lt;/p&gt;
</doctext>
<thread id="1208921">
<node id="1208922"><node id="1208925"><node id="1208926"><node id="1208931"/>
</node>
</node>
</node>
<node id="1208928"/>
<node id="1208929"><node id="1208932"/>
</node>
<node id="1209005"/>
</thread></node>
