<?xml version="1.0" encoding="UTF-8"?>
<node id="11129361" title="system &gt;&gt; 8 is non-zero when child exits with exit(0)" created="2021-03-09 00:20:23" updated="2021-03-09 00:20:23" _doctext_mode="lenient"><type id="115">perlquestion</type>
<author id="125011">bjdean</author>
<doctext field_name="doctext">&lt;p&gt;I have a new situation which appears to have arrived after an upgrade to debian buster.&lt;/p&gt;

&lt;p&gt;I have some scripts in which &lt;c&gt;system(...)&lt;/c&gt; is used to call other perl scripts, and those scripts are calling &lt;c&gt;exit&lt;/c&gt; explicitly and so should be returning a zero exit code (once &gt;&gt;8 is applied to the value returned by &lt;c&gt;system&lt;/c&gt;).&lt;/p&gt;

&lt;p&gt;The expected behaviour almost always happens.&lt;/p&gt;

&lt;p&gt;However in some cases the exit code returned by system is 1 - and I've been unable to either reliably reproduce the behaviour (it just happens sometimes in processes running on the server) nor stop it happening.&lt;/p&gt;

&lt;p&gt;I know that the &lt;c&gt;$CHILD_ERROR / $?&lt;/c&gt; perl variable can be used to change the exit code from a process - eg from the perl docs on perlvar:&lt;/p&gt;

&lt;blockquote&gt;
&lt;c&gt;
END {
    $? = 1 if $? == 255;  # die would make it 255
}
&lt;/c&gt;
&lt;/blockquote&gt;

&lt;p&gt;And I've looked high and low for any code (or modules used) which are doing this and have not found any.&lt;/p&gt;

&lt;p&gt;So my question is this! Why would the perl have started doing this (I've not been able to find a known bug), and has anyone else started seeing &lt;c&gt;system() &gt;&gt; 8 &lt;/c&gt; returning 1 unexpectedly?&lt;/p&gt;</doctext>
<thread id="11129361">
<node id="11129364"><node id="11129375"><node id="11129376"><node id="11129388"><node id="11129439"><node id="11129805"/>
</node>
<node id="11129470"><node id="11129806"/>
</node>
</node>
</node>
</node>
</node>
<node id="11134142"/>
</thread></node>
