<?xml version="1.0" encoding="UTF-8"?>
<node id="11107671" title="Force Getopt::Long to Require ONLY Equals Sign Between Option Name and Value" created="2019-10-18 16:36:19" updated="2019-10-18 16:36:19" _doctext_mode="lenient"><type id="115">perlquestion</type>
<author id="140424">roho</author>
<doctext field_name="doctext">Here is what I am trying to do.  I have a command line option that is defined in Getopt::Long as having an optional string value as follows:
&lt;p&gt;
&lt;code&gt;
use Getopt::Long;
GetOptions( "ot:s" =&gt; \$ot ) or die("Error in command line arguments\n");
&lt;/code&gt;
&lt;p&gt;
The problem occurs when -ot (without a value) is followed by either another option or a program argument. The -ot option will take the next word as its value.
&lt;p&gt;
I know I can signal the end of options by using -- on the command line, but for backward compatibility before this program used Getopt::Long (the program previously used &lt;b&gt;"/usr/bin/perl -s"&lt;/b&gt; to process command line options), I would like to tell Getopt::Long to always require an equals sign before a value so that -ot (without a value) can be followed by a program argument without the invervening -- in the command line.
&lt;p&gt;
Here is what I am aiming for:
&lt;p&gt;
&lt;b&gt;Scenario #1:&lt;/b&gt;
&lt;code&gt;
myprog.pl -ot=123 arg1 arg2 ...
&lt;/code&gt;
&lt;p&gt;
This works fine. Variable $ot='123'
&lt;p&gt;
&lt;b&gt;Scenario #2:&lt;/b&gt;
&lt;code&gt;
myprog.pl -ot arg1 arg2 ...
&lt;/code&gt;
&lt;p&gt;
I need $ot to be set to the empty string '' instead of 'arg1', &lt;i&gt;without the need of an intervening --&lt;/i&gt; on the command line (hence the need to force Getopt::Long to &lt;u&gt;require&lt;/u&gt; an equals sign between the option name and its value).
&lt;p&gt;
Again, my goal is backward compatibility with the way this program was called before adding Getopt::Long, because this program is very central to the system and is literally called in thousands of places. TIA for any suggestions.

&lt;!-- Node text goes above. Div tags should contain sig only --&gt;
&lt;div class="pmsig"&gt;&lt;div class="pmsig-140424"&gt;
&lt;p&gt;
"It's not how hard you work, it's how much you get done."
&lt;/div&gt;&lt;/div&gt;</doctext>
<thread id="11107671">
<node id="11107672"/>
<node id="11107679"/>
<node id="11107686"><node id="11107712"/>
</node>
</thread></node>
