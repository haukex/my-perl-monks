<?xml version="1.0" encoding="UTF-8"?>
<node id="11138881" title="Re: Difference in Module loading and install??" created="2021-11-16 03:47:47" updated="2021-11-16 03:47:47" _doctext_mode="strict"><type id="11">note</type>
<author id="830549">haukex</author>
<doctext field_name="doctext">&lt;p&gt;Unfortunately, I'm having a bit of trouble understanding parts of your description. A [id://1177642] that we could use to reproduce your situation would go a long way in demonstrating your exact situation and would allow us to answer quicker and better.&lt;/p&gt;
&lt;blockquote&gt;&lt;i&gt;This script contains line "package That::Module" to use the subroutine of a package in a module.&lt;/i&gt;&lt;/blockquote&gt;
&lt;p&gt;Note that [doc://package] does not load another module, all it does is define that the following code is in that namespace. Using [doc://package] to access the code defined in another package is not recommended - by that I mean, for example, if there is a module &lt;c&gt;Other::Module&lt;/c&gt; containing a &lt;c&gt;sub foo&lt;/c&gt;, one could theoretically write &lt;c&gt;package Other::Module;&lt;/c&gt; to switch to that namespace and then call &lt;c&gt;foo()&lt;/c&gt;, provided that some other piece of code has loaded &lt;c&gt;Other::Module&lt;/c&gt;. Instead, one should either write &lt;c&gt;Other::Module::foo()&lt;/c&gt; to call that function, or use an import mechanism like &lt;c&gt;use Other::Module qw/foo/;&lt;/c&gt; (provided that &lt;c&gt;Other::Module&lt;/c&gt; has implemented this).&lt;/p&gt;
&lt;blockquote&gt;&lt;i&gt;I want to know how my simulation script runs successfully without the phrase "use".&lt;/i&gt;&lt;/blockquote&gt;
&lt;p&gt;I have to guess, but perhaps this is because some other module somewhere is already loading that module. So for example, if your script does &lt;c&gt;use My::Module;&lt;/c&gt;, and &lt;c&gt;My::Module&lt;/c&gt; does &lt;c&gt;use Other::Module;&lt;/c&gt;, then your script could access &lt;c&gt;Other::Module&lt;/c&gt;. However, this is not a best practice, and your script should explicitly load all of the modules it wants to make use of.&lt;/p&gt;
&lt;blockquote&gt;&lt;i&gt;And want to know how to add(or load? or install? I have some confusion about this.) module and run script2 without the phrase "use" in my test directory.&lt;/i&gt;&lt;/blockquote&gt;
&lt;p&gt;The [doc://use] is generally necessary to load a module. There are some alternatives like [doc://require] but that is often not the best practice. If your question is rather about [doc://@INC], which lists the directories where [doc://use] looks for modules, there are multiple ways to add custom directories there as well.&lt;/p&gt;
&lt;p&gt;The term "installing" a module typically refers to downloading a module e.g. from the CPAN using a tool like [mod://cpanm]. Sometimes, it is used more loosely to mean copying a &lt;c&gt;.pm&lt;/c&gt; file into one of the [doc://@INC] directories.&lt;/p&gt;
&lt;p&gt;&lt;small&gt;&lt;i&gt;Edit: Typo fixes.&lt;/i&gt;&lt;/small&gt;&lt;/p&gt;</doctext>
<root_node field_name="root_node">11138880</root_node>
<parent_node field_name="parent_node">11138880</parent_node>
<thread id="11138880">
<node id="11138881"/>
<node id="11138885"><node id="11138926"/>
</node>
<node id="11138930"/>
<node id="11138944"><node id="11138955"/>
<node id="11138957"/>
</node>
</thread></node>
