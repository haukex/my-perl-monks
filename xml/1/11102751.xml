<?xml version="1.0" encoding="UTF-8"?>
<node id="11102751" title="Script works fine in cmd but doesn't print to txt" created="2019-07-12 14:20:35" updated="2019-07-12 14:20:35" _doctext_mode="lenient"><type id="115">perlquestion</type>
<author id="11102579">clothespeg</author>
<doctext field_name="doctext">Hello!

This works in cmd just fine
&lt;code&gt;
open(File, "file.txt");
while($text = &lt;File&gt;){
@words = split(' ', $text);
for $word(@words){
print "$word\n"}}; 
&lt;/code&gt;

I am trying to get it to print in a txt file, but can't figure out why something like the following is not working:
&lt;code&gt;
open(S, 'file.txt');
while($text = &lt;S&gt;){
@words = split(' ', $text);
for $word(@words){
my $filename = 'report.txt';
open(my $fh, '&gt;', $filename) or die "Could not open file '$filename' $!";
print $fh "$word\n";
close $fh;
print "done\n";
}};
&lt;/code&gt;

I still consider myself new to perl so there must be something I don't get. 

Thanks in advance. 

</doctext>
<thread id="11102751">
<node id="11102752"/>
<node id="11102758"/>
</thread></node>
