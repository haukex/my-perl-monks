<?xml version="1.0" encoding="UTF-8"?>
<node id="1199211" title="Re: string replacement" created="2017-09-12 11:41:50" updated="2017-09-12 11:41:50" _doctext_mode="lenient"><type id="11">note</type>
<author id="1039428">Eily</author>
<doctext field_name="doctext">&lt;p&gt;Please edit your post to correct the formatting. See [id://674668]. You should at least use &amp;lt;code&gt; tags around your config file so that it can be displayed properly.&lt;/p&gt;
&lt;p&gt;The "search and replace from a list of replacements" part can be done using a hash, and the substitution operator.
&lt;c&gt;use strict;
use warnings;
my $text = "I will not stir from this place, do what they can.";

my %replacements = (
                     "will not"         =&gt; "won't",
                     "stir"             =&gt; "move",
                     "this place"       =&gt; "here",
                     "do what they can" =&gt; "let them try");


my $pattern = join "|", map quotemeta, keys %replacements;

print "The pattern is: $pattern\n";
my $replaced = $text =~ s/($pattern)/$replacements{$1}/gr;

print $replaced;&lt;/c&gt;
The pattern part is a [doc://perlre|regular expression] if you don't know about them you can look at [id://519265|those tutorials].&lt;br /&gt;
The [doc://quotemeta] escapes the special characters in the search items so that they are interpreted litteraly in the regular expression.&lt;br /&gt;
The s/search/replace/ part is what does the actual job, the search part will capture the strings it finds (because of the parenthesis) into $1, and $replacements{$1} will return the matching replacement. The g in /gr means "general", and will apply the change everywhere in the string, not just once, and the r means "return", so the result is returned to $replaced, instead of changing $text itself.&lt;/p&gt;

&lt;p&gt;As for reading your config file, one of the [http://search.cpan.org/search?mode=module&amp;query=config|config] modules might do what you want.&lt;/p&gt;</doctext>
<root_node field_name="root_node">1199207</root_node>
<parent_node field_name="parent_node">1199207</parent_node>
<thread id="1199207">
<node id="1199209"/>
<node id="1199211"><node id="1199222"><node id="1199232"/>
</node>
<node id="1199327"/>
</node>
</thread></node>
