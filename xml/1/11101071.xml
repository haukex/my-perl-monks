<?xml version="1.0" encoding="UTF-8"?>
<node id="11101071" title="Re^4: What is an ordinary statement?" created="2019-06-06 19:04:50" updated="2019-06-06 19:04:50" _doctext_mode="lenient"><type id="11">note</type>
<author id="1080444">RonW</author>
<doctext field_name="doctext">&lt;blockquote&gt;&lt;i&gt;it sounds to me like you might be drawing too many parallels between Perl and a truly compiled language such as C/C++, where there are things such as generating a binary and a very clear distinction between compile time and run time.&lt;/i&gt;&lt;/blockquote&gt;
&lt;p&gt;Perl is not as different as you suggest.&lt;/p&gt;
&lt;p&gt;perl compiles Perl code into Perl op codes and operands which are then executed by the Perl virtual machine (PVM).&lt;/p&gt;
&lt;p&gt;In theory, the PVM could be implemented in hardware. Many of the Perl op codes correspond to common CPU opcodes. However, the operands are structures. And many of the op codes are more like "system traps" that call a system API function. The PVM is high level machine.&lt;/p&gt;
&lt;p&gt;But, it is possible to generate a "binary" file. The [https://metacpan.org/pod/perlcompile#The-Bytecode-Backend|Bytecode] backend writes Perl bytecode to a file, which the [https://metacpan.org/pod/perlcompile#ByteLoader|ByteLoader] can feed into the PVM.&lt;/p&gt;
&lt;p&gt;One of the major problems with this is that if code in &lt;c&gt;BEGIN&lt;/c&gt; blocks or in &lt;c&gt;use&lt;/c&gt;'d files has "run time side effects", those won't happen when you run the previously compile program.&lt;/p&gt;
&lt;p&gt;Bytecode isn't the only option for saving compiled Perl. The Simple C backend encodes Perl's op codes and operands into C source that, when compiled by a C compiler will produce an executable that feeds the compiled Perl code into the PVM. The optimized C backend attempts to de-compile the Perl ops and operands into C code.&lt;/p&gt;
&lt;p&gt;But, again, any code in &lt;c&gt;BEGIN&lt;/c&gt; blocks or in &lt;c&gt;use&lt;/c&gt;'d files won't be run.&lt;/p&gt;
&lt;p&gt;If a Perl program is written to have no "run time side effects" at compile time - and only uses modules that are similarly coded, then storing a compiled Perl program in a file for later execution is an option.&lt;/p&gt;</doctext>
<root_node field_name="root_node">11100959</root_node>
<parent_node field_name="parent_node">11101017</parent_node>
<thread id="11100959">
<node id="11100961"><node id="11101004"/>
</node>
<node id="11100972"><node id="11100986"><node id="11101017"><node id="11101071"><node id="11101084"><node id="11101302"/>
</node>
</node>
</node>
</node>
<node id="11101039"><node id="11101068"/>
</node>
</node>
<node id="11100973"/>
<node id="11100974"/>
<node id="11100978"><node id="11101021"/>
</node>
<node id="11101006"/>
<node id="11101035"><node id="11101040"/>
<node id="11101045"/>
<node id="11101057"/>
</node>
<node id="11101121"/>
<node id="11101664"/>
</thread></node>
