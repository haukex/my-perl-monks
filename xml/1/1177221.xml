<?xml version="1.0" encoding="UTF-8"?>
<node id="1177221" title="Printing to STDOUT buffered when invoking Perl within a bash-script using tee" created="2016-12-05 10:01:00" updated="2016-12-05 10:01:00" _doctext_mode="lenient"><type id="115">perlquestion</type>
<author id="1069776">TJCooper</author>
<doctext field_name="doctext">&lt;p&gt;As a solution to recording terminal output into a log file, whilst still viewing output on the terminal (using code within a bash script itself rather than piping to tee on the command-line) I typically use:&lt;/p&gt;

&lt;code&gt;exec &gt; &gt;(tee "$DIR/Logs/System.log")&lt;/code&gt;

&lt;p&gt;Near the top of the bash script.&lt;/p&gt;

&lt;p&gt;This bash script eventually invokes a Perl script. Output printed to the terminal comes from both Bash and Perl, however, the inclusion of the above line causes the buffering of the Perl-specific portion of the output such that it's only displayed on the terminal once the script fully completes. Running the Perl script as a standalone provides the intended real-time updates, as does invoking it via Bash without the above line.&lt;/p&gt;

Could anybody explain why this is happening and also suggest a solution? The ultimate goal here is to provide real-time feedback to the user on the terminal, whilst keeping a hard-copy of this feedback in a log file. </doctext>
<thread id="1177221">
<node id="1177222"><node id="1177223"><node id="1177229"/>
</node>
</node>
</thread></node>
