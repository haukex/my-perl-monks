<?xml version="1.0" encoding="UTF-8"?>
<node id="11123126" title="Applying regex to each line in a record." created="2020-10-24 14:40:02" updated="2020-10-24 14:40:02" _doctext_mode="lenient"><type id="115">perlquestion</type>
<author id="1182149">pritesh_ugrankar</author>
<doctext field_name="doctext">&lt;p&gt;Hi Monks,&lt;/p&gt;
&lt;p&gt;Even after multiple attempts, I am at a total loss of how the "m" and "s" works for regex. I have a file like this:
&lt;/p&gt;
&lt;code&gt;
first:
this:that
here:there
when:what
how:where
now:later

second:
this:that
here:there
when:what
how:where
now:later
&lt;/code&gt;
&lt;p&gt;Please note that in the example, "this:that", "here:there", etc are repeated, but that's not the case with the actual record I am working on. I am trying to write a script that will create a hash of hashes such that I get a hash with a key "first" and its value will be a key value pair such that the key will be "this" and value will be "that", key will "here" and value will be "there", and so on and so forth. But even before I reach there, I need to ensure that I write the correct regex. So, I tried to write a regex that will skip the line if it contains "first" or "second". Of course if the regex works, I can then capture the part and make that as a hashkey, but that's much later. &lt;/p&gt;
&lt;p&gt;I've tried the following, but it does not work. And I am pretty sure it's because my utter failure to understand how "m" and "s" work.
&lt;/p&gt;
&lt;code&gt;
use strict;
use warnings;
use Data::Dumper;
my $file = "new_testfile.txt";
my $testhashref;
open (my $fh, "&lt;",$file) or die "Can't open open file $file:$!"; 

{

	local $/ = "";
	while (my @records = &lt;$fh&gt;) {
		foreach my $line (@records) {
			next if $line =~ /^[a-z]+:$/m;
			print "$line";
		}
   }
}

&lt;/code&gt;
&lt;p&gt;I tried using "s" instead of "m", but when I run the script, it does not read anything.&lt;/p&gt;
&lt;code&gt;
pritesh@pavilion:~/perlscripts$ perl test.pl
pritesh@pavilion:~/perlscripts$ 
&lt;/code&gt;
&lt;p&gt;If I remove the &lt;code&gt;next if $line =~ /^[a-z]+:$/m;&lt;/code&gt;, I get the whole file like so:&lt;/p&gt;
&lt;code&gt;
pritesh@pavilion:~/perlscripts$ perl test.pl
first:
this:that
here:there
when:what
how:where
now:later

second:
this:that
here:there
when:what
how:where
now:later
&lt;/code&gt;
&lt;p&gt;So at least I know it's reading the records right. I will be thankful if you could help me with this one.&lt;/p&gt;</doctext>
<thread id="11123126">
<node id="11123129"/>
<node id="11123130"/>
<node id="11123133"><node id="11123136"/>
</node>
<node id="11123134"/>
<node id="11123135"><node id="11123138"><node id="11123150"/>
</node>
</node>
<node id="11123137"/>
<node id="11123139"><node id="11123154"/>
</node>
<node id="11123145"><node id="11123153"/>
</node>
<node id="11123165"/>
</thread></node>
