<?xml version="1.0" encoding="UTF-8"?>
<node id="11115386" title="accessing an anonymous array" created="2020-04-11 15:55:56" updated="2020-04-11 15:55:56" _doctext_mode="lenient"><type id="115">perlquestion</type>
<author id="659356">WoodyWeaver</author>
<doctext field_name="doctext">&lt;p&gt;pray help a poor wanderer with syntax&lt;/p&gt;

I am populating a table with some statistics, e.g. 
&lt;code&gt;
    $tables{$day}-&gt;{$state} = \[$deathsToday, $m, $double, $intercept, $corr];
&lt;/code&gt;

These  are grouped in my mind as an array, so that's why I used the anonymous array above.  So then it gets used later, viz

&lt;code&gt;
    foreach my $point ( @${$tables{$day}-&gt;{$state}} ) {
      print $fh "\t", $point;
&lt;/code&gt;

After time, I want to add a feature, for which I need to access just the first element.  It seems like it should be something like &lt;code&gt;${$tables{$day}-&gt;{confirmed}}[0];&lt;/code&gt; but that isn't right.  &lt;code&gt;dump $tables{$day}&lt;/code&gt; helpfully offers
&lt;code&gt;
{
  confirmed =&gt; \[1, 0.334638642561918, 2, 0.873307778517249, 0.853943857158699],
  deaths    =&gt; \[],
}
&lt;/code&gt;
(This is a covid visualization thing.)

I could of course convert that five element array to a hash (and actually, just because it comes back from a linear regression as an array doesn't mean it has to stay that way) but I'd like to understand the syntax. Pray, give insight to this lost soul?</doctext>
<thread id="11115386">
<node id="11115387"/>
<node id="11115398"/>
<node id="11115442"><node id="11115443"><node id="11115487"/>
</node>
</node>
</thread></node>
