<?xml version="1.0" encoding="UTF-8"?>
<node id="11109616" title="passing an array without a name to pop()" created="2019-12-03 14:09:35" updated="2019-12-03 14:09:35" _doctext_mode="lenient"><type id="115">perlquestion</type>
<author id="11100429">TieUpYourCamel</author>
<doctext field_name="doctext">I'm off on a tangent of a tangent, not really solving a real problem, but I've come across this oddity and it's really bugging me.  In each online example or tutorial for &lt;code&gt;pop()&lt;/code&gt; they always use a named array.  Is it possible to not do that?  Instead of this:

&lt;code&gt;
my @odds = (1,3,5,7,9);
print pop(@odds);
&lt;/code&gt;

Is there a way to combine that onto one line?  I've tried several variations, all of them generate an error, shown in the comments below the code:

&lt;code&gt;
use strict;
use warnings;
use v5.30;

#works
my @odds = (1,3,5,7,9);
print pop(@odds);
#9 (no error)


print pop((2,4,6));
# Experimental pop on scalar is now forbidden ... near "))"

print pop(@(2,4,6));
# Number found where operator expected ... near "@(2"
#       (Missing operator before 2?)
# syntax error ... near "@(2"

print pop(@{2,4,6});
# Useless use of a constant (2) in void context ... line 19.
# Useless use of a constant (4) in void context ... line 19.
# Can't use string ("6") as an ARRAY ref while "strict refs" in use ... line 19.
&lt;/code&gt;

What's particularly curious is I know I've used the &lt;code&gt;@{}&lt;/code&gt; construct before to explicitly define an array, using XML::Simple.
&lt;code&gt;
$ErrorResponse = $xml-&gt;XMLin($objResponse-&gt;content, ForceArray =&gt; 1);
@Errors = @{$ErrorResponse-&gt;{Errors}};
&lt;/code&gt;

I must be missing something, particularly in that first example: &lt;code&gt;print pop((2,4,6));&lt;/code&gt; generates the error "Experimental pop on scalar is now forbidden".  However, &lt;code&gt;(2,4,6)&lt;/code&gt; clearly isn't a scalar.

</doctext>
<thread id="11109616">
<node id="11109617"><node id="11109622"><node id="11109628"/>
</node>
</node>
<node id="11109618"/>
<node id="11109623"/>
<node id="11109629"/>
<node id="11109630"/>
<node id="11109631"/>
<node id="11109633"/>
<node id="11109647"/>
</thread></node>
