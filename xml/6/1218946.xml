<?xml version="1.0" encoding="UTF-8"?>
<node id="1218946" title="Can you limit the scope of require?" created="2018-07-20 14:58:16" updated="2018-07-20 14:58:16" _doctext_mode="lenient"><type id="115">perlquestion</type>
<author id="1218939">taylorK</author>
<doctext field_name="doctext">&lt;p&gt;Hi Monks,

&lt;/p&gt;&lt;p&gt;First post so I apologize if I miss something or if there are small errors in the example I provided, I am just using it to explain my issue. I have run into this situation a few times now and have not been able to track down an answer. Let me be clear that this is for a work application so I cannot simply change the current setup of files etc...

&lt;/p&gt;&lt;p&gt;Background: We use multiple .conf files to house "parsers" for data files. Each of these .conf files has a sub that contains a header map in the form of an array. These .conf files are not packages.

&lt;/p&gt;&lt;p&gt;Question: I would like to write a program that will get that header map from one of these .conf files then get it from a different one as well but only for the purposes of a very simple operation. My current method is to use a require (as seen below) and grab the sub. The problem is when I use require a second time to get the second .conf file it completely overwrites the first require. Can I call this second one and have the scope of it limited to a particular subroutine or variable?

&lt;p&gt;Example:

&lt;/p&gt;&lt;code&gt;
if (-e "myfilepath/test1.conf") {
    require "myfilepath/test1.conf" or die("myfilepath/test1.conf - $!\n");
}
else {
    die("Missing provider config file: myfilepath/test1.conf\n");
}

$header_map = &amp;get_config;

# grab a file to parse
while (defined(my $line = &lt;FILE&gt;)) {
    my $second_file = &amp;compare();
    # split $line into the header
    if ($first_file{test} =~ /$seconf_file{test}/i) {
        # etc....
    }
}

sub compare {
    if (-e "myfilepath/test2.conf") {
        require "myfilepath/test2.conf" or die("myfilepath/test2.conf - $!\n");
    }
    else {
        die("Missing provider config file: myfilepath/test2.conf\n");
    }
    $header_map_2 = &amp;get_config;

    # grab a file to parse
    while (defined(my $line = &lt;FILE&gt;)) {
        # split $line into the header
    }
    return "$parsed_line";
}
&lt;/code&gt;


&lt;p&gt;Thanks!&lt;/p&gt;</doctext>
<thread id="1218946">
<node id="1218949"/>
<node id="1218954"/>
<node id="1218969"/>
</thread></node>
