<?xml version="1.0" encoding="UTF-8"?>
<node id="11143126" title="Unexpected behavior of Perl on non-breaking space in Mac environment" created="2022-04-20 09:40:40" updated="2022-04-20 09:40:40" _doctext_mode="lenient"><type id="115">perlquestion</type>
<author id="11143124">hishii2001</author>
<doctext field_name="doctext">&lt;p&gt;Hi, I am trying to find a solution around unexpected behavior of the output with my Perl script.&lt;/p&gt;

&lt;p&gt;I have a set of files in non-English language.  This particular language uses non-breaking space (U+00A0) quite often instead of regular space.  The use of non-breaking space character is intentional, and it is very important to use the character instead of normal space character for this particular language.&lt;/p&gt;

&lt;p&gt;What my Perl script does is to change language code "en" for English to something else that is appropriate to the language.  So, I'm simply using the regular expression to search for particular sequence of letter and replacing them to something else.  That's all it does.  Then the script saves the text in a text-based file.  Script does this for thousands of files.&lt;/p&gt;

&lt;p&gt;The problem I'm encountering is that when the original file has non-breaking space character (U+00A0), Perl processes the text, but saves the non-breaking space character as "\_" &lt;/p&gt;

&lt;p&gt;I'm reading the original file as UTF-8 file because the files are saved in UTF-8 to handle non-ASCII characters.  All non-ASCII characters used in the foreign language are handled correctly with correct accents, but only the non-breaking space character is converted to something else in the output file.&lt;/p&gt;

&lt;p&gt;For example, if I have input text:&lt;/p&gt;

&lt;code&gt;issue: "Problém s odesláním"&lt;/code&gt;

&lt;p&gt;The output text becomes as below:&lt;/p&gt;

&lt;code&gt;issue: "Problém s\_odesláním"&lt;/code&gt;

&lt;p&gt;The space character before the "s" character is a normal space character (U+0020), but the space character after the "s" character is a non-breaking space character (U+00A0).&lt;/p&gt;

&lt;p&gt;Does anybody have any idea how to save the non-breaking space as non-breaking space character in without converting to "\_"?  I'm experiencing this issue only in Macintosh environment.  If I use the same Perl code in Windows environment, I do not have this issue.  I appreciate any input.  Thank you in advance.&lt;/p&gt;</doctext>
<thread id="11143126">
<node id="11143127"><node id="11143133"><node id="11143140"/>
</node>
</node>
<node id="11143128"><node id="11143132"/>
</node>
<node id="11143131"><node id="11143134"/>
</node>
<node id="11143146"/>
</thread></node>
