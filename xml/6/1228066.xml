<?xml version="1.0" encoding="UTF-8"?>
<node id="1228066" title="Re^2: locating ultima thule" created="2019-01-05 01:32:19" updated="2019-01-05 01:32:19" _doctext_mode="lenient"><type id="11">note</type>
<author id="947695">Aldebaran</author>
<doctext field_name="doctext">&lt;i&gt;it was, after all, scraping a Fourmilab page for a table that does not contain the object you seek, but only the "major" solar system objects, which, unless it is staying close to one of those objects, did not seem that applicable&lt;/i&gt;

&lt;p&gt;This is true indeed, and thanks for your comments. I looked at the module you posted and the long and short of it is I don't know how to track a single object in space, even possessing the physical values necessary. Nor did I find a perl way to tap into the services at the national jet lab. The ephemeris is the one way I know how to sort out the solar system. It's why I find this so extraordinary. How often do the Gods throw a Pluto Party, have all the titans show up, and leave only us behind to snap the pictures?&lt;/p&gt;
&lt;p&gt;Ultima Thule is essentially invisible from where we are. Its great fortune was being able to occult a star, allowing for predictions of its morphology, which I think is taken to mean composition and shape in this context. These were verified with the fly-by.&lt;/p&gt;
&lt;p&gt;I've marked where I think Ultima Thule is, but I can't get the page to load now, scratching my head as to why:&lt;/p&gt;


&lt;c&gt;a is /home/bob/2.scripts/pages/2.pp/template_stuff/aimages/a.png
b is a.png
a is /home/bob/2.scripts/pages/2.pp/template_stuff/aimages/b.png
b is b.png
Path::Tiny paths require defined, positive-length parts at template_stuff/html7.pm line 126.
$ 
&lt;/c&gt;

&lt;p&gt;I've marked line 126 and don't see how the above could offend:&lt;/p&gt;
&lt;c&gt;sub put_page {

  use 5.011;
  use utils1;
  use Net::SFTP::Foreign;
  use Encode;
  use open OUT =&gt; ':encoding(UTF-8)', ':std';
  use Data::Dumper;

  my ( $sftp, $rvars ) = (@_);
  my %vars = %$rvars;

  #load html file to server
  my $server_dir = $vars{"server_dir"};
  say "server dir is $server_dir";
  my $return1 = createDir( $server_dir, $sftp );
  say "return1 is $return1";
  $sftp-&gt;setcwd("/$server_dir")  or warn "setcwd1 failed $!\n";
  $sftp-&gt;put( $vars{html_file} ) or die "html put failed $!\n";

  #load css file to server
  $sftp-&gt;setcwd("/css") or warn "setcwd2 failed $@\n";
  my $path3 = path( $vars{css_path}, $vars{"css_file"} );
  say "path3 is $path3";
  my $remote_css = $vars{"css_file"};
  $sftp-&gt;put( "$path3", $remote_css ) or warn "css put failed $@\n";

  # upload images
  my $image_dir = $vars{"image_dir"};
  say "image dir is $image_dir";
  my $return2 = createDir( $image_dir, $sftp );
  say "return2 is $return2";
  $sftp-&gt;setcwd("/$image_dir") or warn "setcwd2 failed $!\n";
  my $return3 = createDir( $vars{remote_dir}, $sftp );
  say "return3 is $return3";
  $sftp-&gt;setcwd( $vars{remote_dir} ) or warn "setcwd3 failed $!\n";
  print $sftp-&gt;cwd(), "\n";

  print Dumper $rvars;
  my $ref_content = $vars{refc};
  my @AoA         = @$ref_content;

  for my $i ( 0 .. $#AoA ) {
    my $a = path( $vars{to_images}, $AoA[$i][0] );
    say "a is $a";
    my $b = $a-&gt;basename; #line 126
    say "b is $b";
    $sftp-&gt;put( $a, $b ) or warn "AoA put failed $@\n";
  }
  undef $sftp;

  return 
}&lt;/c&gt;

&lt;p&gt;Fishing for tips,&lt;/p&gt;</doctext>
<root_node field_name="root_node">1227959</root_node>
<parent_node field_name="parent_node">1228007</parent_node>
<thread id="1227959">
<node id="1227985"><node id="1228014"><node id="1228021"/>
</node>
</node>
<node id="1227993"><node id="1228034"><node id="1228042"/>
</node>
</node>
<node id="1228007"><node id="1228066"><node id="1228075"><node id="1228096"><node id="1228098"><node id="1228182"><node id="1228183"/>
<node id="1228185"><node id="1228243"/>
</node>
</node>
</node>
</node>
</node>
</node>
</node>
</thread></node>
