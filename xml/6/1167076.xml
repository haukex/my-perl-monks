<?xml version="1.0" encoding="UTF-8"?>
<node id="1167076" title="Default variable $_ is not getting overwritten" created="2016-07-03 13:09:39" updated="2016-07-03 13:09:39" _doctext_mode="lenient"><type id="115">perlquestion</type>
<author id="1005449">fireblood</author>
<doctext field_name="doctext">&lt;p&gt;Dear Monks, when I first learned perl I read that one needs to be careful to avoid creating $_ in nested loops because the inner one will clobber the outer one.  But on a whim I just created a program called x which contains the following and found that it WORKS!&lt;/p&gt;
&lt;code&gt;
    for (&lt;&gt;)

        {
            print "Current line is $_";

            for (split)

                {
                    print "Current token is $_\n";
                }
        }
&lt;/code&gt;

&lt;p&gt;When I type &lt;code&gt;cat file | x&lt;/code&gt;, each line from &lt;code&gt;file&lt;/code&gt; is first displayed and then each token within that line is displayed immediately afterwards.  I would have thought that the outer "for" would create a list of values that are popped off and assigned to $_ one at time for each pass of the outer "for" loop, but that the inner "for" would replace that list with a list of its own.  But that is not happening, the outer "for" is working fine as though the inner "for" was not there.  It looks as if each execution of the inner "for" creates and then exhausts its own list whose values it assigns to its own $_ and that this has no effect on the list and the $_ that the outer "for" uses.  I could understand this if there were a &lt;code&gt;local $_&lt;/code&gt; within the first pair of braces, but that's not the case here.&lt;/p&gt;
&lt;p&gt;Before I start relying on this behavior in production programs that I write I wanted to seek your wisdom in confirming that this is expected perl behavior that I can reliably count on.  Thank you!</doctext>
<thread id="1167076">
<node id="1167078"/>
<node id="1167083"/>
<node id="1167090"><node id="1167212"/>
</node>
</thread></node>
