<?xml version="1.0" encoding="UTF-8"?>
<node id="11105066" title="Re^2: Using the second word in a line to split a file into multiple files" created="2019-08-26 10:35:01" updated="2019-08-26 10:35:01" _doctext_mode="lenient"><type id="11">note</type>
<author id="11105012">az1962</author>
<doctext field_name="doctext">Hello, thank you so much for your help and guidance, I am very new to this forum.
I was able to get this working, and you are correct, I had filecount in there from the code I was using to start out, and focused mainly on the regex problems. I was able to get it to do exactly what was needed on the test data I presented, then realized the reason it wasn't working on the actual data file I need to parse, is because I have .s in the second word, and that second word always ends with a dot. (.s)

This works for the orignal test data:
&lt;code&gt;
use warnings;
use strict;

my $infn = '/Users/azeller/Documents/Rogers_import/20190822_RR_export-nrcmd.txt';
open(my $infh, '&lt;', $infn) or die "$infn: $!";

my $outfh;
my $filecount = 0;
while ( my $line = &lt;$infh&gt; ) {
        if ( $line =~ /^zone\s+(\w+)\W+\w+\s*$/ ) {
                close $outfh if $outfh;
                my $outfn = sprintf '%sdb', $1;
                open($outfh, '&gt;', $outfn) or die "$outfn: $!";
        }
        if ($outfh) {
                print {$outfh} $line or die "print: $!";
        }
}

close($outfh) if $outfh;
close($infh);

&lt;/code&gt;

But it doesn't work for the actual data I am parsing.

My data file format is actually more like the following:

&lt;code&gt;
one 1file1.nest.  1ss
record1a
record1b
record1c
record	1d	2
record	empty
endoffile

zone 2file2.egg.  1ss
record1a
record1b
record1c
record	1d	2
record	empty
endoffile
&lt;/code&gt;</doctext>
<root_node field_name="root_node">11105015</root_node>
<parent_node field_name="parent_node">11105047</parent_node>
<thread id="11105015">
<node id="11105019"/>
<node id="11105047"><node id="11105066"><node id="11105068"/>
</node>
</node>
<node id="11105093"/>
<node id="11105184"/>
</thread></node>
