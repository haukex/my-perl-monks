<?xml version="1.0" encoding="UTF-8"?>
<node id="11135746" title="Creating an abstract" created="2021-08-09 16:14:20" updated="2021-08-09 16:14:20" _doctext_mode="lenient"><type id="115">perlquestion</type>
<author id="11123653">Bod</author>
<doctext field_name="doctext">&lt;p&gt;In a few places I have code that takes a significant piece of text, such as a knowledgebase article, and splits a small section from the beginning as an abstract.  The code I generally use came from an answer to [id://11128036]&lt;/p&gt;

&lt;p&gt;But now, for a different application, I want to create an abstract of text which may contain HTML tags.  The problem comes with not wanting to split an HTML tag in two.  I want either all of it or none of it.  So this is the code I am using...
&lt;code&gt;
sub abstract {
    my $text = shift;
    if (length $text &gt; 200 and $text =~ /^(.{0,200}\b)(.*)$/s) {
        $text = "$1...";
    }
    
    # Check we have not split an HTML tag
    my $lt = $text =~ tr/&lt;//;
    my $gt = $text =~ tr/&gt;//;
    if ($lt != $gt) {
        my ($keep, $strip) = $text =~ /(.*)&lt;(.*)/;
        $text = "$keep...";
    }
    
    return $text;
}
&lt;/code&gt;
It does exactly what I want.&lt;/p&gt;

&lt;p&gt;However, I cannot help thinking that the code could be more succinct...&lt;br&gt;
Can you suggest a &lt;i&gt;better&lt;/i&gt; way to do it?&lt;/p&gt;</doctext>
<thread id="11135746">
<node id="11135747"/>
<node id="11135749"><node id="11135750"><node id="11135753"/>
</node>
</node>
<node id="11135754"/>
<node id="11135759"/>
</thread></node>
