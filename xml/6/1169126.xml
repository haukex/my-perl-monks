<?xml version="1.0" encoding="UTF-8"?>
<node id="1169126" title="Net::SMTP and resolving an alias" created="2016-08-04 11:14:29" updated="2016-08-04 11:14:29" _doctext_mode="lenient"><type id="115">perlquestion</type>
<author id="748177">mdamazon</author>
<doctext field_name="doctext">I can send an email via Net::SMTP with no problem as long as I use the actual email addresses of those that I wish to send an email.&lt;p&gt;
&lt;code&gt;
$smtp = Net::SMTP-&gt;new(servername);
$smtp-&gt;mail($ENV{REMOTE_HOST});
$smtp-&gt;to($email_address);
$smtp-&gt;data();
$smtp-&gt;datasend("To: " . $what_ever);
$smtp-&gt;datasend("From: name\@xxxx.com \n");
$smtp-&gt;datasend("Subject: whatever");
$smtp-&gt;datasend("\n");
$smtp-&gt;datasend($email_msg);
$smtp-&gt;dataend();
&lt;/code&gt;

If I create an email using my MS-Outlook client, I can just type in the user's login names in the To: form, press Ctrl-K, and it somehow looks up those names, called 'Alias', and converts them to real email addresses.&lt;p&gt;
Is there a way to do the same thing in Perl so that I can use the aliases with Net::SMTP instead of the real email addresses?</doctext>
<thread id="1169126">
<node id="1169128"><node id="1169151"><node id="1169176"/>
</node>
</node>
<node id="1169129"/>
<node id="1169134"/>
<node id="1169152"/>
</thread></node>
