<?xml version="1.0" encoding="UTF-8"?>
<node id="1215016" title="Modifying $_ in foreach (%hash)" created="2018-05-22 00:39:28" updated="2018-05-22 00:39:28" _doctext_mode="lenient"><type id="115">perlquestion</type>
<author id="351517">dse</author>
<doctext field_name="doctext">&lt;p&gt;Is the behavior defined when modifying &lt;code&gt;$_&lt;/code&gt; when doing a &lt;code&gt;foreach&lt;/code&gt; directly on a &lt;code&gt;%hash&lt;/code&gt; variable, i.e., without using &lt;code&gt;keys&lt;/code&gt; or &lt;code&gt;values&lt;/code&gt;, to iterate through the hash keys *and* values?  If it's specified somewhere in the man pages it's not clear to me so pointing me to where would be fine.&lt;/p&gt;

&lt;p&gt;In other words, is it &lt;strong&gt;defined&lt;/strong&gt; that the example code below:&lt;/p&gt;

&lt;blockquote&gt;&lt;pre&gt;
#!/usr/bin/env perl
use warnings;
use strict;

my @array;
my %hash;

%hash = (1, 2, 3, 4);
@array = %hash;

print("&lt;&lt;&lt;&lt; @array\n");

foreach (%hash) {
    print("&lt;&lt; $_\n");
    $_ *= 11;
    print("&gt;&gt; $_\n");
}

@array = %hash;

&lt;strong&gt;print("&gt;&gt;&gt;&gt; @array\n");&lt;/strong&gt;
&lt;/pre&gt;&lt;/blockquote&gt;

&lt;p&gt;outputs the following (last line is key)?&lt;/p&gt;

&lt;blockquote&gt;&lt;pre&gt;
&amp;lt;&amp;lt;&amp;lt;&amp;lt; 1 2 3 4
&amp;lt;&amp;lt; 1
&amp;gt;&amp;gt; 11
&amp;lt;&amp;lt; 2
&amp;gt;&amp;gt; 22
&amp;lt;&amp;lt; 3
&amp;gt;&amp;gt; 33
&amp;lt;&amp;lt; 4
&amp;gt;&amp;gt; 44
&lt;strong&gt;&amp;gt;&amp;gt;&amp;gt;&amp;gt; 1 22 3 44&lt;/strong&gt;
&lt;/pre&gt;&lt;/blockquote&gt;

&lt;p&gt;(This is Perl v5.26.1.)&lt;/p&gt;

&lt;p&gt;My project is to implement a version of &lt;code&gt;chomp&lt;/code&gt; that tries to modify its arguments as close to what &lt;code&gt;chomp&lt;/code&gt; is capable of though I realize you cannot specify a prototype on a subroutine that handles it exactly the same.&lt;/p&gt;

&lt;p&gt;Basically I'm trying to determine if it is defined what happens when a &lt;code&gt;%hash&lt;/code&gt; argument is passed to a &lt;code&gt;sub (@)&lt;/code&gt; prototyped subroutine.&lt;/p&gt;</doctext>
<thread id="1215016">
<node id="1215020"/>
<node id="1215023"/>
<node id="1215044"/>
<node id="1215046"/>
<node id="1215060"/>
</thread></node>
