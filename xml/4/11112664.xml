<?xml version="1.0" encoding="UTF-8"?>
<node id="11112664" title="Re^2: Code Flow" created="2020-02-09 00:12:44" updated="2020-02-09 00:12:44" _doctext_mode="lenient"><type id="11">note</type>
<author id="11101214">CougarXR7</author>
<doctext field_name="doctext">&lt;p&gt;It's for my friend's program.&lt;/p&gt;
&lt;p&gt;Gamespy was a 3rd party that enabled people to play games multiplayer.&lt;/p&gt;
&lt;p&gt;Gamespy shuts down and does not exist anymore but abandoning players and their game.&lt;/p&gt;
&lt;p&gt;My friend wrote in perl code that would allow players to play unreal tournament game and found out some other games would function too.&lt;/p&gt; 
&lt;p&gt;The program has a main folder with 5 other folders which contain a number of files in them, and a start file.&lt;/p&gt;
&lt;code&gt;#!/usr/bin/perl

package MasterServer;
use strict;
use warnings;
use Cwd 'abs_path';

our $ROOT;
BEGIN { ($ROOT = abs_path $0) =~ s{/util/masterserver\.pl$}{}; }
use lib $ROOT.'/lib';
use MasterServer;

our %S;
require "$ROOT/data/masterserver-config.pl";

#add %C from config.pl to OBJ
$MasterServer::OBJ-&gt;{$_}   = $S{$_} for (keys %S);

# load MasterServer core libs
MasterServer::load_recursive('MasterServer::Core', 
                                                'MasterServer::UDP',
                                                'MasterServer::TCP',
			                       'MasterServer::Util');

# Run the MasterServer process
MasterServer::run();&lt;/code&gt;

&lt;p&gt;I can trace that out but I know there has to be a quicker way.&lt;/p&gt;
&lt;p&gt;That is also the mean frame of the code.&lt;/p&gt;
&lt;p&gt;I'm hoping to trace the whole program, from the maim to the sub-routines&lt;/p&gt;
Thanks!</doctext>
<root_node field_name="root_node">11112558</root_node>
<parent_node field_name="parent_node">11112563</parent_node>
<thread id="11112558">
<node id="11112563"><node id="11112664"><node id="11112678"/>
<node id="11112679"/>
</node>
</node>
</thread></node>
