<?xml version="1.0" encoding="UTF-8"?>
<node id="11149744" title="Diamond errors" created="2023-01-21 11:42:41" updated="2023-01-21 11:42:41" _doctext_mode="lenient"><type id="115">perlquestion</type>
<author id="773348">tlhackque</author>
<doctext field_name="doctext">Consider a typical magic diamond (ARGV) usage, reducto ad absurdum:
&lt;code&gt;
  perl -e'while(&lt;&gt;){1;}continue{print "$ARGV\n" if eof;}; print $!' file1 file2 file3
file1
Can't open file2: No such file or directory at -e line 1, &lt;&gt; line 222.
file3
Bad file descriptor
&lt;/code&gt;
&lt;p&gt; Note that file 2 doesn't exist, and Perl generates an error message.  So the user knows, probably.&lt;/p&gt;
&lt;p&gt;However, after the loop, there doesn't seem to be a way for the program to detect that a file wasn't processed.  In real life, that could be awkward.  Am I missing something?&lt;/p&gt;

&lt;p&gt; I suppose that in a continue block, I could push $ARGV onto an array using eof and compare the result to @ARGV after the loop.  But that seems awkward and ugly.&lt;/p&gt;
&lt;p&gt; Saner alternatives (other than forgoing magic)?&lt;/p&gt;


&lt;!-- Node text goes above. Div tags should contain sig only --&gt;
&lt;div class="pmsig"&gt;&lt;div class="pmsig-773348"&gt;
&lt;P&gt;This communication may not represent my employer's views,
if any, on the matters discussed. &lt;/P&gt;

&lt;/div&gt;&lt;/div&gt;</doctext>
<thread id="11149744">
<node id="11149745"/>
<node id="11149746"/>
<node id="11149748"/>
<node id="11149749"/>
<node id="11149750"/>
<node id="11149751"/>
<node id="11149752"/>
<node id="11149753"/>
</thread></node>
