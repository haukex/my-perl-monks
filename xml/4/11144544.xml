<?xml version="1.0" encoding="UTF-8"?>
<node id="11144544" title="Change Carp's output to Mojo::Log" created="2022-06-09 06:22:43" updated="2022-06-09 06:22:43" _doctext_mode="lenient"><type id="115">perlquestion</type>
<author id="1165397">bliako</author>
<doctext field_name="doctext">&lt;p&gt;Dear Monks,&lt;/p&gt;

&lt;p&gt;A [mod://Mojolicious] app uses an external module ([mod://Mail::IMAPClient]) which uses [mod://Carp] which logs to STDERR. I need to tell Carp to use Mojoapp's own [mod://Mojo::Log] object (which logs to a file with specific settings etc.).&lt;/p&gt;

&lt;p&gt;many thanks, bliako&lt;/p&gt;

&lt;p&gt;EDIT: I realised that [mod://Mail::IMAPClient] outputs debug information as well and in its documentation it says that one can specify a filehandle to redirect just that *debug* output. In retrospect, this was my biggest problem when I asked this question. I stupidly thought all that debug output was going through Carp. For sending Carp's output to a (Mojo) logger, see solution below [id://11144549]. For redirecting the debug output as well, I leave this as a reminder mostly to me (it is copypasted from Mojo::Log's and Mail::IMAPClient's doc):&lt;/p&gt;

&lt;c&gt;
# redirect Mail::IMAPClient to Mojo::Log's filehandle.
use Mail::IMAPClient;
use Mojo::Log;
my $log = Mojo::Log-&gt;new(path =&gt; "logfile");
my $client = Mail::IMAPClient-&gt;new(
    ...
    Debug =&gt; 1,
    Debug_fh =&gt; $log-&gt;handle
);
&lt;/c&gt;
</doctext>
<thread id="11144544">
<node id="11144546"/>
<node id="11144549"><node id="11144619"/>
</node>
<node id="11144555"/>
<node id="11144599"/>
</thread></node>
