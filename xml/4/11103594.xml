<?xml version="1.0" encoding="UTF-8"?>
<node id="11103594" title="Re^2: writing a utility to determine whether I have a working wifi connection" created="2019-07-29 15:29:43" updated="2019-07-29 15:29:43" _doctext_mode="lenient"><type id="11">note</type>
<author id="947695">Aldebaran</author>
<doctext field_name="doctext">&lt;p&gt;(re-ordered for thematic reasons) Thanks all for responses.&lt;/p&gt;

&lt;i&gt;It might also be useful to GET your modem's status page. It can tell if your connection from your house or office to your ISP is up or not.&lt;/i&gt;


&lt;p&gt;This simple advice came just in time for me to ask the comcast guy the address. It isn't the old 192.0.0.1 it used to be, and the page seems to have more functionality than I recall. &lt;/p&gt;

&lt;i&gt;Another option is LWP::Online.&lt;/i&gt;
&lt;p&gt;I'd like to unpack this a bit. First, I do have a working script:&lt;/p&gt;

&lt;c&gt;$ ./1.lwp.pl 
execution here
execution there
$ cat 1.lwp.pl 
#!/usr/bin/perl
use 5.016;
use warnings;
use LWP::Online 'online';

say "execution here";
# "Is the internet working?"
die "NO INTARWWEB!!!" unless online();
say "execution there";
__END__
$ 
&lt;/c&gt;
&lt;p&gt;I attempted to dig through a few parts of this [https://metacpan.org/release/LWP-Online/source/lib/LWP/Online.pm|listing for Online.pm]. I have any number of questions after having contended with the source for several days now. Regarding this snippet:&lt;/p&gt;

&lt;c&gt;# Set up configuration data
use vars qw{%SUPPORTED @RELIABLE_HTTP};
BEGIN {
        # What transports do we support
        %SUPPORTED = map { $_ =&gt; 1 } qw{ http };
 
        # (Relatively) reliable websites
        @RELIABLE_HTTP = (
                # These are some initial trivial checks.
                # The regex are case-sensitive to at least
                # deal with the "couldn't get site.com case".
                'http://www.msftncsi.com/ncsi.txt' =&gt; sub { $_ eq 'Microsoft NCSI' },
                'http://google.com/'               =&gt; sub { /About Google/         },
                'http://yahoo.com/'                =&gt; sub { /Yahoo!/               },
                'http://amazon.com/'               =&gt; sub { /Amazon/ and /Cart/    },
                'http://cnn.com/'                  =&gt; sub { /CNN/                  },
        );
}&lt;/c&gt;

&lt;p&gt;What is happening with the subs here?&lt;/p&gt;



&lt;p&gt;You get about hip deep in it and need to look at [https://metacpan.org/release/libwww-perl/source/lib/LWP/Simple.pm|source for LWP::Simple]. Is this a core module?&lt;/p&gt;

&lt;p&gt;In this snippet from LWP::Online, how does it import a lexical variable?&lt;/p&gt;

&lt;c&gt;use LWP::Simple 5.805 qw{ get $ua };
 
use vars qw{$VERSION @ISA @EXPORT_OK};
BEGIN {
        $VERSION = '1.08';
 
        # We are an Exporter
        require Exporter;
        @ISA       = qw{ Exporter };
        @EXPORT_OK = qw{ online offline };
 
        # Set the useragent timeout
        $ua-&gt;timeout(30);
}&lt;/c&gt;

&lt;p&gt;This is get in [LWP::Simple]&lt;/p&gt;

&lt;c&gt;sub get ($)
{
    my $response = $ua-&gt;get(shift);
    return $response-&gt;decoded_content if $response-&gt;is_success;
    return undef;
}&lt;/c&gt;



&lt;p&gt;How would one add his own site to @RELIABLE_HTTP?  &lt;/p&gt;
&lt;p&gt;Thanks for your comments&lt;/p&gt;</doctext>
<root_node field_name="root_node">11103395</root_node>
<parent_node field_name="parent_node">11103399</parent_node>
<thread id="11103395">
<node id="11103396"><node id="11103398"/>
</node>
<node id="11103399"><node id="11103427"/>
<node id="11103441"/>
<node id="11103594"><node id="11103596"/>
<node id="11103612"/>
</node>
</node>
<node id="11103424"><node id="11104201"><node id="11104235"/>
</node>
</node>
<node id="11103501"><node id="11103513"/>
</node>
</thread></node>
