<?xml version="1.0" encoding="UTF-8"?>
<node id="1167104" title="Return array from sub or return empty array" created="2016-07-04 00:39:03" updated="2016-07-04 00:39:03" _doctext_mode="lenient"><type id="115">perlquestion</type>
<author id="1167102">gregzartman</author>
<doctext field_name="doctext">&lt;p&gt;I am doing some dev for a linux distro with a perl API for managing the configuration for the server.  Quite a bit of the underling API are bits of perl code we call event and actions. &lt;/p&gt;

&lt;p&gt;One of the "practices" many follow is assigning an empty value when a string is set so as not to end up with a undef value.  Something like this: &lt;/p&gt;

&lt;code&gt;
$var = some_class-&gt;some_method() || ''
&lt;/code&gt;

&lt;p&gt;This is assuming some_method returns a scalar.&lt;/p&gt;

&lt;p&gt;I've just always used this approach but found that this doesn't work when returning arrays "some_method"   I'm hoping you all can help me understand what perl is doing when it returns and array.&lt;/p&gt;

&lt;p&gt;Here's the bit of code I was testing with:&lt;/p&gt;

&lt;code&gt;
@array1 = get_array2() || ();

foreach (@array1) {print "$_\n";}


sub get_array2 {

@array2 = ("greg","Zartman");

return (@array2);
}
&lt;/code&gt;

&lt;p&gt;I was thinking that @array1 gets assigned @array2 via the get_array2 function unless undef, in which case @array1 get assigned the empty array ().   However, I can see from this test code that my thinking is wrong.&lt;/p&gt;

Thanks! 

</doctext>
<thread id="1167104">
<node id="1167105"/>
<node id="1167107"/>
<node id="1167110"><node id="1167113"/>
<node id="1167158"><node id="1167169"/>
</node>
</node>
<node id="1167199"/>
</thread></node>
