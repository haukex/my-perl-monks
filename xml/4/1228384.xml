<?xml version="1.0" encoding="UTF-8"?>
<node id="1228384" title="Re: Regex not matching closing newline?" created="2019-01-11 04:42:20" updated="2019-01-11 04:42:20" _doctext_mode="strict"><type id="11">note</type>
<author id="830549">haukex</author>
<doctext field_name="doctext">&lt;p&gt;&lt;c&gt;$&lt;/c&gt; is a zero-width assertion that matches at the end of the string, &lt;i&gt;or just before&lt;/i&gt; the newline at the end of the string (note its behavior can be changed by the &lt;c&gt;/m&lt;/c&gt; modifier). &lt;c&gt;?&lt;/c&gt; makes the preceding &lt;c&gt;.*&lt;/c&gt; non-greedy, so that it allows &lt;c&gt;$&lt;/c&gt; to match just before the newline, while a normal &lt;c&gt;.*&lt;/c&gt; is greedy, causing it to gobble up everything it can, and the &lt;c&gt;$&lt;/c&gt; matches just after the newline. Note that since you're using the &lt;c&gt;/s&lt;/c&gt; modifier, the dot &lt;c&gt;.&lt;/c&gt; can also match a newline, which it normally does not.&lt;/p&gt;
&lt;p&gt;What is your desired behavior? If you want the match to always include the newline, I'd be specific about it: &lt;c&gt;/ab.*\n/&lt;/c&gt;, or, if you want the newline to be optional (e.g. the last line in a file may not have a newline), then I'd probably write &lt;c&gt;/ab.*(?:\z|\n)/&lt;/c&gt;. If you want the match to always exclude the newline, I'd leave off the &lt;c&gt;/s&lt;/c&gt; modifier, or be specific about what you want to match by saying e.g. &lt;c&gt;[^\n]*&lt;/c&gt; or &lt;c&gt;\N*&lt;/c&gt;.&lt;/p&gt;</doctext>
<root_node field_name="root_node">1228382</root_node>
<parent_node field_name="parent_node">1228382</parent_node>
<thread id="1228382">
<node id="1228384"><node id="1228385"><node id="1228389"/>
</node>
</node>
</thread></node>
