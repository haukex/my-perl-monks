<?xml version="1.0" encoding="UTF-8"?>
<node id="1183004" title="How to determine if a sub is defined in a package, without calling it?" created="2017-02-27 15:10:45" updated="2017-02-27 15:10:45" _doctext_mode="lenient"><type id="115">perlquestion</type>
<author id="1183002">lamber45</author>
<doctext field_name="doctext">&lt;p&gt;So I'm trying to build an interpreter for a certain non-perl bytecode, and I think I'll be able to represent classes in that system as perl packages.  &lt;/p&gt;

&lt;p&gt;Method invocations in that system are by method name (OK to overload) and argument types. When the bytecode says to call a method "foo" with descriptor "EGXX", I'd like my code to first check whether a sub "foo__EGXX" exists in the target package (or any of its base packages, recursively); if it doesn't find it, I'd like to check for a sub "foo" (also recursively).&lt;/p&gt;

&lt;p&gt;Is there a (standard or easy-to-install) module that does the "find a sub, don't invoke it yet" part?

If I have to roll my own, I guess I'd do recursive ascent through @ISA, but how do I test if a sub is defined? Is it as simple as

&lt;code&gt;
  if (${ $package . '::' }{ $longName }) {
    $value = eval '$objectref-&gt;' . $longName . '( @args )';
  } else {
    $value = eval '$objectref-&gt;' . $shortName . '( @args )';
  }
&lt;/code&gt;

?&lt;/p&gt;
  </doctext>
<thread id="1183004">
<node id="1183007"/>
<node id="1183009"/>
</thread></node>
