<?xml version="1.0" encoding="UTF-8"?>
<node id="1229524" title="Deleting certein elements from an array" created="2019-02-07 03:53:33" updated="2019-02-07 03:53:33" _doctext_mode="lenient"><type id="115">perlquestion</type>
<author id="1229522">Eso</author>
<doctext field_name="doctext">&lt;p&gt; Hi there, newbie here. I'm trying to compile a program which has to generate an array containing random numbers (including negative ones), but no more than 19. After that, every positive number in the above-mentioned array has to be deleted, making it contain only the negative ones. The whole code looks like this: &lt;/p&gt;

&lt;code&gt;#!/usr/bin/perl
use strict;
use warnings;
use feature qw/say/;
use 5.10.1;

my @rand=();

my $all_the_numbers=int(rand(19));

my $i=0;

while ($all_the_numbers&lt;20)
{
 my $number=20-int(rand(30));
 push @rand, $number;
 $i++;
 last if $i==$all_the_numbers;
}
say "@rand";

my $h=0;

while ($all_the_numbers&lt;20)
{
 for (@rand)
 {
 if ($rand[0]&lt;0)
  {
   next;  #in order to make it skip the negative ones
  }
   shift @rand;
 }
 $h++;
 last if $h==$all_the_numbers;
}
say "@rand"; &lt;/code&gt;

&lt;p&gt; However, this resoults in deleting only the positive numbers that come before the first negative one in the array, leaving what comes right after it untouched... So, how is it that the while loop doesn't keep on deleting the positives? Here's a picture to better explain myself: https://i.ibb.co/Gkmj8jc/Immagine.png
Thanks in advance. &lt;/p&gt;</doctext>
<thread id="1229524">
<node id="1229525"/>
<node id="1229526"/>
<node id="1229536"><node id="1229544"><node id="1229549"/>
</node>
</node>
<node id="1229550"/>
<node id="1229571"/>
</thread></node>
