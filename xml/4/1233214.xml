<?xml version="1.0" encoding="UTF-8"?>
<node id="1233214" title="Match strings in order of character length, and remove the string from further processing" created="2019-05-01 00:59:11" updated="2019-05-01 00:59:11" _doctext_mode="lenient"><type id="115">perlquestion</type>
<author id="1225562">ScarletRoxanne</author>
<doctext field_name="doctext">&lt;p&gt;Hi! I hope this question won't seem super lazy. I just have no idea where to start with this.&lt;/p&gt;

&lt;p&gt;I have three files. File 1 contains stop words, File 2 contains a list of words and phrases (terms), and File 3 is the file I want to extract information from.&lt;/p&gt;

&lt;p&gt;I want to remove the stop words from File 3, and print the remaining words in a list. This part was no problem.&lt;/p&gt;

&lt;p&gt;The problem is that before I remove the stop words from File 3, I want to see if any of the terms from File 2 match a string in File 3. I want the longest phrase from File 2 to be searched for in File 3 first, then the second longest, and so on. Then I want to output the term surrounded by *, and remove it from being processed by the next part of the script, which removes stop words.&lt;/p&gt;

&lt;p&gt;So let's say the files look like this and are split on the , &lt;/p&gt;

&lt;p&gt; File 1: I, am, the, of, and&lt;/p&gt; 

&lt;p&gt;File 2: manager of sales &lt;/p&gt; 

&lt;p&gt;File 3: I am the senior manager of sales and of marketing &lt;/p&gt; 

&lt;p&gt;Output: &lt;/p&gt; 
&lt;p&gt;senior &lt;/p&gt; 
&lt;p&gt;*manager of sales* &lt;/p&gt; 
&lt;p&gt;marketing &lt;/p&gt;    

&lt;p&gt;I'm so sorry, but I just don't even know where to start with this. &lt;/p&gt; 

&lt;p&gt;Here's the script, which right now only removes stop words, nothing else: &lt;/p&gt; 

&lt;code&gt;#!/usr/bin/perl
use warnings;
use strict;

my %stops;
my %terms;

open (FILE, $ARGV[0]);
while (&lt;FILE&gt;) {
    chomp;
    $stops{$_} = 1;
}

open (FILE, $ARGV[1]);
while (&lt;FILE&gt;) {
    chomp;
    $terms{$_} = 1;
}

open (FILE, $ARGV[2]);
while (&lt;FILE&gt;) {
    chomp;

#Starting with the longest term from ARGV[1], then going to the next largest, and so on, if the term also exists in ARGV[2], surround it by *, print the term, and remove the term from further processing. 

#after that, remove the stop words from the remainder of the file that didn't match a string in [ARGV[1]
    
    $_ =~ tr/A-Z/a-z/;
    my @words = split ('[^a-z0-9]', $_);
    for my $word (@words) {
	unless ($stops{$word}++){
	    print "$word\n" 
	}
    }
}
&lt;/code&gt;
</doctext>
<thread id="1233214">
<node id="1233215"/>
<node id="1233219"/>
</thread></node>
