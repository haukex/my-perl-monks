<?xml version="1.0" encoding="UTF-8"?>
<node id="1207804" title="Re: Illegal division by zero" created="2018-01-24 03:56:11" updated="2018-01-24 03:56:11" _doctext_mode="strict"><type id="11">note</type>
<author id="830549">haukex</author>
<doctext field_name="doctext">&lt;p&gt;I see a couple of issues with this code:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;You're using a &lt;c&gt;foreach&lt;/c&gt; where a &lt;c&gt;while&lt;/c&gt; would be better. The former will read the entire file into memory and then iterate over the lines, while a &lt;c&gt;while&lt;/c&gt; will read the file line-by-line.&lt;/li&gt;
&lt;li&gt;On each iteration of the loop, you replace the entire contents of &lt;c&gt;@windsordigits&lt;/c&gt;. If the last line read does not match the regex, &lt;c&gt;@windsordigits&lt;/c&gt; will end up empty, which would explain your error. As [AnomalousMonk] already pointed out, you probably want to use [doc://push] instead.&lt;/li&gt;
&lt;li&gt;You're attempting to read what looks like a CSV file by hand. You would be much better off using [mod://Text::CSV] (plus [mod://Text::CSV_XS] for speed), as that will also handle the case of a comma inside quotes, which you've got there according to your sample data.&lt;/li&gt;
&lt;/ul&gt;
&lt;c&gt;
use warnings;
use strict;
use Data::Dump; # Debug
use Text::CSV;
use List::Util qw/sum/;

my $filename = 'part4.csv';
my $csv = Text::CSV-&gt;new({binary=&gt;1, auto_diag=&gt;2});
open my $fh, '&lt;', $filename or die "$filename: $!";
my @windsordigits;
while ( my $row = $csv-&gt;getline($fh) ) {
	dd $row; # Debug
	if ( $row-&gt;[1] =~ /WINDSOR\s+RIVERSIDE/ ) {
		push @windsordigits, $row-&gt;[3];
	}
}
$csv-&gt;eof or $csv-&gt;error_diag;
close $fh;
dd @windsordigits; # Debug
die "No digits" unless @windsordigits;
my $average = sum(@windsordigits) / @windsordigits;
dd $average;

__END__

["CA006139520", "WINDSOR RIVERSIDE, ON CA", "2018-01-02", 10]
["CA006139520", "FOO", "2018-01-02", 99]
["CA006139520", "WINDSOR RIVERSIDE ON CA", "2018-01-02", 24]
["CA006139520", "RIVERSIDE WINDSOR", "2018-01-02", 99]
["CA006139520", "WINDSOR RIVERSIDE, ON CA", "2018-01-02", 59]
(10, 24, 59)
31

&lt;/c&gt;</doctext>
<root_node field_name="root_node">1207797</root_node>
<parent_node field_name="parent_node">1207797</parent_node>
<thread id="1207797">
<node id="1207798"/>
<node id="1207804"><node id="1207835"/>
</node>
<node id="1207805"><node id="1207850"><node id="1207856"/>
<node id="1207865"/>
</node>
</node>
</thread></node>
