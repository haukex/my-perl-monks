<?xml version="1.0" encoding="UTF-8"?>
<node id="1162804" title="Perl on Windows: file names with accented characters, UTF-8 and -e" created="2016-05-11 16:47:41" updated="2016-05-11 16:47:41" _doctext_mode="lenient"><type id="115">perlquestion</type>
<author id="190859">bart</author>
<doctext field_name="doctext">Using Strawberry Perl x64 on Windows, I'm having a bit of a problem with &lt;code&gt;-e&lt;/code&gt; that fails recognize file names if the name contains an accented character, meaning: &lt;code&gt;-e $path&lt;/code&gt; returns false even though the file exists.

&lt;p&gt;I'm reading the file names out of an XML file, and thus, they are strings with the UTF-8 flag set.

&lt;P&gt;I'm quite sure the cause of the problem is the mix of character encodings in Perl on Windows, as the standard encoding of the CMD shell (under which Perl runs) is CP-850, Explorer and the Windows API appear to be using either CP-1252 or 16-bit wide Unicode characters; and the Unicode-enabled strings in Perl. 

&lt;p&gt;I have no idea which encoding &lt;code&gt;-e&lt;/code&gt; expects to work as it should. It does work if the path contains only plain ASCII characters.

&lt;P&gt;So... what do I have to do to make it work?
</doctext>
<thread id="1162804">
<node id="1162808"/>
<node id="1162817"/>
<node id="1162842"/>
<node id="1162874"/>
</thread></node>
