<?xml version="1.0" encoding="UTF-8"?>
<node id="1184924" title="Passing Arguments for IPC::Run start()" created="2017-03-16 16:16:21" updated="2017-03-16 16:16:21" _doctext_mode="lenient"><type id="115">perlquestion</type>
<author id="1182107">archeman2</author>
<doctext field_name="doctext">I want to start a program that requires 4 string arguments.
&lt;p&gt;
The program can't use 'run', I need to use 'start' with harness because I need the script to keep that task running while the script goes off to do something else. The command isn't going to stop on it's own...by design. I'll kill_kill it afterwards.

&lt;p&gt;I'm having a bit of a problem getting my head around how to get the arguments passed to the command. I can't use the $in because these aren't inputs that the command is waiting for, it needs them as arguments at the moment of invocation.

I am able to call the command without arguments, but when I put the arguments in with the command string, it interprets them as separate commands. Need a little help here. I have read the documentation but that focuses on programs that take input after launch, not as arguments.
&lt;p&gt;http://search.cpan.org/~toddr/IPC-Run-0.94/lib/IPC/Run.pm

&lt;code&gt;
my @run_connector = ('/disk2/home/rlong/workdir/auto/scripts/myConnectorScript.pl');
my @run_connectorArgs = ('log1', '10.10.101.111', 'userName', 'myPa$$W0rD');
use IPC::Run qw( start pump finish timeout ) ;
my $in = '';
my $out = '';
my $err = '';
#####NEED HELP RIGHT HERE#####
my $harness = start	\@run_connector, \$in, \$out, \$err, timeout( 600 ) or die "run_connector: $?\n";
print "\nSleeping 30 Seconds\n";
sleep 30;
$harness-&gt;kill_kill;
 $?\n";
print "\nFinished run_connector\n";
print "Here is the Out:\n";
print "$out\n";
print "Here is the Error:\n";
print "$err\n";
&lt;/code&gt;</doctext>
<thread id="1184924">
<node id="1184925"><node id="1185273"/>
<node id="1186541"/>
</node>
</thread></node>
