<?xml version="1.0" encoding="UTF-8"?>
<node id="11119825" title="execute remote perl script via ssh" created="2020-07-26 05:47:41" updated="2020-07-26 05:47:41" _doctext_mode="lenient"><type id="115">perlquestion</type>
<author id="734134">vinoth.ree</author>
<doctext field_name="doctext">&lt;b&gt;Hello all,&lt;/b&gt;
&lt;p&gt;
&lt;p&gt;I have a perl script on unix server and running it from windows machine. I have already synced the public key generated from windows machine to unix server so it does not ask for password.&lt;/p&gt;
&lt;c&gt;
ssh.exe username@xxx.xxx.xxx.xxx /fullpath/check_user_exists.pl username
&lt;/c&gt;

&lt;p&gt;The remote perl script does checks weather the username name exists in a csv file in the unix server that's it. If it exists it exits with 0 otherwise 1.&lt;/p&gt;

&lt;p&gt;This is run by all the users in my team it works most of the users for particular user it does not works. When I run this command via command line manually it return 0, but when I call via perl script it returns 255.&lt;/p&gt;

&lt;p&gt;What could be the issue? how to debug this issue? &lt;/p&gt;

&lt;p&gt;This is how i call this command locally from a perl script on windows side.&lt;/p&gt;
&lt;c&gt;
sub executeCommand{
  my $command = join ' ', @_;
  $prot and print("Command: $command\n");
  my $response = qx($command 2&gt;&amp;1);
  my $status = $? &gt;&gt; 8;
  return ($status, $response);
}

my @src_cmd = ('ssh.exe ', $ssh_user.'@'.$hostname ." /fullpath/check_user_exists.pl " . "$user");
my ($status, $output) = &amp;executeCommand(@src_cmd);
&lt;/c&gt;
&lt;/p&gt;
&lt;c&gt;&lt;/c&gt;
&lt;div class="pmsig"&gt;&lt;div class="pmsig-734134"&gt;
&lt;hr&gt;
&lt;b&gt;&lt;i&gt;All is well. I learn by answering your questions...&lt;/i&gt;&lt;/b&gt;
&lt;/div&gt;&lt;/div&gt;</doctext>
<thread id="11119825">
<node id="11119826"><node id="11119830"><node id="11120559"/>
</node>
</node>
<node id="11119835"/>
<node id="11119846"><node id="11119851"/>
</node>
<node id="11119856"/>
</thread></node>
