<?xml version="1.0" encoding="UTF-8"?>
<node id="11117265" title="Appending /*.* to default Perl variable $_" created="2020-05-26 03:35:59" updated="2020-05-26 03:35:59" _doctext_mode="lenient"><type id="115">perlquestion</type>
<author id="11117262">geertvc</author>
<doctext field_name="doctext">First time ever post here...
&lt;br/&gt;&lt;br/&gt;
I have an array with directory names:

&lt;code&gt;
my @codeOutputDirs = (
    "output/$harvester$flavour/layout",
    "output/$harvester$flavour/tabview",
    "output/$harvester$flavour/udw",
    "output/$harvester$flavour/values",
    "output/$harvester$flavour/udw/service",
    "output/$harvester$flavour/udw/cfg"
);
&lt;/code&gt;

The variables &lt;code&gt;$harvester&lt;/code&gt; and &lt;code&gt;$flavour&lt;/code&gt; are passed as parameters on the command line when calling the Perl script.
&lt;br/&gt;&lt;br/&gt;
I want to use a for loop to go through the array and do some stuff like so:

&lt;code&gt;
make_path "output/$harvester$flavour/layout";
unlink glob "output/$harvester$flavour/layout/*.*";
&lt;/code&gt;

And this, for each and every element in the array.
&lt;br/&gt;&lt;br/&gt;
For this, I'm using the following Perl-ish (I guess?) way of traversing through each and every element of the array:


&lt;code&gt;
for (@codeOutputDirs) {
    make_path $_;
    unlink glob $_/*.*;
}
&lt;/code&gt;

The first command, &lt;code&gt;make_path $_;&lt;/code&gt; is fine.
&lt;br/&gt;
However, the second command has issues when I append &lt;code&gt;/*.*&lt;/code&gt; to the default variable &lt;code&gt;$_&lt;/code&gt;
&lt;br/&gt;&lt;br/&gt;
Can someone tell me how to accomplish this using the correct syntax?  Putting &lt;code&gt;$_&lt;/code&gt; between &lt;code&gt;{}&lt;/code&gt; doesn't seem to be the solution.
&lt;br/&gt;&lt;br/&gt;
Best,
&lt;br/&gt;
--Geert</doctext>
<thread id="11117265">
<node id="11117267"/>
<node id="11117268"><node id="11117292"/>
</node>
</thread></node>
