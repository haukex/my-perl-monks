<?xml version="1.0" encoding="UTF-8"?>
<node id="1229795" title="Re: Retrieve average" created="2019-02-12 09:37:49" updated="2019-02-12 09:37:49" _doctext_mode="strict"><type id="11">note</type>
<author id="830549">haukex</author>
<doctext field_name="doctext">&lt;p&gt;Hello and welcome to Perl and the Monastery, [Tigor]!&lt;/p&gt;
&lt;p&gt;Here are my suggestions on how to solve your problem:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;The problem with the code you showed is that &lt;c&gt;m/^C/&lt;/c&gt; requires an uppercase &lt;c&gt;C&lt;/c&gt; at the beginning of the line, but the test data you've showed includes whitespace at the beginning of the line and the name starts with a lowercase &lt;c&gt;c&lt;/c&gt;. You can either change the regular expression into &lt;c&gt;m/^\s*C/i&lt;/c&gt; to allow for case-insensitive matching and whitespace at the beginning of the line, or you could also remove whitespace from the beginning of the line with &lt;c&gt;$line=~s/^\s+//;&lt;/c&gt;. Yet another alternative is to match the name &lt;i&gt;after&lt;/i&gt; the &lt;c&gt;split&lt;/c&gt; that I describe below, because that will remove the whitespace for you, and then only compare the name itself.&lt;/li&gt;
&lt;li&gt;You can use [doc://split] to split the line on whitespace and store it into an array. For example, &lt;c&gt;my @fields = split ' ', $line;&lt;/c&gt;&lt;/li&gt;
&lt;li&gt;You can then use [doc://shift] to remove the first field from an array. For example, &lt;c&gt;my $first = shift @array;&lt;/c&gt;.&lt;/li&gt;
&lt;li&gt;You can use &lt;c&gt;sum&lt;/c&gt; from [doc://List::Util] to sum a list of numbers. For example, put &lt;c&gt;use List::Util qw/sum/;&lt;/c&gt; at the top of your script, and then use &lt;c&gt;my $sum = sum(@array);&lt;/c&gt;&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;I hope this is enough information to get you started. For a good introduction to Perl in general, see [doc://perlintro].&lt;/p&gt;
&lt;p&gt;In addition, here are some stylistic tips:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;I'd suggest using a separate variable for the filename. For example, &lt;c&gt;my $file = "test_scores.txt";&lt;/c&gt;, and later on you can declare &lt;c&gt;$line&lt;/c&gt; in the loop with &lt;c&gt;while( my $line=&lt;FH&gt; )&lt;/c&gt;.&lt;/li&gt;
&lt;li&gt;The more modern three-argument [doc://open] and lexical filehandles are generally recommended. For example, &lt;c&gt;open my $fh, '&lt;', $file or die "$file: $!";&lt;/c&gt; - then, use &lt;c&gt;$fh&lt;/c&gt; instead of &lt;c&gt;FH&lt;/c&gt;.&lt;/li&gt;
&lt;/ul&gt;</doctext>
<root_node field_name="root_node">1229793</root_node>
<parent_node field_name="parent_node">1229793</parent_node>
<thread id="1229793">
<node id="1229794"/>
<node id="1229795"/>
<node id="1229806"/>
<node id="1229838"/>
</thread></node>
