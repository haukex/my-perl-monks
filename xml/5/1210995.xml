<?xml version="1.0" encoding="UTF-8"?>
<node id="1210995" title="map() and grep() suppress &quot;Can't use an undefined value as an ARRAY reference at ...&quot; errors" created="2018-03-15 16:34:28" updated="2018-03-15 16:34:28" _doctext_mode="lenient"><type id="115">perlquestion</type>
<author id="1210992">icanhezperl</author>
<doctext field_name="doctext">Hello,
&lt;p&gt;
I'm seeing an odd behavior wehere the "Can't use an undefined value as an ARRAY reference at ..." is generated when expected but not when map() and grep() are involved.
&lt;p&gt;
Here's the code. In the first case with map(), perl runs without an error (ditto for grep). But in the second case, without any map or grep, the expected error is thrown becaus the value being dereferenced is undefined.
&lt;p&gt;
&lt;c&gt;
use strict;
use warnings;

my $this_key = "NOT_IN_HASH";
my $hash_ref = {};

my @items;

if (0) {
    # this gives no error
    @items = map { $_-&gt;{value} } @{$hash_ref-&gt;{$this_key}};
} else {
    # but this generates to the expected error.
    @items =                     @{$hash_ref-&gt;{$this_key}};
}
&lt;/c&gt;
&lt;p&gt;
Am I missing something obvious? Is this behavior intentional?
&lt;p&gt;
Thank you.</doctext>
<thread id="1210995">
<node id="1210996"/>
<node id="1210997"><node id="1211000"/>
</node>
<node id="1210999"/>
<node id="1211001"/>
</thread></node>
