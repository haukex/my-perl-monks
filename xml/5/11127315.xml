<?xml version="1.0" encoding="UTF-8"?>
<node id="11127315" title="Re^3: I am confused by a &quot;Learning Perl&quot; sample showing &quot;unshift&quot; (updated)" created="2021-01-22 17:48:13" updated="2021-01-22 17:48:13" _doctext_mode="strict"><type id="11">note</type>
<author id="830549">haukex</author>
<doctext field_name="doctext">&lt;blockquote&gt;&lt;i&gt;Or should I simply go with "No, I won't pay any attention to the man behind the curtain."?&lt;/i&gt;&lt;/blockquote&gt;
&lt;p&gt;Yes. Perl functions generally do what they say on the tin &lt;c&gt;:-)&lt;/c&gt;&lt;/p&gt;
&lt;blockquote&gt;&lt;i&gt;Admitted, this is not one of the all-time consuming questions about the Universe, but when something doesn't act the way I expect, I like to know how it achieves the result(s) that it gets.&lt;/i&gt;&lt;/blockquote&gt;
&lt;p&gt;As far as I can tell from the internal implementation of [https://github.com/Perl/perl5/blob/fa08f8614adc/pp.c#L5810|&lt;c&gt;pp_unshift&lt;/c&gt;] and [https://github.com/Perl/perl5/blob/fa08f8614adc/av.c#L697|&lt;c&gt;av_unshift&lt;/c&gt;], your guess that it's the latter (&lt;small&gt;&lt;i&gt;Edit 2:&lt;/i&gt;&lt;/small&gt; that is, shift the array up by &lt;i&gt;N&lt;/i&gt; at once) appears to be correct. But as stated above, that should be considered an implementation detail that you don't need to worry about.&lt;/p&gt;
&lt;blockquote&gt;&lt;i&gt;My personal bet is on the second as it'd be faster.&lt;/i&gt;&lt;/blockquote&gt;
&lt;p&gt;If concerned about performance, always measure first, using [doc://Benchmark] and/or [mod://Devel::NYTProf].&lt;/p&gt;
&lt;readmore&gt;
&lt;p&gt;The following is kind of a pointless comparison, instead it's just intended to show how one might use the former of the two modules.&lt;/p&gt;
&lt;c&gt;
use warnings;
use strict;
use Benchmark qw/cmpthese/;

cmpthese(-2, {
	plain =&gt; sub {
		my @array = 1..50;
		my @add = 100..150;
		unshift @array, @add;
	},
	reverse =&gt; sub {
		my @array = 1..50;
		my @add = 100..150;
		unshift @array, reverse @add;
	},
	loop =&gt; sub {
		my @array = 1..50;
		my @add = 100..150;
		unshift @array, $_ for @add;
	},
});

__END__

            Rate    loop reverse   plain
loop    278364/s      --    -31%    -32%
reverse 400772/s     44%      --     -2%
plain   407520/s     46%      2%      --
&lt;/c&gt;
&lt;/readmore&gt;
&lt;p&gt;&lt;b&gt;Update:&lt;/b&gt; Actually, [LanX]'s post [id://11127314|here] provides something more interesting to benchmark. I've also added a built-in test of the benchmarked code here.&lt;/p&gt;
&lt;c&gt;
use warnings;
use strict;
use Benchmark qw/cmpthese/;
use constant TEST =&gt; 0;

my $EXP = join $", 100..150, 1..50;

cmpthese(-2, {
	unshift =&gt; sub {
		my @array = 1..50;
		my @add = 100..150;
		unshift @array, @add;
		"@array" eq $EXP or die "@array" if TEST;
	},
	loop =&gt; sub {
		my @array = 1..50;
		my @add = 100..150;
		unshift @array, $_ for reverse @add;
		"@array" eq $EXP or die "@array" if TEST;
	},
	concat =&gt; sub {
		my @array = 1..50;
		my @add = 100..150;
		@array = ( @add, @array );
		"@array" eq $EXP or die "@array" if TEST;
	},
});

__END__

            Rate    loop  concat unshift
loop    273062/s      --    -19%    -33%
concat  337646/s     24%      --    -18%
unshift 409595/s     50%     21%      --
&lt;/c&gt;</doctext>
<root_node field_name="root_node">11127306</root_node>
<parent_node field_name="parent_node">11127313</parent_node>
<thread id="11127306">
<node id="11127307"/>
<node id="11127309"><node id="11127313"><node id="11127314"><node id="11127340"><node id="11127341"/>
<node id="11127343"/>
</node>
</node>
<node id="11127315"/>
</node>
</node>
<node id="11127312"/>
<node id="11127337"/>
<node id="11127344"/>
<node id="11127396"/>
</thread></node>
