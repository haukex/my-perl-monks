<?xml version="1.0" encoding="UTF-8"?>
<node id="11112815" title="Re^3: Nested iterations throgh hash" created="2020-02-11 16:01:22" updated="2020-02-11 16:01:22" _doctext_mode="strict"><type id="11">note</type>
<author id="830549">haukex</author>
<doctext field_name="doctext">&lt;blockquote&gt;&lt;i&gt;This means that "each" mechanism is not very good to use.&lt;/i&gt;&lt;/blockquote&gt;
&lt;p&gt;It depends: As described in [doc://each], every hash has an iterator that is per-hash. The code you showed in the root node doesn't work because both of the &lt;c&gt;each&lt;/c&gt; calls access the same iterator. My solution fetches the list of [doc://keys] once at the beginning of the &lt;c&gt;for&lt;/c&gt; loop, which will use a bit more memory; I could have done this for both loops, but in this example I knew that the inner &lt;c&gt;each&lt;/c&gt; would work because there's nothing else interfering with the iterator, so I kept it. [doc://keys] does have the advantage that if you want to loop through the hash in a predictable order, you can just say &lt;c&gt;sort keys %hash&lt;/c&gt;.&lt;/p&gt;
&lt;p&gt;Also, note that if you want to create the [wp://Cartesian product] of two sets of hash keys, there are plenty of modules that will do this, see the links in the section "See Also" in my module [mod://Algorithm::Odometer::Tiny].&lt;/p&gt;
&lt;c&gt;
use warnings;
use strict;
use Algorithm::Odometer::Tiny;

my $h = {a=&gt;1, b=&gt;2, c=&gt;3, d=&gt;4};

my $odo = Algorithm::Odometer::Tiny
	-&gt;new( [sort keys %$h], [sort keys %$h] );

while ( my @x = $odo-&gt;() ) {
	print "outer: $x[0], inner: $x[1]\n";
}
&lt;/c&gt;</doctext>
<root_node field_name="root_node">11112801</root_node>
<parent_node field_name="parent_node">11112812</parent_node>
<thread id="11112801">
<node id="11112804"><node id="11112807"/>
</node>
<node id="11112808"><node id="11112812"><node id="11112815"/>
</node>
</node>
<node id="11112819"/>
</thread></node>
