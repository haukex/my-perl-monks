<?xml version="1.0" encoding="UTF-8"?>
<node id="11104905" title="Problem executing command via ssh" created="2019-08-23 11:45:23" updated="2019-08-23 11:45:23" _doctext_mode="lenient"><type id="115">perlquestion</type>
<author id="11104903">mgalindez</author>
<doctext field_name="doctext">folks,

&lt;p&gt;I need to execute a program in a remote machine, that takes its input from STDIN.&lt;/p&gt;

&lt;p&gt;I created a user in such remote machine, and set my program as the shell for that account (so I don't need to provide shell access).&lt;/p&gt;

&lt;p&gt;On my local node, I have this program:&lt;/p&gt;

&lt;code&gt;
#!/usr/bin/perl

	use IPC::Open3;
	use POSIX;


	$inputfile= $ARGV[0];
	open(FILE, '&lt;', "$ARGV[0]") or die $!;
	$command= "ssh user\@remotehost.com";
	$pid = open3('&lt;&amp;FILE', '&gt;&amp;STDOUT', '&gt;&amp;STDERR', $command);

	$ret=waitpid( $pid, 0 );
&lt;/code&gt;

&lt;p&gt;
What I expect is that I pass the name of the file as arguments of this program ($ARGV[0]), and the contents of this file is fed as STDIN of the child I've spawn with open3. This should be received as STDIN of my remote program, and the results of the remote program would be printed on STDOUT.&lt;/p&gt;

&lt;p&gt;This works well. However, my program only reads the first line, and then terminates. &lt;/P&gt;

&lt;p&gt;If I do a manual test, and do ssh user@remotehost.com, and type multiple lines of input, then things work as expected.&lt;/p&gt;

&lt;p&gt;Any clues?&lt;/p&gt;

&lt;p&gt;Thanks!&lt;br&gt;

-m
&lt;/p&gt;
</doctext>
<thread id="11104905">
<node id="11104916"/>
<node id="11104920"><node id="11104930"/>
<node id="11104984"><node id="11105005"/>
</node>
</node>
</thread></node>
