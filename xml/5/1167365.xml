<?xml version="1.0" encoding="UTF-8"?>
<node id="1167365" title="Re: cpan build environment" created="2016-07-07 06:07:55" updated="2016-07-07 06:07:55" _doctext_mode="strict"><type id="11">note</type>
<author id="830549">haukex</author>
<doctext field_name="doctext">&lt;p&gt;Hi [DennisLGolden],&lt;/p&gt;
&lt;p&gt;In one of your replies in this thread you wrote &lt;i&gt;"If UID and GID are always 1000:1000 from CPAN, I can live with it."&lt;/i&gt; I just downloaded three .tar.gz files from CPAN at random and can verify that it is &lt;b&gt;not&lt;/b&gt; the case, I've got varying UID/GIDs.&lt;/p&gt;
&lt;p&gt;Tar archives store the UID/GID information, and my understanding is that when extracting tar archives as root, those UID/GIDs will be restored, and when running as a normal user they won't (because they can't). So [mod://CPAN] may not change the permissions, but untarring as root does.&lt;/p&gt;
&lt;p&gt;Modifying the system Perl is a whole nother topic, but one of the reasons not to do so, in short, is that installing modules into the system Perl can go wrong, and then picking apart the mess caused by that can be pretty bad. I now always install a fresh Perl in a different location (you can do this by hand fairly easily or with [mod://perlbrew]), and install all the modules I want there, and I see doing so recommended often. When I install modules into the system Perl I only use the system's repositories and package manager.&lt;/p&gt;
&lt;p&gt;Having said that, it's possible to clean up the situation - as was already said, you could just &lt;c&gt;sudo chown -R&lt;/c&gt; the &lt;c&gt;~/.cpan&lt;/c&gt; directory. Another possible solution that hasn't been mentioned yet is to run the build process as a normal user, but configure [doc://CPAN] to use [man://sudo] for only those commands where it is necessary, which is the "install" step. Search [doc://CPAN|the CPAN page] for "sudo", and it'll reveal the two config options you need to set: &lt;c&gt;make_install_make_command&lt;/c&gt; and &lt;c&gt;mbuild_install_build_command&lt;/c&gt; (note I haven't tried this myself but I see it referenced in several articles online).&lt;/p&gt;
&lt;p&gt;Hope this helps,&lt;br&gt;-- Hauke D&lt;/p&gt;</doctext>
<root_node field_name="root_node">1167279</root_node>
<parent_node field_name="parent_node">1167279</parent_node>
<thread id="1167279">
<node id="1167283"><node id="1167328"><node id="1167338"/>
</node>
</node>
<node id="1167290"><node id="1167329"/>
</node>
<node id="1167291"><node id="1167292"/>
<node id="1167330"/>
</node>
<node id="1167364"/>
<node id="1167365"/>
</thread></node>
