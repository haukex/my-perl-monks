<?xml version="1.0" encoding="UTF-8"?>
<node id="11160609" title="Repeating a capture group pattern within a pattern" created="2024-07-15 03:09:23" updated="2024-07-15 03:09:23" _doctext_mode="lenient"><type id="115">perlquestion</type>
<author id="1129327">mldvx4</author>
<doctext field_name="doctext">&lt;p&gt;
I am looking to simplify a pattern.  If I have a string &lt;code&gt;my $x = "0.01 NaN 2.30 4.44";&lt;/code&gt; then the following pattern finds the items present:
&lt;/p&gt;

&lt;code&gt;
my $r1 = qr/([Na0-9\.\-\+]+)\s+
	    ([Na0-9\.\-\+]+)\s+
	    ([Na0-9\.\-\+]+)\s+
	    ([Na0-9\.\-\+]+)/x;
&lt;/code&gt;

&lt;p&gt;
Notice that the same capture group criteria are repeated.  I wonder how I may write that so it is simpler, shorter, and all on one line.  Here is some pseudo-code to try to show what I am aiming for: &lt;code&gt;my $r1 = qr/(?=([Na0-9\.\-\+]+)\s+){4}/&lt;/code&gt;
&lt;/p&gt;

&lt;p&gt;
However, I've tried that and some permutations without luck:
&lt;/p&gt;

&lt;code&gt;
#!/usr/bin/perl

use strict;
use warnings;

my $x = "0.01 NaN 2.30 4.44";

# the following works as desired
my $r1 = qr/([Na0-9\.\-\+]+)\s+
	    ([Na0-9\.\-\+]+)\s+
	    ([Na0-9\.\-\+]+)\s+
	    ([Na0-9\.\-\+]+)/x;

my ($d, $e, $f, $g) = ($x =~ m/$r1/x );
print qq($d, $e, $f, $g\n);


# the following finds the first number twice
my $r2 = qr/(?=(([Na0-9\.\-\+]+)\s*)){4}/x;

($d, $e, $f, $g) = ($x =~ m/$r2/x );
print qq($d, $e, $f, $g\n);

# the following finds a null prior to the first item
my $r3 = qr/((?=([Na0-9\.\-\+]+)\s*){4})/x;

($d, $e, $f, $g) = ($x =~ m/$r3/x );
print qq($d, $e, $f, $g\n);

exit(0);
&lt;/code&gt;

&lt;p&gt;
How can I write that pattern so that the pattern it contains is repeated but not locked into the values found in the very first match?  Is this a case for using recursive patterns?
&lt;/p&gt;</doctext>
<thread id="11160609">
<node id="11160610"/>
<node id="11160613"><node id="11160614"><node id="11160617"><node id="11160618"><node id="11160619"/>
<node id="11160623"/>
<node id="11160628"/>
</node>
</node>
</node>
</node>
<node id="11160615"/>
<node id="11160616"/>
<node id="11160627"/>
<node id="11160667"/>
<node id="11160763"/>
</thread></node>
