<?xml version="1.0" encoding="UTF-8"?>
<node id="1194479" title="Re^3: Easiest way to filter a file based on user input (updated)" created="2017-07-07 07:38:13" updated="2017-07-07 07:38:13" _doctext_mode="strict"><type id="11">note</type>
<author id="830549">haukex</author>
<doctext field_name="doctext">&lt;blockquote&gt;&lt;i&gt;So for example, if the user enters -3, all the lines in the file that begin with a numerical value that is greater than 3 will be excluded.
Or if the user enters the value 1, all lines beginning with a value greater than 1 will be excluded.&lt;/i&gt;&lt;/blockquote&gt;
&lt;p&gt;So based on that description, the user entering &lt;c&gt;-3&lt;/c&gt; is the same as entering &lt;c&gt;3&lt;/c&gt;?&lt;/p&gt;
&lt;c&gt;
#!/usr/bin/env perl 
use strict;
use warnings;

print "Enter limit: ";
chomp( my $limit = &lt;STDIN&gt; );
$limit = abs($limit);

open my $in, '&lt;', "file.hairpin" or die $!;
open my $sifted, '&gt;', "new_file.hairpin" or die $!;

while (&lt;$in&gt;){
	next if /^None/;
	next if /^(\d+)/ &amp;&amp; $1 &gt; $limit;
	print $sifted $_;
}

close $in;
close $sifted;
&lt;/c&gt;
&lt;p&gt;Or as a oneliner (where "123" is the limit):&lt;/p&gt;
&lt;c&gt;
perl -ne 'print unless /^None/ || ( /^(\d+)/ &amp;&amp; $1&gt;123 )' file.hairpin &gt;new_file.hairpin
&lt;/c&gt;
&lt;p&gt;As for your code [id://1194471|here], it looks like you don't need to collect your lines in arrays but can write them to the output file directly (or, at the very least you don't need to [doc://open] your output file once per line of output).&lt;/p&gt;
&lt;p&gt;&lt;b&gt;Update:&lt;/b&gt; I just noticed that the sample input in the OP includes decimals and negative numbers, so you'd have to adjust the regex in my example code above accordingly. But before you try to develop really complex regexes, have a look at [mod://Regexp::Common::number].&lt;/p&gt;</doctext>
<root_node field_name="root_node">1194465</root_node>
<parent_node field_name="parent_node">1194472</parent_node>
<thread id="1194465">
<node id="1194467"><node id="1194471"><node id="1194491"><node id="1194503"/>
</node>
</node>
</node>
<node id="1194468"><node id="1194472"><node id="1194479"><node id="1194509"/>
<node id="1194518"><node id="1194519"><node id="1194686"><node id="1194688"><node id="1195200"><node id="1195201"><node id="1195207"><node id="1195209"><node id="1195212"/>
</node>
</node>
</node>
</node>
</node>
</node>
</node>
</node>
</node>
</node>
</node>
</thread></node>
