<?xml version="1.0" encoding="UTF-8"?>
<node id="11129239" title="Win32::PowerPoint Extension for Lines and Shapes" created="2021-03-07 00:53:31" updated="2021-03-07 00:53:31" _doctext_mode="lenient"><type id="115">perlquestion</type>
<author id="275270">nzsvz9</author>
<doctext field_name="doctext">I've added a function to the Win32::PowerPoint module successfully to draw a line and astonishingly enough it works:
&lt;code&gt;
sub add_line {
  my ($self, $options) = @_;

  return unless $self-&gt;slide;

  $options = {} unless ref $options eq 'HASH';

  my $new_line = $self-&gt;slide-&gt;Shapes-&gt;AddLine($options-&gt;{x1}, $options-&gt;{y1}, $options-&gt;{x2}, $options-&gt;{y2});

  return $new_line;
}
&lt;/code&gt;
I've tried to add a function to draw a shape with very similar extension like this:
&lt;code&gt;
sub add_shape {
  my ($self, $shape, $options) = @_;

  return unless $self-&gt;slide;

  $options = {} unless ref $options eq 'HASH';

  my $new_shape = $self-&gt;slide-&gt;Shapes-&gt;AddShape($shape, $options-&gt;{left}, $options-&gt;{top}, $options-&gt;{width}, $options-&gt;{height});

  return $new_shape;
}
&lt;/code&gt;
But it doesn't work.
&lt;p&gt;
In the horrible MSDN documents on VBA there's discussion about encoded shape names like 'msoShapeRectangle' which is shape enumeration 1, and when I call this function it draws nothing. I've tried calling this new function both the following ways with no success:
&lt;code&gt;
# Add the Rectangle
$pp-&gt;add_shape('msoShapeRectangle', { left=&gt;100, top=&gt;100, width=&gt;100, height=&gt;100});

# Add the Rectangle
$pp-&gt;add_shape(1, { left=&gt;100, top=&gt;100, width=&gt;100, height=&gt;100});
&lt;/code&gt;
Neither works.
&lt;p&gt;
Any help or guidance would be apprecaited. There's almost no help in the Win32::PowerPoint in CPAN.
&lt;p&gt;
Also, I've searched for hours for examples on even how to change the color of the line that I can successfully draw - to no avail.
&lt;p&gt;
Kind regards, nzsvz9
&lt;p&gt;
I'm not a coder, I just play one in my job</doctext>
<thread id="11129239">
<node id="11129245"><node id="11129288"><node id="11129294"/>
</node>
</node>
</thread></node>
