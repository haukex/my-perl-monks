<?xml version="1.0" encoding="UTF-8"?>
<node id="11109969" title="Re: File::Copy and wildcards" created="2019-12-11 05:46:02" updated="2019-12-11 05:46:02" _doctext_mode="lenient"><type id="11">note</type>
<author id="176576">eyepopslikeamosquito</author>
<doctext field_name="doctext">&lt;P&gt;
Dug out a primitive little helper function I wrote years ago in case it's of use.
Just does a simple non-recursive copy of wild card files from one dir to another.
Not proud of it but it still seems to work fine.
Might be useful if you just need something small and simple:
&lt;CODE&gt;
use strict;
use warnings;
use File::Copy;
use File::Basename;

=head2 copy_files_wild FROMDIR TODIR VERBOSE WILD

Copy files in wildcard WILD from FROMDIR to TODIR.
Return the number of files successfully copied.
Die if something goes wrong.

=cut

sub copy_files_wild
{
   my ( $fromdir, $todir, $verbose, $wild ) = @_;
   print "copy '$fromdir/$wild' to '$todir'.\n";
   -d $fromdir or die "error: dir '$fromdir' not found\n";
   -d $todir   or die "error: dir '$todir' not found\n";

   $fromdir =~ s/ /\\ /g;    # Escape any spaces in $fromdir
   my $ncopy = 0;
   while ( glob("$fromdir/$wild") ) {
      next unless -f;
      my $fname = File::Basename::basename($_);
      my $targ  = "$todir/$fname";
      $verbose and print "$fname\n";
      if ( File::Copy::copy( $_, $targ ) ) {
         ++$ncopy;
      } else {
         die "error: copy '$_' to '$targ':$!";
      }
   }
   print "done.\n";
   return $ncopy;
}

# Example
my $ncopied = copy_files_wild( 'DIR1', 'DIR2', 1, '*.txt' );
print "$ncopied files copied\n";
&lt;/CODE&gt;
&lt;/P&gt;
</doctext>
<root_node field_name="root_node">11109953</root_node>
<parent_node field_name="parent_node">11109953</parent_node>
<thread id="11109953">
<node id="11109954"/>
<node id="11109955"/>
<node id="11109956"/>
<node id="11109969"><node id="11109985"/>
</node>
<node id="11109973"/>
</thread></node>
