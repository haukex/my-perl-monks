<?xml version="1.0" encoding="UTF-8"?>
<node id="11101689" title="Re: Scanning Hashes for Repeated values" created="2019-06-21 15:31:41" updated="2019-06-21 15:31:41" _doctext_mode="lenient"><type id="11">note</type>
<author id="961">Anonymous Monk</author>
<doctext field_name="doctext">&lt;p&gt; My apologies for some of the ambiguity, I'll do what I can to clear it up. From the data, only one new group is added at a time, and it will never include parts of an group that has already been established eg. if A:B has been looped through B:C or A:C cannot exist, but C:D can. Only one new group or single element are added at a time. Here's an example list of what I mean: 51:50, 51:50:47, 51:50:47:42, 37:30, 51:50:47:42:37:30 and so on. Here's my current loop which only works for groups of two such as A:B.&lt;/p&gt;

&lt;code&gt; foreach (0..$#names) {
	my $dist = (($distances[$_]*$xaxisstep)+42);
	my @tmp = split /:/, $names[$_];
	my @tmp1;
	if (($#tmp+1)==2) {
		foreach (@tmp){ 
			$_++;
			print PS " 42.00 $labelmarks{$_} mt $dist $labelmarks{$_} ls\n";
		}
		print PS " $dist $labelmarks{${tmp[0]}} mt $dist $labelmarks{${tmp[1]}} ls\n";
		$xlabelmarks{$tmp[0].":".$tmp[1]}= $dist;
		$labelmarks{$tmp[0].":".$tmp[1]} = (($labelmarks{$tmp[0]}+$labelmarks{$tmp[1]})/2);
		delete ($labelmarks{$tmp[0]});
		delete ($labelmarks{$tmp[1]});	
	}
&lt;/code&gt;</doctext>
<root_node field_name="root_node">11101686</root_node>
<parent_node field_name="parent_node">11101686</parent_node>
<reputation field_name="reputation">9</reputation>
<thread id="11101686">
<node id="11101687"/>
<node id="11101689"><node id="11101700"/>
</node>
</thread></node>
