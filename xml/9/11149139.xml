<?xml version="1.0" encoding="UTF-8"?>
<node id="11149139" title="Re^3: Unclear about 'our'" created="2022-12-27 13:01:54" updated="2022-12-27 13:01:54" _doctext_mode="strict"><type id="11">note</type>
<author id="830549">haukex</author>
<doctext field_name="doctext">&lt;blockquote&gt;&lt;i&gt;In your rewrite, could you have written my $bar = 42; instead?&lt;/i&gt;&lt;/blockquote&gt;
&lt;p&gt;No, because lexical variables declared with [doc://my] are limited in visibility to their lexical scope, in this case that's the file &lt;c&gt;foo.pm&lt;/c&gt;; they don't show up in the package symbol table either. "Package globals" are "global" in the sense that you can reach across package boundaries to access other package's package variables - in the example here, you're reaching from &lt;c&gt;package main&lt;/c&gt; into &lt;c&gt;package foo&lt;/c&gt;. IMHO package globals are more and more rare and should nowadays only be used in places where they make sense.&lt;/p&gt;
&lt;p&gt;Note that package variables can always be accessed by their fully qualified name, e.g. &lt;c&gt;$foo::bar&lt;/c&gt;, while [doc://our] creates a shorter alias to those package variables, and the visibility of that alias is the current lexical scope.&lt;/p&gt;
&lt;c&gt;
package foo;
sub x {
	our $bar;  # $foo::bar
	$bar++;
	my $quz;   # a lexical $quz, not accessible outside this sub!
}
sub z {
	our $bar;  # the *same* $foo::bar
	$bar++;
	my $quz;   # a *different* lexical $quz
}
# no $bar here! but:
sub w {
	$foo::bar++;   # still the same $foo::bar
}
&lt;/c&gt;
&lt;blockquote&gt;&lt;i&gt;In the modules and programs I've written, I typically declare a bunch of 'my' variables up top, outside of any blocks, which subsequent subroutines can freely access, so I'm still baffled by what 'our' buys me.&lt;/i&gt;&lt;/blockquote&gt;
&lt;p&gt;For example, if one of those &lt;c&gt;my&lt;/c&gt; variables in your module was a default for something in one of the functions, then a user of your module could reach into your package and change that default, lexicals wouldn't allow this. (For debugging it's possible to inspect lexicals elsewhere, but this is not for production use.) I've whipped up a quick example of what I mean below.&lt;/p&gt;
&lt;p&gt;One example might be that in [doc://Data::Dumper], one way to change its configuration options is via such package variables, as in &lt;c&gt;$Data::Dumper::Useqq=1&lt;/c&gt;. However, it should be noted that this change is then global, so if you were to do it in a module, a user of your module might be surprised when their [doc://Data::Dumper] changes behavior - the OO interface of the module is probably better in that case. Another thing is that only package variables, not lexicals, can be used in [doc://local] to temporarily change their value in the current dynamic scope (the current call stack).&lt;/p&gt;
&lt;c&gt;
package bar;
our $DEFAULT_QUZ = "World";
sub hello {
	my $what = shift || $DEFAULT_QUZ;
	print "Hello, $what!\n";
}

package main;
bar::hello();          # Hello, World!
bar::hello("Alice");   # Hello, Alice!
$bar::DEFAULT_QUZ = "Bob";
bar::hello();          # Hello, Bob!
&lt;/c&gt;</doctext>
<root_node field_name="root_node">11149136</root_node>
<parent_node field_name="parent_node">11149138</parent_node>
<thread id="11149136">
<node id="11149137"><node id="11149138"><node id="11149139"><node id="11149152"><node id="11149162"><node id="11149168"><node id="11149169"/>
<node id="11149197"/>
</node>
</node>
</node>
</node>
<node id="11149150"/>
<node id="11149165"><node id="11149167"><node id="11149171"><node id="11149175"><node id="11149182"><node id="11149183"><node id="11149184"><node id="11149186"/>
</node>
<node id="11149189"/>
</node>
</node>
</node>
</node>
</node>
<node id="11149170"><node id="11149173"><node id="11149176"/>
</node>
</node>
<node id="11149195"/>
</node>
<node id="11149190"/>
</node>
</node>
<node id="11149140"/>
</thread></node>
