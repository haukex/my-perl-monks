<?xml version="1.0" encoding="UTF-8"?>
<node id="1203359" title="Re: iterative foreach loop" created="2017-11-14 06:28:33" updated="2017-11-14 06:28:33" _doctext_mode="lenient"><type id="11">note</type>
<author id="992570">Laurent_R</author>
<doctext field_name="doctext">Hi [hchana],
&lt;p&gt;
One possible solution relatively close to yours:
&lt;c&gt;
#!\usr\bin perl
use warnings;
use strict;
use diagnostics;
use feature 'say';

open my $FILE, "&lt;", "Text2.txt" or die;       # Three-argument syntax for open
my @lines = &lt;$FILE&gt;;            # actually reading the file into the array
foreach my $line (@lines) {     # iterating directly over the @lines array items
   say $line;                   # note that your lines already have an end of line, you would probably prefer to use print rather than say
}
close $FILE;
&lt;/c&gt;
But if all you want to do is to print the lines, then you don't need to store the file in an array: you can read the lines one by one and print them.

&lt;c&gt;
#!\usr\bin perl
use warnings;
use strict;
use diagnostics;

open my $FILE, "&lt;", "Text2.txt" or die; 
while (my $line = &lt;$FILE&gt;) {
    print $line;
}
close $FILE;
&lt;/c&gt;
The &lt;c&gt;while&lt;/c&gt; loop could be boiled down to one line with a statement modifier:
&lt;c&gt;
open my $FILE, "&lt;", "Text2.txt" or die; 
print while &lt;$FILE&gt;;
&lt;/c&gt;
In fact it can be even simpler, using the fact that &lt;c&gt;print&lt;/c&gt; provides a list context to the &lt;c&gt;&lt;...&gt;&lt;/c&gt; operator:
&lt;c&gt;
open my $FILE, "&lt;", "Text2.txt" or die; 
print &lt;$FILE&gt;;
&lt;/c&gt;
</doctext>
<root_node field_name="root_node">1203349</root_node>
<parent_node field_name="parent_node">1203349</parent_node>
<thread id="1203349">
<node id="1203351"><node id="1203358"><node id="1203360"/>
<node id="1203376"/>
</node>
</node>
<node id="1203359"/>
<node id="1203362"/>
<node id="1203459"><node id="1203462"><node id="1203464"/>
</node>
</node>
</thread></node>
