<?xml version="1.0" encoding="UTF-8"?>
<node id="1198749" title="Filtering content in Text::Xslate" created="2017-09-06 03:36:59" updated="2017-09-06 03:36:59" _doctext_mode="lenient"><type id="115">perlquestion</type>
<author id="1198741">IruP</author>
<doctext field_name="doctext">&lt;p&gt;In Mason i could define a filter:&lt;/p&gt;

&lt;code&gt;
&lt;%filter Div($class)&gt;
        &lt;div class="&lt;% $class %&gt;"&gt;
                &lt;% $yield-&gt;() %&gt;
        &lt;/div&gt;
&lt;/%filter&gt;
&lt;/code&gt;

&lt;p&gt;And later i could use it &lt;/p&gt; 

&lt;code&gt;
% $.Div("row") {{
   1 The "$yield-&gt;()" method returns everything from here (free text)
%       $.Div("col") {{
                2 even could have another nested filter, etc...
%       }}
% }}
&lt;/code&gt;

&lt;p&gt;with result&lt;/p&gt; 

&lt;code&gt; 
&lt;div class="row"&gt;
    1 The "$yield-&gt;()" method returns everything from here (free text)
   &lt;div class="col"&gt;
            2 even could have another nested filter, etc...   
   &lt;/div&gt;
&lt;/div&gt;
&lt;/code&gt;

&lt;p&gt;
E.g. the $yield-&gt;() method returns everything what is inside of the enclosed filter.

Want achieve same functionality using Text::Xslate, but don't know how.

The closest thing what I found ,are the macro blocks where i could write:
&lt;/p&gt; 

&lt;code&gt;
: macro div -&gt; ($cls,$str) {
&lt;div class="&lt;: $cls :&gt;"&gt;
        &lt;: $str :&gt;
&lt;/div&gt;
: }
&lt;/code&gt;

&lt;p&gt;and use it as&lt;/p&gt; 

&lt;code&gt;
: div("row",
:       div("col",
:               "my string"
:       )
: )
&lt;/code&gt;

&lt;p&gt;but the my string must be quoted and : marked, so it is impossible to use any free html text. E.g. the following dies. 

: div("row",

some free text here

&lt;code&gt;
:       div("col",
               nested
               here
:       )
: )
&lt;/code&gt;

After the long introduction, the question is simple:

Exists in the Text::Xslate something as the Mason's $yield-&gt;() call, which returns the content of the enclosed filter call? (or if not exists, how i could mimic it?)
&lt;/p&gt; </doctext>
<thread id="1198749">
<node id="1198756"/>
<node id="1198880"/>
</thread></node>
