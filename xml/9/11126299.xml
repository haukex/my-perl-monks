<?xml version="1.0" encoding="UTF-8"?>
<node id="11126299" title="Re: Array of hashes?" created="2021-01-04 12:25:35" updated="2021-01-04 12:25:35" _doctext_mode="strict"><type id="11">note</type>
<author id="830549">haukex</author>
<doctext field_name="doctext">&lt;blockquote&gt;&lt;i&gt;I thought it was an array of hashes, but apparently, it is not. How do I access it?&lt;/i&gt;&lt;/blockquote&gt;
&lt;p&gt;It is in fact an array of hashes, but I suspect the confusion might be coming from the fact that it's a &lt;i&gt;reference&lt;/i&gt; to an array of hashes, as indicated by the square brackets &lt;c&gt;[...]&lt;/c&gt;. In other words, &lt;c&gt;$VAR1&lt;/c&gt; here is a reference to an array, and the elements of that array are the hashes. You can access it like any reference to an array, i.e. &lt;c&gt;$VAR1-&gt;[0]{Lang9}&lt;/c&gt; is &lt;c&gt;'well'&lt;/c&gt;. See [doc://perldsc], [doc://perlreftut], and [doc://perlref].&lt;/p&gt;
&lt;p&gt;Note that you haven't shown your code, but in case it looks like &lt;c&gt;print Dumper(@array);&lt;/c&gt;, then I would guess that you're probably doing something like &lt;c&gt;my @array = decode_json(...);&lt;/c&gt;, which isn't quite correct, as &lt;c&gt;decode_json&lt;/c&gt; functions typically don't return an array, but instead a reference to an array or a hash. So in other words, you should be doing &lt;c&gt;my $arrayref = decode_json('[...]');&lt;/c&gt;, and then use the dereferencing operators that are described in the links above - for example, &lt;c&gt;for my $elem (@$arrayref)&lt;/c&gt; or &lt;c&gt;my $first = $arrayref-&gt;[0];&lt;/c&gt;.&lt;/p&gt;
&lt;p&gt;&lt;small&gt;&lt;i&gt;Edit:&lt;/i&gt; Added a little bit more info to first sentence.&lt;/small&gt;&lt;/p&gt;</doctext>
<root_node field_name="root_node">11126297</root_node>
<parent_node field_name="parent_node">11126297</parent_node>
<thread id="11126297">
<node id="11126299"><node id="11126302"><node id="11126304"><node id="11126308"/>
</node>
</node>
</node>
<node id="11126300"><node id="11126303"/>
</node>
<node id="11126307"/>
</thread></node>
