<?xml version="1.0" encoding="UTF-8"?>
<node id="1212149" title="Re: Is there any way to ignore certain words when substituing?" created="2018-04-02 06:42:55" updated="2018-04-02 06:42:55" _doctext_mode="strict"><type id="11">note</type>
<author id="830549">haukex</author>
<doctext field_name="doctext">&lt;blockquote&gt;&lt;i&gt;
&lt;c&gt;
$texttosub =~ s/(?!log10)|([a-zA-Z][A-Za-z_0-9]+)/$cats{$1}/i
&lt;/c&gt;
But I am getting an infinite loop for some reason.
&lt;/i&gt;&lt;/blockquote&gt;
&lt;p&gt;You don't need the alternation operator &lt;c&gt;|&lt;/c&gt;. [doc://perlre#Lookaround-Assertions] like &lt;c&gt;(?!...)&lt;/c&gt; are &lt;i&gt;zero-width&lt;/i&gt; ([doc://perlretut#Looking-ahead-and-looking-behind|tutorial]). So the regex &lt;c&gt;/ (?!foo) | bar /x&lt;/c&gt; means "match a &lt;b&gt;zero-length string&lt;/b&gt; as long as the next thing isn't &lt;c&gt;foo&lt;/c&gt;, or match &lt;c&gt;bar&lt;/c&gt;".&lt;/p&gt;
&lt;p&gt;However, even with the alternation operator removed, the regex still won't do what you expect: the pattern basically means "any letters or numbers, as long as the next thing isn't &lt;c&gt;log10&lt;/c&gt;", so the result will be &lt;c&gt;"l(5)"&lt;/c&gt;, because the substring &lt;c&gt;og10&lt;/c&gt; matches the pattern and there is no key &lt;c&gt;og10&lt;/c&gt; in &lt;c&gt;%cats&lt;/c&gt;!&lt;/p&gt;
&lt;p&gt;Although I've already said that I agree with [Corion] that using a proper parser is better, and I've [id://1212145|shown a different solution], just for the sake of completeness and TIMTOWTDI, here are two additional ways to do what you want. First, you can use the word boundary &lt;c&gt;\b&lt;/c&gt; to make sure that the string being matched isn't just a portion of a longer identifier: &lt;c&gt;s/(?!log10)(\b[a-zA-Z][A-Za-z_0-9]+)/$cats{$1}/&lt;/c&gt; works on the sample input you've shown.&lt;/p&gt;
&lt;p&gt;Second, you could look at all identifiers, and then figure out what they are once you've matched them. Here, I'm taking advantage of the &lt;c&gt;/e&lt;/c&gt; modifier to execute the replacement part as Perl code:&lt;/p&gt;
&lt;c&gt;
use warnings;
use strict;

my %cats = (blackcat=&gt;5, whitecat=&gt;10,orangecat=&gt;20);
my %funcs = map {$_=&gt;1} qw/ log10 sin cos /; # etc.

my $texttosub = "log10(blackcat)*whitecat*(log10(orangecat))";

$texttosub =~ s{(\b[a-zA-Z][a-zA-Z0-9_]+\b)}{
	my $repl;
	if ($cats{$1}) { $repl = $cats{$1} }
	elsif ($funcs{$1}) { $repl = $1 }
	else { die "Unknown identifier '$1'" }
	print "Matched '$1', replacement '$repl'\n"; # Debug
	$repl }eg;

print $texttosub, "\n";

__END__

Matched 'log10', replacement 'log10'
Matched 'blackcat', replacement '5'
Matched 'whitecat', replacement '10'
Matched 'log10', replacement 'log10'
Matched 'orangecat', replacement '20'
log10(5)*10*(log10(20))
&lt;/c&gt;</doctext>
<root_node field_name="root_node">1212142</root_node>
<parent_node field_name="parent_node">1212142</parent_node>
<thread id="1212142">
<node id="1212143"><node id="1212145"/>
</node>
<node id="1212149"/>
<node id="1212150"><node id="1212152"><node id="1212164"/>
</node>
</node>
<node id="1212160"/>
<node id="1212211"/>
</thread></node>
