<?xml version="1.0" encoding="UTF-8"?>
<node id="1189739" title="Re: Two Versions of Perl... (updated)" created="2017-05-07 11:01:34" updated="2017-05-07 11:01:34" _doctext_mode="strict"><type id="11">note</type>
<author id="830549">haukex</author>
<doctext field_name="doctext">&lt;p&gt;If this "get_iplayer" app is the one I found [https://github.com/get-iplayer/get_iplayer/blob/master/get_iplayer|here], then it starts with &lt;c&gt;#!/usr/bin/env perl&lt;/c&gt;, which means it will be run with whatever Perl comes first in the &lt;c&gt;PATH&lt;/c&gt; environment variable. You could show us this variable by running the command &lt;c&gt;echo $PATH&lt;/c&gt; in your terminal, and posting it here inside &lt;c&gt;&lt;code&gt;&lt;/c&gt; tags. Most likely, this second version of Perl installed in &lt;c&gt;/opt/dvr/bin&lt;/c&gt; comes first in &lt;c&gt;PATH&lt;/c&gt;.&lt;/p&gt;
&lt;p&gt;There are several ways to approach fixing this, such as fiddling with &lt;c&gt;PATH&lt;/c&gt; variable. However, that can get complicated quickly*. So here's the quick fix: since this app appears to contain only two Perl scripts, &lt;c&gt;get_iplayer&lt;/c&gt; and &lt;c&gt;get_iplayer.cgi&lt;/c&gt;, you could change the first line of both files from "&lt;c&gt;#!/usr/bin/env perl&lt;/c&gt;" to "&lt;c&gt;#!/usr/bin/perl&lt;/c&gt;" (I think that's the system Perl on OS X). Of course you'd have to make this change every time you updated the app, but depending on how often that happens, this might still be easier than changing the environment variables.&lt;/p&gt;
&lt;p&gt;* &lt;i&gt;Update:&lt;/i&gt; For example, I ask myself whether whichever program(s) that installed the Perl at &lt;c&gt;/opt/dvr/bin&lt;/c&gt; would break if you simply rearranged your &lt;c&gt;PATH&lt;/c&gt; entries. Also, it's possible to change &lt;c&gt;PATH&lt;/c&gt; temporarily when the &lt;c&gt;get_iplayer&lt;/c&gt; scripts get run via a small wrapper script. But whether that would be "easier" than my suggestion above depends on how much experience with shell scripting on OS X you have.&lt;/p&gt;
&lt;p&gt;&lt;b&gt;Update 2:&lt;/b&gt; If my fix above doesn't work, then that might mean that your script is being run in a way that disregards the shebang line, which is also a possibility. In that case some further investigation would be necessary, since it might be possible to make the change at the point where &lt;c&gt;perl get_iplayer&lt;/c&gt; is invoked. Or, you'd have to see about fixing your &lt;c&gt;PATH&lt;/c&gt;.&lt;/p&gt;</doctext>
<root_node field_name="root_node">1189737</root_node>
<parent_node field_name="parent_node">1189737</parent_node>
<thread id="1189737">
<node id="1189739"><node id="1189742"/>
<node id="1189746"><node id="1189747"><node id="1189750"><node id="1189756"><node id="1189758"/>
</node>
</node>
</node>
</node>
</node>
</thread></node>
