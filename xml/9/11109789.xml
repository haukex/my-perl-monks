<?xml version="1.0" encoding="UTF-8"?>
<node id="11109789" title="Best practices for local libraries" created="2019-12-06 23:11:48" updated="2019-12-06 23:11:48" _doctext_mode="lenient"><type id="115">perlquestion</type>
<author id="11109786">Barrabas</author>
<doctext field_name="doctext">&lt;p&gt;I would inquire of the monks: What are the best practices for storing locally generated libraries?&lt;/p&gt;

&lt;p&gt;Background: I write a lot of perl functions that are used by local programs and for obvious reasons I keep them in library files somewhere in my home directory for reuse. These are not CPAN functions, just lots of things I have written over time and that are useful to my workflow.&lt;/p&gt;

&lt;p&gt;I do work across several machines, and some of the programs need configuration parameters (think: IP addresses, usernames, port assignments and the like) so I also had a file "~/.config/perl/SiteConfig.pm" that exposes a hash of various bits of information of that nature.&lt;/p&gt;

&lt;p&gt;I set PERL5LIB so that any program I ran could access these libraries and everything worked well enough.&lt;/p&gt;

&lt;p&gt;All of this went horribly wrong when I went to apt-get install something as superuser - the root login carries over the PERL5LIB from the base user, and some of my files conflicted with system ones and the entire install process got borked. (Clearing PERL5LIB in root/bashrc fixes the problem, but doesn't fix the broken installs.)&lt;/p&gt;

&lt;p&gt;So my question is: what is the *preferred* placement and configuration for locally generated libraries and programs specific to a login?&lt;/p&gt;

&lt;p&gt;For instance, I have a directory "Math" containing several files "EM.pm", "Levy.pm", "Log2.pm", and so on. Should I place my library dirs in a subdir "Site" so that I have to "use Site::SomeDir::SomeLib" for each library, to avoid namespace conflicts? Should I use the -I option on all shebang lines with a specific directory, or is it better to use PERL5LIB?&lt;/p&gt;

&lt;p&gt;And is there a standard place (in my home directory) to put the directories containing the library files? For example, is .config/perl/ the right place for a shelf of library dirs?&lt;/p&gt;

What is "best practices" for this sort of setup?
&lt;p&gt;A related question is where to put executable perl programs that are specific to my login. Is there a "bin" directory position within my home directory that's seen as standard or typical?&lt;/p&gt;</doctext>
<thread id="11109789">
<node id="11109793"/>
<node id="11109796"><node id="11109797"><node id="11109798"/>
</node>
</node>
<node id="11109817"/>
<node id="11109828"><node id="11109835"/>
</node>
<node id="11109836"/>
<node id="11109893"/>
</thread></node>
