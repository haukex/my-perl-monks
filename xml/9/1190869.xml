<?xml version="1.0" encoding="UTF-8"?>
<node id="1190869" title="Re: What is correct way to reference?" created="2017-05-22 13:51:15" updated="2017-05-22 13:51:15" _doctext_mode="strict"><type id="11">note</type>
<author id="830549">haukex</author>
<doctext field_name="doctext">&lt;blockquote&gt;&lt;i&gt;One element could either have a single value or an array of values.&lt;/i&gt;&lt;/blockquote&gt;
&lt;p&gt;I agree with [Your Mother], sounds like you might be using [mod://XML::Simple]. I have to strongly recommend against this module (as its own documentation does, see the section "Status of this Module"), except for maybe the very simplest of cases, of which this is probably not one. Instead, look into other XML parsing modules, such as [mod://XML::LibXML], [mod://XML::Twig], or [mod://XML::Rules], which can output data structures much like [mod://XML::Simple]:&lt;/p&gt;
&lt;c&gt;
use warnings;
use strict;
use Data::Dump;
use XML::Rules ();

my $XML1 = '&lt;root&gt;&lt;phone&gt;&lt;callTo&gt;X&lt;/callTo&gt;&lt;/phone&gt;&lt;/root&gt;';
my $XML2 = '&lt;root&gt;&lt;phone&gt;&lt;callTo&gt;Y&lt;/callTo&gt;
	&lt;callTo&gt;Z&lt;/callTo&gt;&lt;/phone&gt;&lt;/root&gt;';

my $parser = XML::Rules-&gt;new(
	stripspaces =&gt; 3|4,
	rules =&gt; [
		root =&gt; 'pass',
		phone =&gt; 'as is',
		callTo =&gt; 'content array',
		_default =&gt; sub { die "Unknown tag $_[0]" },
	] );
dd $parser-&gt;parse($XML1);
dd $parser-&gt;parse($XML2);

__END__

{ phone =&gt; { callTo =&gt; ["X"] } }
{ phone =&gt; { callTo =&gt; ["Y", "Z"] } }
&lt;/c&gt;
&lt;p&gt;As for [mod://XML::Simple], note its &lt;c&gt;ForceArray&lt;/c&gt; option:&lt;/p&gt;
&lt;c&gt;
use XML::Simple qw/:strict XMLin/;

dd XMLin($XML1, KeyAttr=&gt;{}, ForceArray=&gt;[]);
dd XMLin($XML2, KeyAttr=&gt;{}, ForceArray=&gt;[]);
dd XMLin($XML1, KeyAttr=&gt;{}, ForceArray=&gt;['callTo']);

__END__

{ phone =&gt; { callTo =&gt; "X" } }
{ phone =&gt; { callTo =&gt; ["Y", "Z"] } }
{ phone =&gt; { callTo =&gt; ["X"] } }
&lt;/c&gt;
&lt;p&gt;Lastly, to answer your original question, if you have a scalar that holds either a single value or an array reference, one way to handle that is like the following. However, it should be clear from the above that this is not the best way to fix your problem.&lt;/p&gt;
&lt;c&gt;
foo( "one" );
foo( ["two", "three"] );

sub foo {
	my $x = shift;
	# the following does the trick
	my @values = ref $x eq 'ARRAY' ? @{$x} : $x;
	for my $val (@values) {
		print "&lt;$val&gt;\n";
	}
}

__END__

&lt;one&gt;
&lt;two&gt;
&lt;three&gt;
&lt;/c&gt;
&lt;p&gt;&lt;small&gt;&lt;i&gt;Update:&lt;/i&gt; Refactored last code example slightly to make the solution more clear. &lt;i&gt;Update 2:&lt;/i&gt; Used &lt;c&gt;callTo =&gt; 'content array'&lt;/c&gt; instead of &lt;c&gt;'as array'&lt;/c&gt; to make [mod://XML::Rules] output identical to [mod://XML::Simple].&lt;/small&gt;&lt;/p&gt;</doctext>
<root_node field_name="root_node">1190864</root_node>
<parent_node field_name="parent_node">1190864</parent_node>
<thread id="1190864">
<node id="1190865"/>
<node id="1190866"/>
<node id="1190868"/>
<node id="1190869"/>
<node id="1190875"><node id="1190885"><node id="1190894"/>
</node>
</node>
<node id="1190884"><node id="1190963"/>
</node>
</thread></node>
