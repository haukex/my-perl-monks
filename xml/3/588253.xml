<?xml version="1.0" encoding="UTF-8"?>
<node id="588253" title="Exiting subroutine via next" created="2006-12-06 22:58:52" updated="2006-12-06 17:58:52" _doctext_mode="lenient"><type id="115">perlquestion</type>
<author id="350425">InfiniteLoop</author>
<doctext field_name="doctext">Greetings Monks, &lt;br&gt;
&lt;div&gt;
&amp;nbsp;&amp;nbsp;My question is related to this post [id://503952].  Take this &lt;a href="http://sunson.livejournal.com/150682.html"&gt;code&lt;/a&gt;:
&lt;/div&gt;
&lt;code&gt;
sub foo {
	my $param = shift;
	if ($param eq "skip") {
		next;
	}
}

foreach my $var (1, 2, 3, "skip", 4) {
	foo($var);
	print $var;
}
&lt;/code&gt;
&lt;readmore&gt;
&lt;div&gt;
&amp;nbsp;I was expecting perl to throw me an error for using &lt;i&gt;next&lt;/i&gt; to return from a sub{} , however it doesn't happen so. (although I could get a warning, if I use the -w flag). Also [perldoc://next], says:
&lt;pre&gt;
"next" cannot be used to exit a block which returns a value such as "eval {}", "sub {}" or "do {}"...
&lt;/pre&gt;
&lt;/div&gt;
&lt;br&gt;
&lt;div&gt;
My question is: why this behaviour ? what is the logic behind it ? wouldn't it be beneficial if the interpreter threw a error for invalid use of next, in this case ? (I have tested this code under perl v5.8.6)
&lt;/div&gt;
&lt;/readmore&gt;</doctext>
<thread id="588253">
<node id="588264"><node id="588267"/>
<node id="588268"><node id="588476"/>
</node>
</node>
<node id="588271"/>
<node id="588272"/>
</thread></node>
