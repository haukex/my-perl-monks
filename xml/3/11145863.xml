<?xml version="1.0" encoding="UTF-8"?>
<node id="11145863" title="URL resolve (operations on path etc.)" created="2022-07-31 03:37:19" updated="2022-07-31 03:37:19" _doctext_mode="lenient"><type id="115">perlquestion</type>
<author id="1165397">bliako</author>
<doctext field_name="doctext">&lt;p&gt;Esteemed Monks,&lt;/p&gt;

&lt;p&gt;
I am trying to translate the following Node.js fragment to Perl (it takes a URL and sort of &lt;c&gt;chdir&lt;/c&gt; to it, clever enough to remove endpoint if a file and query):&lt;/p&gt;
&lt;c&gt;
const url = require('url');
const x = 'http:/a.com/a/b/c/d/e.json?x=1&amp;y=2';
const y = '../../../';
console.log(url.resolve(x, y));
&lt;/c&gt;
&lt;p&gt;result is : &lt;c&gt;http:/a.com/a/&lt;/c&gt;&lt;/p&gt;

&lt;p&gt;Now, if &lt;c&gt;x='http:/a.com/a/b/c/d/e.json/?x=1&amp;y=2';&lt;/c&gt; (i.e. the path points to a "directory" rather than to a "file") the result is &lt;c&gt;http:/a.com/a/b/&lt;/c&gt; (as expected IMO)&lt;/p&gt;

&lt;p&gt;I tried to do this with [mod://URI] but failed:&lt;/p&gt;
&lt;c&gt;
use URI;
my $x = 'http:/a.com/a/b/c/d/e.json?x=1&amp;y=2';
my $y = '../../../';
my $u = URI-&gt;new($x);
my $p = $u-&gt;path().'/'.$y;
$u-&gt;path($p);
print $u;
&lt;c&gt;
&lt;p&gt;With this result: &lt;c&gt;http:/a.com/a/b/c/d/e.json/../../../?x=1&amp;y=2&lt;/c&gt;&lt;/p&gt;

&lt;p&gt;The Node.js version seems to be able to understand that the last segment before the query is a "file" and removes it. Then it does the merging of the two paths including successful relative operations (&lt;c&gt;../&lt;/c&gt;). What is the safest way to achieve that behaviour in Perl? The last resort would be to shell-out to Node.js ...&lt;/p&gt;

&lt;p&gt;bw, bliako&lt;/p&gt;
</doctext>
<thread id="11145863">
<node id="11145864"><node id="11145865"/>
<node id="11145866"/>
<node id="11145884"/>
</node>
<node id="11145867"/>
</thread></node>
