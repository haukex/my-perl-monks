<?xml version="1.0" encoding="UTF-8"?>
<node id="1229163" title="Calling subclass’ method from superclass" created="2019-01-30 07:10:56" updated="2019-01-30 07:10:56" _doctext_mode="lenient"><type id="115">perlquestion</type>
<author id="1212863">Fendo</author>
<doctext field_name="doctext">&lt;p&gt;Hello, kind monks of Perl! Once again I come to ask for your wisdom, for I am short of good solutions on the problem I found.&lt;/p&gt;

&lt;p&gt;To give some background and idea of structure, I have a fairly simple program which deals with files. The object-oriented ‘Path::Class’ package is used for basic operations on files, but because I need to store some additional info about the files, and do some custom operations, I have decided to make my own subclasses, to extend what ‘Path::Class’ can do. To illustrate the structure of the classes:&lt;/p&gt;

&lt;ul&gt;
  &lt;li&gt;&lt;code&gt;Path::Class::File&lt;/code&gt; is a file class.&lt;/li&gt;
  &lt;li&gt;&lt;code&gt;Path::Class::Dir&lt;/code&gt; is a directory class.&lt;/li&gt;
  &lt;li&gt;&lt;code&gt;Own::File&lt;/code&gt; is my own file subclass to &lt;code&gt;Path::Class::File&lt;/code&gt; and &lt;code&gt;Own&lt;/code&gt;.&lt;/li&gt;
  &lt;li&gt;&lt;code&gt;Own::Dir&lt;/code&gt; is my own directory subclass to &lt;code&gt;Path::Class::Dir&lt;/code&gt; and &lt;code&gt;Own&lt;/code&gt;.&lt;/li&gt;
  &lt;li&gt;&lt;code&gt;Own&lt;/code&gt; is a secondary superclass holding subroutines common to both &lt;code&gt;Path::Class::File&lt;/code&gt; and &lt;code&gt;Path::Class::Dir&lt;/code&gt;, to avoid their duplication. There should be no direct objects of this class.&lt;/li&gt;
&lt;/ul&gt;

&lt;p&gt;Now, I want to make my own ‘parent’ subroutine, which would return an ‘Own::Dir’ object of a file's (or a directory's) parent directory. Because it should work the same for ‘Own::File’ and ‘Own::Dir’, I want to define it in ‘Own’, so that those two can inherit it. The problem is that the subroutine needs to invoke &lt;code&gt;Own::Dir-&gt;new&lt;/code&gt; in order to make the ‘Own::Dir’ object it's supposed to return. But when I write &lt;code&gt;return $Own::Dir-&gt;new ($parent)&lt;/code&gt;, Perl gives an error saying it ‘can't call method “new” on an undefined value’.&lt;/p&gt;

&lt;p&gt;From what I can gather, this is because the ‘Own’ package can't really access the innards of the ‘Own::Dir’ package. I didn't want to mess around with cyclic inheritances, and the best thing I could think of was implementing the subroutine in ‘Own::Dir’ and making that a superclass to ‘Own::File’, but conceptually that wouldn't make much sense, and would be rather inelegant.&lt;/p&gt;

&lt;p&gt;Is there a nice way of allowing ‘Own’ to invoke a method of ‘Own::Dir’? Should I maybe restructure the classes in a better way? For the record, I use the ordinary, built-in class (package) system. Thank you in advance!&lt;/p&gt;</doctext>
<thread id="1229163">
<node id="1229164"><node id="1229312"><node id="1229315"/>
</node>
</node>
<node id="1229168"><node id="1229433"/>
</node>
</thread></node>
