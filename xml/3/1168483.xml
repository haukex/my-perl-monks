<?xml version="1.0" encoding="UTF-8"?>
<node id="1168483" title="building sql statement" created="2016-07-25 13:42:37" updated="2016-07-25 13:42:37" _doctext_mode="lenient"><type id="115">perlquestion</type>
<author id="1168479">mwill007</author>
<doctext field_name="doctext">&lt;p&gt;I have to update database manually alot, so I wrote an app that reads the first line of a csv file and creates the headers/tables for an insert or update statement. The next lines are the data in the file.&lt;/p&gt;

&lt;p&gt;I build the statement like so&lt;/p&gt;

&lt;code&gt;
 if ($checksite eq $none)
  {
    $insertsqlstatement = 'INSERT INTO database.database(';

   $x=0;
   foreach (@dbcolumn)
    {
     $insertsqlstatement = $insertsqlstatement . "@dbcolumn[$x]";
#
     if ($x != $numelements )
      {
       $insertsqlstatement =  $insertsqlstatement . ",";
      }
#
     else
      {
       $insertsqlstatement = $insertsqlstatement . ') VALUES (';
      }
#
   $x=$x+1;
  }
#
   $x=0;
   foreach (@dbcolumn)
    {
     $insertsqlstatement = $insertsqlstatement . '\'' . "@splitarray[$x]" . '\'';
     $splitstatement = $splitstatement . '$' .@dbcolumn[$x] ;
#
     if ($x != $numelements)
      {
      $insertsqlstatement = $insertsqlstatement . ',';
      $splitstatement = $splitstatement . ',';
      }
#
     else
      {
       $insertsqlstatement = $insertsqlstatement .')';
      }
    $x=$x+1;
   }
  }
&lt;/code&gt;

&lt;p&gt;when I print the value of &lt;c&gt;$insertsqlstatement&lt;/c&gt; is equal to
&lt;c&gt;INSERT INTO database.database(site_id, site_store_type, project, site_city, site_state, site_country, project_begin ) VALUES ('9999','SC OSR','Prebuild','Penfield','NY','US','8/8/2016 ')&lt;/c&gt;&lt;/p&gt;

&lt;p&gt;I can copy it an it runs just fine in sql&lt;/p&gt;

&lt;p&gt;Buy when i write from my sql statement it will not write &lt;c&gt;sqlexec($insertsqlstatement);&lt;/c&gt;&lt;/p&gt;

&lt;p&gt;But if i manualy set the value of
&lt;c&gt;$insertsqlstatement = "INSERT INTO database.database(site_id, site_store_type, project, site_city, site_state, site_country, project_begin ) VALUES ('9999','SC OSR','Prebuild','Penfield','NY','US','8/8/2016 ')";&lt;/c&gt;&lt;/p&gt;

&lt;p&gt;The &lt;c&gt;sqlexec($insertsqlstatement);&lt;/c&gt; executes and updates the database&lt;/p&gt;
</doctext>
<thread id="1168483">
<node id="1168491"/>
<node id="1168493"><node id="1168496"/>
<node id="1168497"/>
</node>
<node id="1168494"/>
</thread></node>
