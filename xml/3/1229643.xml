<?xml version="1.0" encoding="UTF-8"?>
<node id="1229643" title="Suspending after catching TSTP" created="2019-02-08 14:37:57" updated="2019-02-08 14:37:57" _doctext_mode="lenient"><type id="115">perlquestion</type>
<author id="1229641">fanedd</author>
<doctext field_name="doctext">Hello everybody, I need some help with the following issue. 

&lt;p&gt;I am overwriting the default signal handler for TSTP signal, which will increment a flag. After this, I want the program to be suspended, so I can send it to background.&lt;/p&gt; I tried to send the  STOP signal after the program resumes execution, but the problem is that that can be anytime, and I want to keep the code relatively clean. &lt;p&gt;I also tried to send the signal from inside the signal handler, but that causes some core dumps from time to time. Another thing that I tried is to tie the flag, and in the STORE method send the STOP, but this also gets executed when handling the signal.&lt;/p&gt; Is there a way that I can maybe call STOP on resume, or call the default signal handler after I increment my flag? 


&lt;p&gt;Thanks in advance!&lt;/p&gt;</doctext>
<thread id="1229643">
<node id="1229644"/>
</thread></node>
