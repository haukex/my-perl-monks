<?xml version="1.0" encoding="UTF-8"?>
<node id="11109153" title="Re: Reversing the action of the while loop" created="2019-11-24 14:48:49" updated="2019-11-24 14:48:49" _doctext_mode="strict"><type id="11">note</type>
<author id="830549">haukex</author>
<doctext field_name="doctext">&lt;p&gt;Is this the same question as [id://11109149]? If so, then it would have been better to post it in a single thread.&lt;/p&gt;
&lt;p&gt;If your file is small enough to be read entirely into memory, then note that you can iterate over an array in both directions:&lt;/p&gt;
&lt;c&gt;
use warnings;
use strict;

my @array = qw/ abc def ghi /;

for my $x (@array) {
	print "$x\n";
}
for my $x (reverse @array) {
	print "$x\n";
}

while (@array) {
	my $x = shift @array;
	print "$x\n";
}

@array = qw/ abc def ghi /;
while (@array) {
	my $x = pop @array;
	print "$x\n";
}
&lt;/c&gt;
&lt;p&gt;See [doc://reverse], [doc://shift], and [doc://pop]. However, unless the files are short, reading an entire file into memory is usually less efficient than reading a file line-by-line, and [stevieb] [id://11109152|showed] one solution to reading the file backwards in the other thread.&lt;/p&gt;
&lt;p&gt;Also, note that your if-elsif chain would probably be better written as a hash. Basically, &lt;c&gt;$hash{$match}++&lt;/c&gt; would create a hash where the keys are the digits, and the values are the counters. If you wanted to have the keys be named after the digits, like your variables, a second hash would help, e.g. &lt;c&gt;my %digits = ( 1=&gt;'one', 2=&gt;'two', ... ); ... $hash{$digits{$match}}++&lt;/c&gt;. Also, in your code you're using fixed array indicies (&lt;c&gt;$lines[12]&lt;/c&gt; and &lt;c&gt;@lines[0..11]&lt;/c&gt;), but you're shrinking the array with [doc://shift], so that won't be accessing the same array elements. You can use &lt;c&gt;$#array&lt;/c&gt; to get the index of the last element of the array (-1 if it is empty), and you can say &lt;c&gt;$array[-1]&lt;/c&gt; to access the last element of the array, &lt;c&gt;$array[-2]&lt;/c&gt; for the second-to-last, etc.&lt;/p&gt;
&lt;p&gt;If you want help with your algorithm, please show some sample input and the expected output for that input, along with a runnable piece of code, in a single node. See also [id://174051] and [id://1177642].&lt;/p&gt;</doctext>
<root_node field_name="root_node">11109150</root_node>
<parent_node field_name="parent_node">11109150</parent_node>
<thread id="11109150">
<node id="11109153"><node id="11109159"><node id="11109220"><node id="11109271"><node id="11109284"/>
</node>
</node>
</node>
</node>
<node id="11109154"><node id="11109163"><node id="11109164"/>
<node id="11109171"><node id="11109182"><node id="11109184"/>
</node>
<node id="11109185"><node id="11109204"/>
<node id="11109216"/>
</node>
</node>
</node>
</node>
</thread></node>
