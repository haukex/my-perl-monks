<?xml version="1.0" encoding="UTF-8"?>
<node id="11102953" title="Re: validations in perl" created="2019-07-17 10:03:12" updated="2019-07-17 10:03:12" _doctext_mode="lenient"><type id="11">note</type>
<author id="847566">Don Coyote</author>
<doctext field_name="doctext">&lt;p&gt;Hello [id://11101807] and welcome to the Monastery&lt;/p&gt;
&lt;p&gt;The x modifier can help make matches readable in code. With this modifier any whitespace is not compiled into the pattern, unless escaped using the &lt;c&gt;\s&lt;/c&gt; class.&lt;/p&gt;
&lt;code&gt;
#!/usr/bin/perl -T;
use strict;
use warnings;

my $password = 'a!1"a$234]5%}&amp;&lt;AcdEf^&amp;B*#$")';

$password =~
                 /\A              # start of string
                 (                # start capture  
                 [                # start character class 
                 A-Z a-z _        # alpha letters (\w)
                 0-9              # numbers (\d)
                 &gt; &amp; { } &lt; \$ [    # specials 
                 \] ! " % ) ^ *  #
                 ]                # end character class
                 {28}             # match exactly 28 characters
                 )                # end capture
                 \z               # end of string
                 /x;              # match flags 


$password = $1 or die send_log('password_reset');

print "$password\n";

sub send_log{ 

        my $log_type = shift;
	$log_type =~ s/\A([A-Za-z][A-Za-z0-9_]{7,20})\z/$1/ or die;
	                                                  
     if($log_type eq 'password_reset'){
  print "The first part of a new password has been sent to\n",
	"your inbox. Call facilities on the internal line\n",
	"to recieve the next part. You will also recieve\n",
	"instructions for locating the pigeon wearing the\n",
	"last part.\n";
  # exit( release_pigeon( ++$pc ) ); 
    }
  exit;  
}
&lt;/code&gt;
&lt;p&gt;Hope this helps. To go back and see your earlier posts, with lots of good advice about this kind of thing, just click on the write ups number on your home node.&lt;/p&gt;
&lt;hr&gt;

</doctext>
<root_node field_name="root_node">11102928</root_node>
<parent_node field_name="parent_node">11102928</parent_node>
<thread id="11102928">
<node id="11102929"/>
<node id="11102930"><node id="11102948"><node id="11102949"/>
</node>
</node>
<node id="11102953"><node id="11102959"/>
</node>
<node id="11102958"/>
<node id="11102962"/>
<node id="11102977"/>
<node id="11103613"/>
</thread></node>
