<?xml version="1.0" encoding="UTF-8"?>
<node id="964383" title="Help with Regular Expressions" created="2012-04-10 14:25:57" updated="2012-04-10 14:25:57" _doctext_mode="lenient"><type id="115">perlquestion</type>
<author id="641409">FFSparky</author>
<doctext field_name="doctext">&lt;p&gt;Oh wise ones,&lt;/p&gt; 
&lt;p&gt;I have 2 string variables (these will be dynamic) containing file system paths.  As labeled one string ($BasePath) will contain the base / root path and the other string ($CurrentPath) will be the current path as the script is scanning the file system starting from the base path.    What I am after is a string containing just the subdirectory without the base / root path.&lt;/p&gt;
&lt;p&gt;Using the working example I have provided the result is what I am after  \DirD\DirE&lt;/p&gt;
&lt;p&gt;However, I am rather weak with regular expressions thus to me my solution seems to be rough and naturally only deals with the dreaded windows paths. I am hoping there may be a more efficient alternative that could be dynamic and also support unix paths.&lt;/p&gt;
&lt;code&gt;
use strict;
my $BasePath = '\\DirA\DirB\DirC';
my $CurrentPath = '\\DirA\DirB\DirC\DirD\DirE';

$BasePath =~ s/\x5c/\x0d/g;
$CurrentPath =~ s/\x5c/\x0d/g;

$CurrentPath =~ s/$BasePath//;

$CurrentPath =~ s/\x0d/\x5c/g;

print "\n\$CurrentPath: ".$CurrentPath."\n";
&lt;/code&gt;
&lt;p&gt;Thanks in advance.&lt;/p&gt;
</doctext>
<thread id="964383">
<node id="964389"><node id="964392"/>
</node>
<node id="964391"><node id="964400"><node id="964408"/>
</node>
</node>
<node id="964416"/>
<node id="964485"><node id="964541"/>
</node>
</thread></node>
