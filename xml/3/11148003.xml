<?xml version="1.0" encoding="UTF-8"?>
<node id="11148003" title="Re: How can I create an iterator that works inside foreach()" created="2022-11-06 03:12:06" updated="2022-11-06 03:12:06" _doctext_mode="strict"><type id="11">note</type>
<author id="830549">haukex</author>
<doctext field_name="doctext">&lt;p&gt;One of the major advantages of iterators is that they can produce items one at a time, which can be very significant if the items are expensive to produce and you don't need all of them, or you don't want the upfront cost all at once. Since, as others have said, &lt;c&gt;foreach&lt;/c&gt; generally operates on a list, it doesn't seem the most efficient approach to convert an iterator to a list. Consider for example processing the results of HTTP API calls: do you want to first do a ton of HTTP calls, store the results, and then process them, or do you want to process each result as it comes in?&lt;/p&gt;
&lt;p&gt;Another thing to consider is that the number of items returned from an iterator may not be known - it may even be infinite!* If you, for example, [doc://tie] an iterator to an array, using that array in a &lt;c&gt;foreach&lt;/c&gt; will cause it to actually iterate over the items returned from the iterator one-by-one, &lt;i&gt;but&lt;/i&gt; it will also call &lt;c&gt;FETCHSIZE&lt;/c&gt; to find out how many items there are.&lt;/p&gt;
&lt;p&gt;So my question would be: &lt;i&gt;why&lt;/i&gt; do you want to use an iterator in a &lt;c&gt;foreach&lt;/c&gt; instead of &lt;c&gt;while&lt;/c&gt;?&lt;/p&gt;
&lt;p&gt;(BTW, personally I like [mod://Iterator::Simple] a little bit better than [mod://Iterator]. You may also be interested in how I implemented a really simple iterator, including an overloading of the &lt;c&gt;&lt;&gt;&lt;/c&gt; operator, in my [https://metacpan.org/dist/Algorithm-Odometer-Tiny/source/lib/Algorithm/Odometer/Gray.pm|Algorithm::Odometer::Gray].)&lt;/p&gt;
&lt;p&gt;* &lt;i&gt;Update:&lt;/i&gt; To preempt any questions about what good an infinite iterator is: they can be used in operations like [mod://Iterator::Simple|&lt;c&gt;islice&lt;/c&gt;] or &lt;c&gt;zip&lt;/c&gt;.&lt;/p&gt;</doctext>
<root_node field_name="root_node">11147970</root_node>
<parent_node field_name="parent_node">11147970</parent_node>
<thread id="11147970">
<node id="11147971"/>
<node id="11147974"/>
<node id="11147975"><node id="11147977"><node id="11147981"/>
<node id="11147982"/>
<node id="11147983"/>
</node>
</node>
<node id="11148003"/>
</thread></node>
