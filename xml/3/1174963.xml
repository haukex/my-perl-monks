<?xml version="1.0" encoding="UTF-8"?>
<node id="1174963" title="Cool uses for multi-value GET/POST parameters" created="2016-10-30 10:48:29" updated="2016-10-30 10:48:29" _doctext_mode="lenient"><type id="115">perlquestion</type>
<author id="826278">Dallaylaen</author>
<doctext field_name="doctext">&lt;p&gt;Hello dear esteemed monks,&lt;/p&gt;

&lt;p&gt;I'm working on a &lt;a href="https://metacpan.org/pod/MVC::Neaf"&gt;toy web framework&lt;/a&gt;. One of its key features is that fetching user input, like cookies and parameters, requires regexp-based validation.&lt;/p&gt;

&lt;p&gt;The API is as follows: &lt;/p&gt;

&lt;code&gt;
    my $request = shift;
    my $foo = $request-&gt;param( foo =&gt; '\d+' );
&lt;/code&gt;

&lt;p&gt;This would only return a value when the WHOLE foo parameter matches regexp (one or more digits in this case), empty string* (or a sane default provided as optional third argument) otherwise.&lt;/p&gt;

&lt;p&gt;For now, this API deliberately ignores multi-valued query parameters, only using the LAST value. The time has come to fill this gaping hole. &lt;/p&gt;

&lt;p&gt;I'm thinking of the following method:&lt;/p&gt;

&lt;code&gt;
    my $request = shift;
    $request-&gt;param_arrayref( bar =&gt; '\d+' );
&lt;/code&gt;

&lt;p&gt;Note that we don't rely on calling context, but require an explicit statement ("list expected!") instead.&lt;/p&gt;

&lt;p&gt;One thing I'm confused about is what I should do if the value passed by user contains both matching and non-matching values. Filter out matching ones? Return empty array? Return undef (and possibly break someone's code in runtime)? Croak is not a variant - this is left for the user.&lt;/p&gt;

&lt;p&gt;I'm leaning towards the second options (a missing array is MUCH easier to debug than a missing element in the array). But may be there are other concerns? Or am I doing it wrong altogether? And btw, can arrayref be shortened to just array?&lt;/p&gt;

&lt;p&gt;Last but not least, what could be the real-life usage examples of passing the same argument multiple times in a web form? I'd rather do some trial and error as well as write a meaningful example which I can click through with my browser.&lt;/p&gt;

&lt;p&gt;Thank you,&lt;/p&gt;

&lt;p&gt;*To be replaced with undef in next version.&lt;/p&gt;</doctext>
<thread id="1174963">
<node id="1174965"/>
<node id="1174966"><node id="1174967"/>
</node>
<node id="1174982"><node id="1175060"/>
</node>
</thread></node>
