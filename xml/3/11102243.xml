<?xml version="1.0" encoding="UTF-8"?>
<node id="11102243" title="Re^4: Exiting eval via next: is that so bad?" created="2019-07-01 11:16:58" updated="2019-07-01 11:16:58" _doctext_mode="strict"><type id="11">note</type>
<author id="830549">haukex</author>
<doctext field_name="doctext">&lt;blockquote&gt;&lt;i&gt;Now imagine several more "verify" type calls within that same eval.&lt;/i&gt;&lt;/blockquote&gt;
&lt;p&gt;Yeah, I figured it was something like that. Personally I still wouldn't use that "&lt;c&gt;eval&lt;/c&gt;/&lt;c&gt;die&lt;/c&gt; with a fixed string" pattern, and instead maybe wrap each call in an &lt;c&gt;eval&lt;/c&gt;, or to make it look a little nicer, perhaps [mod://Try::Tiny]. Of course, this being Perl, other ways come to mind &lt;c&gt;:-)&lt;/c&gt; Say you've got a bunch of &lt;c&gt;verify_*&lt;/c&gt; functions that you can't change and whose errors are fatal, but you want nonfatal versions of them:&lt;/p&gt;
&lt;c&gt;
use warnings;
use strict;

# UUT
use Scalar::Util qw/looks_like_number/;
sub verify_number {
	my ($arg) = @_;
	die "not a number" if !defined $arg;
	looks_like_number($arg) or die "not a number";
	return;
}

# wrap the funcs
my @funcs = qw/ verify_number /;
for my $f (@funcs) {
	my $orig = \&amp;{$f};
	my $wrapped = sub { eval { $orig-&gt;(@_); 1 } };
	{ no strict 'refs'; *{$f.'_nf'} = $wrapped }
}

use Test::More tests =&gt; 4;

# helper
sub exception (&amp;)
	{ eval { shift-&gt;(); 1 } ? undef : ($@ || die "\$@ was false") }

ok  !exception { verify_number("3.14") };
like exception { verify_number("foo")  }, qr/\bnot a number\b/i;

ok  verify_number_nf("3.14");
ok !verify_number_nf("foo");
&lt;/c&gt;
&lt;p&gt;Of course you don't need to install the wrapped versions into the symbol table, a hash would work fine too.&lt;/p&gt;</doctext>
<root_node field_name="root_node">104789</root_node>
<parent_node field_name="parent_node">11102240</parent_node>
<thread id="104789">
<node id="104794"/>
<node id="104801"><node id="927610"><node id="927634"><node id="932005"><node id="932110"/>
<node id="969006"><node id="969023"/>
</node>
</node>
</node>
</node>
</node>
<node id="104810"/>
<node id="11101996"><node id="11102025"/>
<node id="11102046"/>
<node id="11102146"><node id="11102240"><node id="11102243"/>
</node>
</node>
</node>
</thread></node>
