<?xml version="1.0" encoding="UTF-8"?>
<node id="11135713" title="read database 500 error" created="2021-08-08 16:02:37" updated="2021-08-08 16:02:37" _doctext_mode="lenient"><type id="115">perlquestion</type>
<author id="11135711">PaganJim</author>
<doctext field_name="doctext">&lt;p&gt;Hello everyone. I have been away from PERL for quite a while, a few years actually. But I was looking at an old script that I wrote that I want to resurrect from the dead, so to speak. The old script was basically to help me learn now I am using that way again but it will go back up on the web at some point. When I originally wrote it, I didn't need a database, I just had a small amount of data that I kept in txt files. But now, I am expecting a lot more data so I am trying to move it to a db. I have a db and table populated with info. So I am trying to write a script to read the db line by line using fetchrow_array. But for some reason the script keeps popping a 500 error and there is nothing in my log files.&lt;/p&gt;

&lt;p&gt;So the script so far seems pretty straight forward but I cannot for the life of me find the error. And no errors show up in my error logs and no error is displayed. Anyone see anything? All feedback greatly appreciated since I am just getting back into perl.&lt;/p&gt;

&lt;code&gt;
#!/usr/bin/perl
use CGI::Carp qw(fatalsToBrowser warningsToBrowser);
use DBI;

## mysql user database name
$db ="dbname";
## mysql database user name
$user = "dbuser";
## mysql database password
$pass = "abc123"; 
## user hostname 
$host="localhost";

my $dsn = "DBI:mysql:database=$db;host=$host";
my $dbh = DBI-&gt;connect($dsn, $user, $pass) or 
            die("Error connecting to the database: $DBI::errstr\n");

my $sql = qq/select * from table/; 
my $sth = $dbh-&gt;prepare ($sql);

while (@row = $sth-&gt;fetchrow_array) { 
   my ($a, $b, $c, $d ) = @row;
       print ("--", @row), "\n\n"; }

$dbh-&gt;disconnect (); 
exit (0);
&lt;/code&gt;
</doctext>
<thread id="11135713">
<node id="11135715"><node id="11135727"/>
</node>
<node id="11135717"/>
<node id="11135720"><node id="11135773"/>
</node>
</thread></node>
