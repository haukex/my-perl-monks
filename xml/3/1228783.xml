<?xml version="1.0" encoding="UTF-8"?>
<node id="1228783" title="system commands/shell and perl variables." created="2019-01-21 16:32:34" updated="2019-01-21 16:32:34" _doctext_mode="lenient"><type id="115">perlquestion</type>
<author id="961">Anonymous Monk</author>
<doctext field_name="doctext">&lt;p&gt;Hello there, how you guys doing? Hope well.

I'm trying to write a perl script which will receive command line arguments and pass it to system.
&lt;/p&gt;

&lt;code&gt;

#!/usr/local/bin/perl
use strict;
use warnings;

# Check if program is being executead as root
if ("$&gt;" ne "0") {
        print "This program can't be executed as root\n";
        exit 2
}

# Get running vm's
my @get_vm_list = (`xl list`);


sub show_vm_list {
        print @get_vm_list;
}

my $vm_run = pop @ARGV ;



my %rec_args = ( vmlist =&gt; \&amp;show_vm_list,
                getdomid =&gt; \&amp;get_dom_id,
        );

my $arg = shift @ARGV;

if (defined $arg &amp;&amp; exists $rec_args{$arg}) {
        $rec_args{$arg}-&gt;();
}

&lt;/code&gt;

&lt;p&gt;
OK

here is my function which I'm trying to get working
&lt;/p&gt;

&lt;code&gt;
sub get_dom_id {
       my $XENDOMID = "xl list| DOM_ID=`fgrep $vm_run` | awk '{print $2}'` &amp;&amp; VNCPORT=`ps x \ 
| fgrep -m 1 "domid $DOM_ID" | awk '{print $1}'` &amp;&amp; sockstat -4l | fgrep $VNCPORT | awk '{print $6}'";
       print `$DOMID`;
}

&lt;/code&gt;

&lt;p&gt; I have already tried set system environment using $ENV{'MYVAR'} without success

&lt;p&gt; Any Directions will be appreciated, thanks in advance &lt;/p&gt;
</doctext>
<reputation field_name="reputation">9</reputation>
<thread id="1228783">
<node id="1228784"><node id="1228794"/>
</node>
<node id="1228802"/>
<node id="1228819"><node id="1228827"><node id="1228829"><node id="1228836"><node id="1228887"/>
</node>
</node>
</node>
</node>
<node id="1228846"/>
</thread></node>
