<?xml version="1.0" encoding="UTF-8"?>
<node id="1221393" title="SimpleZip If file doesn't exist" created="2018-08-30 16:41:02" updated="2018-08-30 16:41:02" _doctext_mode="lenient"><type id="115">perlquestion</type>
<author id="1221388">AlexandreG</author>
<doctext field_name="doctext">&lt;p&gt;Hello,&lt;/p&gt;

&lt;p&gt;I'm using SimpleZip to backup all my files/folders in a directory. I'm using the following code that working fine:&lt;/p&gt;

&lt;p&gt;find( sub { eval {$zip-&gt;add($_, Name =&gt; $File::Find::name) or warn "Cannot add '$_' to zip file: $SimpleZipError\n" ;}; warn $@ if $@ }, $data);&lt;/p&gt;

&lt;p&gt;The problem is as follow, this is how I understand it:
When Find go through the folder it create the first pass for all files, then adding the files one by one in the Zip file. We have large files that will take time to be added to the Zip file, if during time the large file is getting add to the zip file, one of the temporary file get remove by another process, when it's time to add this remove file to the zip, all other files will failed. then I'm getting the following error:&lt;/p&gt;

&lt;p&gt;Example:&lt;/p&gt;
&lt;p&gt;Cannot add 'File1' to zip file: File 'File_that_Was_Remove' does not exist&lt;/p&gt;
&lt;p&gt;Cannot add 'File2' to zip file: File 'File_that_Was_Remove' does not exist&lt;/p&gt;
&lt;p&gt;Cannot add 'File3' to zip file: File 'File_that_Was_Remove' does not exist&lt;/p&gt;

&lt;p&gt;It's like if the $_ value is correct but it report the error that it cannot find the file  $File::Find::name (stuck on the file that was removed), then the script end. Only the files from the FIND order was added up to the file that was removed.&lt;/p&gt;

&lt;p&gt;Hope that make sense.&lt;/p&gt;

&lt;p&gt;I would like to skip the deleted file in the FIND order when it try to add the deleted file in the SimpleZip.&lt;/p&gt;

&lt;p&gt;Thanks,&lt;/p&gt;
&lt;p&gt;Alex &lt;/p&gt;
</doctext>
<thread id="1221393">
<node id="1221400"/>
<node id="1221437"/>
<node id="1221470"/>
</thread></node>
