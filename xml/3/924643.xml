<?xml version="1.0" encoding="UTF-8"?>
<node id="924643" title="Why is variable length lookahead implemented while lookbehind is not?" created="2011-09-07 15:02:17" updated="2011-09-07 15:02:17" _doctext_mode="lenient"><type id="115">perlquestion</type>
<author id="871073">Kc12349</author>
<doctext field_name="doctext">&lt;p&gt;This is just a curiosity, but why is variable length lookbehind not implemented while variable length lookahead is?  Is it because there is no logical need for it, i.e. there is a better way to write a regex that uses it? Or is due to some limitation of how regex can be implemented in perl?
&lt;/p&gt;

&lt;p&gt;Admittedly, each time I have run into the error writing a quick regex, there has indeed been a better way to write the pattern.  To that end, a second question would be has anyone run into a use case where variable length lookbehind would be clearly useful over alternative patterns?
&lt;/p&gt;


&lt;p&gt;
The below is a useless piece of code save only to generate the error.&lt;/p&gt;

&lt;c&gt;
my $string = 'aaaabcccc';
$string =~ s/(?&lt;=a+)b(?=c+)/B/;
say $string;

# Variable length lookbehind not implemented in regex m/(?&lt;=a+)b(?=c+)/ at C:\Projects\test.pl line 15.
&lt;/c&gt;</doctext>
<thread id="924643">
<node id="924645"><node id="924670"/>
</node>
<node id="924647"/>
<node id="924648"><node id="924662"/>
</node>
<node id="924651"/>
<node id="924652"><node id="924657"><node id="924667"><node id="924671"/>
<node id="924681"/>
</node>
<node id="924673"/>
</node>
</node>
</thread></node>
