<?xml version="1.0" encoding="UTF-8"?>
<node id="1180363" title="DBI and SQLite's .read feature" created="2017-01-25 23:06:55" updated="2017-01-25 23:06:55" _doctext_mode="lenient"><type id="115">perlquestion</type>
<author id="64219">aplonis</author>
<doctext field_name="doctext">&lt;p&gt;So in SQLite you can do a .read to run all the SQL commands inside of a *.sql file. So in effect, you can drop and build temporary tables, populate them and finish with a &lt;code&gt;SELECT&lt;/code&gt; at the end.&lt;/p&gt;

&lt;p&gt;So... After a &lt;code&gt;.read foo.sql&lt;/code&gt; via the CLI in from SQLite, I get back rows which are the result of only just that final &lt;code&gt;SELECT&lt;/code&gt;.&lt;/p&gt;

&lt;p&gt;It would vastly simplify my Perl if I could issue such a &lt;code&gt;.read foo.sql&lt;/code&gt; via DBI and so receive back only just that final &lt;code&gt;SELECT&lt;/code&gt; just as &lt;code&gt;fetchrowarray()&lt;/code&gt; would get it. This rather than having to separately via DBI do all the table building and populating.&lt;/p&gt;

&lt;p&gt;This being my wish, is there any way, via DBI, to accomplish the equivalent of a &lt;code&gt;fetchrowarray()&lt;/code&gt; but with &lt;code&gt;.read foo.sql&lt;/code&gt; as the SQL command?&lt;/p&gt;</doctext>
<thread id="1180363">
<node id="1180364"/>
<node id="1180366"/>
<node id="1180368"><node id="1180385"/>
</node>
<node id="1180371"><node id="1180372"><node id="1180380"/>
</node>
</node>
<node id="1180373"/>
</thread></node>
