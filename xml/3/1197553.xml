<?xml version="1.0" encoding="UTF-8"?>
<node id="1197553" title="match digit, followed by hyphen, followed again by digit - then add whitespaces and replace in file" created="2017-08-17 08:48:28" updated="2017-08-17 08:48:28" _doctext_mode="lenient"><type id="115">perlquestion</type>
<author id="1124290">fasoli</author>
<doctext field_name="doctext">&lt;p&gt;Hi Wise Monks!&lt;/p&gt; 

&lt;p&gt;I've been really confused about a problem I'm having with a file. It's a text file, with 4 columns, and with a few thousand lines. The contents are numbers that look like this&lt;/p&gt; 

&lt;code&gt;1.234 5.6789 -1.235&lt;/code&gt;

&lt;p&gt;Those files occur as outputs from a software. The problem is that in some cases the contents look like this&lt;/p&gt; 

&lt;code&gt;1.234 5.6789-12.235&lt;/code&gt;

&lt;p&gt;*notice the number of the last column: because now there are 2 numbers before the decimal point, the number gets stuck on the second column.&lt;/p&gt; 

&lt;p&gt;Naturally now I'm having trouble plotting this file. So I'm trying to match strings where there is a digit, followed by a hyphen, followed by another digit, and then I want to replace this -hopefully correctly- with an added whitespace so that the numbers are correct.&lt;/p&gt; 

&lt;p&gt;I'm trying this and the regex match works, it does print the problematic bits:.&lt;/p&gt; 

&lt;code&gt;#!/usr/bin/perl

use warnings;
use strict;

my $test;

open my $INPUT, '&lt;', "file.txt" or die $!;

while (&lt;$INPUT&gt;) {
chomp $_;

   if ($_=~/(\d)(-)(\d)/) {
   print "$1$2$3 \n";
   }
}
&lt;/code&gt;

&lt;p&gt;But now I'm stuck: how do I complete the replace action? And how do I print the new contents of the file? I haven't succeeded in anything more than compilation errors. In terms of replacing, I've tried this &lt;/p&gt;

&lt;code&gt;   if ($_=~s/(\d)(-)(\d)/(\d)    (-)(\d)/) {  &lt;/code&gt;
 
&lt;p&gt;(supposedly telling the script to add spaces between the digit before the hyphen and the hyphen itself)&lt;/p&gt;

&lt;p&gt;but I get this error&lt;/p&gt;

&lt;code&gt;Unrecognized escape \d passed through&lt;/code&gt;

&lt;p&gt;Then I tried it with the $1$2$3 but again it was wrong. Can you give me any hints about how to make the replace function work?? Thank you so much!&lt;/p&gt;</doctext>
<thread id="1197553">
<node id="1197555"><node id="1197557"><node id="1197559"/>
</node>
</node>
<node id="1197556"><node id="1197560"><node id="1197561"/>
</node>
</node>
<node id="1197591"/>
<node id="1197601"/>
</thread></node>
