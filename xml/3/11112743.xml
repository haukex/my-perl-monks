<?xml version="1.0" encoding="UTF-8"?>
<node id="11112743" title="PERL REST API Post script" created="2020-02-10 15:51:44" updated="2020-02-10 15:51:44" _doctext_mode="lenient"><type id="115">perlquestion</type>
<author id="11112741">ptone</author>
<doctext field_name="doctext">Hi Team,

I am trying to use arguments to pass information from the command line into a perl script. These are the IP, Email, the User and the password. The IP works but the rest does not get parsed into the req for the content. If I hardcode these the script works.

&lt;code&gt;##!/usr/bin/perl -l

use strict;
use warnings;
use LWP::UserAgent;

my $username = "Polycom";
my $password = "789";
my $phoneip = $ARGV[0];
my $user = $ARGV[1];
my $Address = $ARGV[2];
my $passcode = $ARGV[3];

my $ua = LWP::UserAgent-&gt;new(
    ssl_opts =&gt; { verify_hostname =&gt; 0 },
    protocols_allowed =&gt; ['https'],
);

my $URL = "https://$phoneip/api/v1/mgmt/skype/signIn";

{

# Create the request object and add the authentication header and content
  my $req =  HTTP::Request-&gt;new(POST =&gt; $URL);
  $req-&gt;content_type('application/json');
  $req-&gt;authorization_basic( $username, $password );
  $req-&gt;content('{"data":{"Address": "$Address","User": "$user","Password": "$passcode","Domain":"","LockCode":""}}');

# Send the request to the user agent and print the result
my $response = $ua-&gt;request($req);
print "\r\n";
print $response-&gt;decoded_content;
}&lt;/code&gt;

With the above, I am trying to help one of our customers to remotely sign into a Phone but I cannot parse the info. It must be something little but I am stuck. Could someone kindly help me out? I actually managed the same in Power Shell but I like to offer various examples to them.

Best Regards

Steffen</doctext>
<thread id="11112743">
<node id="11112744"/>
<node id="11112745"/>
<node id="11112749"/>
<node id="11112761"/>
<node id="11112785"><node id="11112789"/>
</node>
</thread></node>
