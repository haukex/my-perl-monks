<?xml version="1.0" encoding="UTF-8"?>
<node id="1167773" title="Re: Variable in a foreach loop" created="2016-07-14 08:50:51" updated="2016-07-14 08:50:51" _doctext_mode="lenient"><type id="11">note</type>
<author id="1150057">perldigious</author>
<doctext field_name="doctext">&lt;p&gt;Can you show your complete code that your are attempting to run?  I see no declaration or initialization for &lt;code&gt;$max_cols&lt;/code&gt; or &lt;code&gt;$max_rows&lt;/code&gt;.  Assuming they exist elsewhere, this code would just print out the numbers 0 to whatever is in &lt;code&gt;$max_rows&lt;/code&gt; a number of times equal to &lt;code&gt;$max_cols&lt;/code&gt;+1 &lt;strike&gt;and then print the value of &lt;code&gt;$max_rows&lt;/code&gt; one additional time&lt;/strike&gt;.&lt;/p&gt;

&lt;p&gt;EDIT: Ah, I think I see the behavior you are trying to illustrate.  The last print actually doesn't get the last value (&lt;code&gt;$max_rows&lt;/code&gt;) of the inner foreach loop despite the omission of &lt;code&gt;my&lt;/code&gt;, it gets the original value scoped outside the loop (I added declarations and initializations).&lt;/p&gt;

&lt;p&gt;&lt;code&gt;use strict;
use warnings;

my $max_cols = 3;
my $max_rows = 3;

my $row_element = 0;
my $col_element = 0;

foreach my $col_element (0 .. $max_cols)
{
    foreach $row_element (0 .. $max_rows)
    {
        print $row_element;
    }
}

print $row_element;&lt;/code&gt;&lt;/p&gt;

&lt;p&gt;This isn't what I would have expected (obviously from my original post) either, but as [haukex] pointed out it does appear to be the intended behavior.  I've never tried using a foreach loop without &lt;code&gt;my&lt;/code&gt;.  I guess you would be stuck doing something like this if you wanted the original scalar outside the foreach loop to update.&lt;/p&gt;

&lt;p&gt;&lt;code&gt;use strict;
use warnings;

my $max_cols = 3;
my $max_rows = 3;

my $row_element = 0;
my $col_element = 0;

foreach my $col_element (0 .. $max_cols)
{
    foreach my $row_element_a (0 .. $max_rows)
    {
		$row_element = $row_element_a;
        print $row_element;
    }
}

print $row_element;&lt;/code&gt;&lt;/p&gt;

&lt;!-- Node text goes above. Div tags should contain sig only --&gt;
&lt;div class="pmsig"&gt;&lt;div class="pmsig-1150057"&gt;
I love it when things get difficult; after all, difficult pays the mortgage.  - Dr. Keith Whites&lt;br&gt;
I hate it when things get difficult, so I'll just sell my house and rent cheap instead.  - perldigious
&lt;/div&gt;&lt;/div&gt;</doctext>
<root_node field_name="root_node">1167769</root_node>
<parent_node field_name="parent_node">1167769</parent_node>
<thread id="1167769">
<node id="1167770"/>
<node id="1167771"/>
<node id="1167773"><node id="1167774"><node id="1167780"/>
</node>
</node>
<node id="1167829"/>
<node id="1167885"/>
</thread></node>
