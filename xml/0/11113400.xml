<?xml version="1.0" encoding="UTF-8"?>
<node id="11113400" title="ne vs. ! eq" created="2020-02-25 08:34:18" updated="2020-02-25 08:34:18" _doctext_mode="lenient"><type id="115">perlquestion</type>
<author id="1225732">boerni</author>
<doctext field_name="doctext">Hey Monks

I never realized perl behaves like this. There is a difference between (! 'a' eq 'b') and ('a' ne 'b').

&lt;code&gt;
#!perl
use strict;
use warnings;
use Data::Dumper;

my $s1 = 'bla';
my $s2 = 'blu';

my $r = $s1 eq $s2;
print Dumper $r;

if (! $s1 eq $s2) {
    print "$s1 and $s2 are not the same\n";
}
else {
    print "$s1 and $s2 are the same\n";
}

exit 0;
&lt;/code&gt;

prints:
&lt;code&gt;
$VAR1 = '';
bla and blu are the same
&lt;/code&gt;

&lt;p&gt;The "correct" ($s1 ne $s2) works as expected. But why does (! $s1 eq $s2) not work?&lt;/p&gt;
&lt;p&gt;Probably there is a simple explanation but I don't know it... Maybe one of you Monks can explain this.&lt;/p&gt;

&lt;p&gt;Thank you&lt;/p&gt;
</doctext>
<thread id="11113400">
<node id="11113401"><node id="11113403"/>
</node>
<node id="11113414"/>
</thread></node>
