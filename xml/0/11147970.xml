<?xml version="1.0" encoding="UTF-8"?>
<node id="11147970" title="How can I create an iterator that works inside foreach()" created="2022-11-04 17:55:47" updated="2022-11-04 17:55:47" _doctext_mode="lenient"><type id="115">perlquestion</type>
<author id="11146525">PUCKERING</author>
<doctext field_name="doctext">I am trying to create an iterator that will work inside a foreach ().  No problem creating one for a while (), but foreach () eludes me.
&lt;p&gt;
I've tried a closure, and a class (with a next method), and even the Iterator module.  No joy.
&lt;/p&gt;&lt;p&gt;
Here's an example, cribbed from the Iterator POD:
&lt;/p&gt;
&lt;code&gt;
use Iterator;

sub irange_limited {
    my ($start, $end) = @_;
 
    return Iterator-&gt;new ( 
        sub {
            Iterator::is_done()
                if $start &gt; $end;
     
            return $start++;
        }
    );
}

my $it = irange_limited(3, 5);

foreach ( $it-&gt;value ) {say $_ }
&lt;/code&gt;
That prints 3, when it should print lines containing 3, 4 and 5.
&lt;p&gt;Even the venerable High Order Perl book, which has an entire chapter devoted to iterators and some fine examples, fails to mention anything about using them in foreach -- or not being able to.  And while foreach is used within the implementation of some examples, they use cases all use while().  This leads me to think there may be some fundamental reason why it cannot work.
&lt;/p&gt;&lt;p&gt;
Your wisdom and enlightenment, as always, would be much appreciated.
&lt;/p&gt;
</doctext>
<thread id="11147970">
<node id="11147971"/>
<node id="11147974"/>
<node id="11147975"><node id="11147977"><node id="11147981"/>
<node id="11147982"/>
<node id="11147983"/>
</node>
</node>
<node id="11148003"/>
</thread></node>
