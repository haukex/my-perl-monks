<?xml version="1.0" encoding="UTF-8"?>
<node id="1205840" title="How to grep matching IP address from a log file?" created="2017-12-19 01:50:05" updated="2017-12-19 01:50:05" _doctext_mode="lenient"><type id="115">perlquestion</type>
<author id="1205777">dotowwxo</author>
<doctext field_name="doctext">&lt;p&gt; Hi all, currently i have this script that looks for IP address that is in the netmask specified in $matcher and print that line from mylogFile.log &lt;/p&gt;

&lt;code&gt;

#!/usr/bin/perl

use strict;
use warnings;
use Net::Subnet;

my $filename = 'mylogFile.log';
open(my $fh, '&lt;:encoding(UTF-8)', $filename)
  or die "Could not open file '$filename' $!";
my @cidr_list = &lt;$fh&gt;;

my $matcher = subnet_matcher qw(192.168.1.0/22);

my @grepIPs = grep(/\b(25[0-5]|2[0-4][0-9]|[01]?[0-9][0-9]?)\./,@cidr_list);

for (my $i=0; $i &lt; scalar @grepIPs; $i++){	

	my $boolean = $matcher-&gt;($grepIPs[$i]);
	if ($boolean == 1){
		print $grepIPs[$i];
	}
	else{
		print "Nothing\n";
	}
}


&lt;/code&gt;

&lt;p&gt; However, $grepIPS only matches lines that only have IP address specified without any other strings in that particular line. &lt;/p&gt;

&lt;p&gt; For e.g., my script only matches lines that only has IP address: &lt;/p&gt;
&lt;code&gt;
192.168.1.2
192.168.1.3
192.168.1.4 
&lt;/code&gt;

&lt;p&gt; it does not matches lines that has other strings involved as shown below: &lt;/p&gt;
&lt;code&gt;
2017-12-08 07:01:39 &lt;my source IP&gt; GET /course-detail.aspx id=66&amp;catColor=0 443 - 192.168.1.0 (e.g., IP) &lt;server version etc. strings&gt; 200 0 0 530 
&lt;/code&gt;

&lt;p&gt; What I want as my output:  I want to grep only the IP addresses (not the whole line which I can't feed into my for loop to check) from the line so that I can feed it into my for loop to see if the IP is in the netmask, and if yes, I would like to print the whole line. Can anyone guide me on how I can achieve this? any response is kindly appreciated, thank you. &lt;/p&gt; 

&lt;code&gt; 
Sample output that I want to achieve: 

2017-12-08 07:01:39 &lt;my source IP&gt; GET /course-detail.aspx id=66&amp;catColor=0 443 - 192.168.1.1 (e.g., IP) &lt;server version etc. strings&gt; 200 0 0 530
2017-12-08 07:01:39 &lt;my source IP&gt; GET /course-detail.aspx id=66&amp;catColor=0 443 - 192.168.1.2 (e.g., IP) &lt;server version etc. strings&gt; 200 0 0 530
2017-12-08 07:01:39 &lt;my source IP&gt; GET /course-detail.aspx id=66&amp;catColor=0 443 - 192.168.1.3 (e.g., IP) &lt;server version etc. strings&gt; 200 0 0 530

#Note that I only want to grep IP addresses (192.168.1.1-3) that are in the same netmask specified in $matcher
&lt;/code&gt;
</doctext>
<thread id="1205840">
<node id="1205841"/>
<node id="1205842"><node id="1205844"><node id="1205845"/>
<node id="1205854"/>
</node>
</node>
<node id="1205846"><node id="1205847"/>
</node>
<node id="1205876"/>
</thread></node>
