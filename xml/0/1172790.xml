<?xml version="1.0" encoding="UTF-8"?>
<node id="1172790" title="Re: Perl script to run for x hours" created="2016-09-27 22:45:11" updated="2016-09-27 22:45:11" _doctext_mode="lenient"><type id="11">note</type>
<author id="210889">marinersk</author>
<doctext field_name="doctext">&lt;p&gt;As noted by others, what the script is doing generally drives the decision on how to implement the time limit.&lt;/p&gt;

&lt;p&gt;That said, if you have something that can run for hours, one hopes it is doing some menial task inside a loop.  The most graceful (but to [Happy-the-monk]'s point, not necessarily the most timely) approach, in my opinion, is to grab the time at the start of the loop, precompute the ending time, and then check current time at the top of the loop and see if we've passed the deadline.&lt;/p&gt;

&lt;p&gt;Something like this:&lt;/p&gt;

&lt;p&gt;
&lt;code&gt;
#!/usr/bin/perl
use strict;
use warnings;

my $run_limit_hours = 9;              # You would of course grab this from @ARGV or something
my $start_time = time;                # Number of seconds since epoch
my $end_time =                        # Deadline -- end time in seconds-since-epoch
    $start_time +
    $run_limit_hours * 60 * 60;       # 60 seconds per minute, 60 minutes per hour

while (1)
{
    my $current_time = time;
    if ($current_time &gt; $end_time)
    {
        last;
    }
    &amp;doTheThing();
}

exit;
&lt;/code&gt;
&lt;/p&gt;
</doctext>
<root_node field_name="root_node">1172747</root_node>
<parent_node field_name="parent_node">1172747</parent_node>
<thread id="1172747">
<node id="1172749"><node id="1172850"><node id="1172914"/>
</node>
</node>
<node id="1172756"/>
<node id="1172757"/>
<node id="1172790"/>
</thread></node>
