<?xml version="1.0" encoding="UTF-8"?>
<node id="1178780" title="Global variables and SimpleHTTP request handler" created="2017-01-01 17:37:43" updated="2017-01-01 17:37:43" _doctext_mode="lenient"><type id="115">perlquestion</type>
<author id="961">Anonymous Monk</author>
<doctext field_name="doctext">&lt;p&gt;I an writing a simple web server that receives web requests from an application (Scratch), which I want to process into commands and place in a queue. For the queue I'm trying to use a global array, then have a loop watching that queue to process when able. I can not get the global variables to work.&lt;/p&gt;

&lt;p&gt;A cut down version of the code is:&lt;/p&gt;
&lt;code&gt;
package MyServer;
use warnings;
use strict;

use HTTP::Server::Simple::CGI;
use base qw(HTTP::Server::Simple::CGI);

our @command_queue;
sub handle_request {
	my ($self, $cgi) = @_;
	my $cmd_out;

        # Some processing here which populates $cmd_out
	push(@command_queue, $cmd_out);
	myDebug("Command queue has $#command_queue elements");
}

my $pid = MyServer-&gt;new($PORT)-&gt;background();
my $quit=0;
while($quit==0) {
        print "Commands in the queue = $#command_queue\n";
        sleep(1);
}
&lt;/code&gt;

&lt;p&gt;As each request comes in, the debugger reports the queue increasing in size, so command_queue is being maintained between calls to request_handler. However in the queue monitoring loop at the end, command_queue is constantly reported as empty, -1 in size.&lt;/p&gt;

&lt;p&gt;How can I share data between the request_handler and another section of the program?&lt;/p&gt;
</doctext>
<reputation field_name="reputation">8</reputation>
<thread id="1178780">
<node id="1178782"><node id="1178812"><node id="1178815"><node id="1178846"/>
</node>
</node>
</node>
<node id="1178801"><node id="1178850"><node id="1178851"><node id="1178881"/>
</node>
<node id="1178855"/>
</node>
</node>
<node id="1178813"/>
</thread></node>
