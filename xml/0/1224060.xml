<?xml version="1.0" encoding="UTF-8"?>
<node id="1224060" title="Re^2: Skript help needed - RegEx &amp; Hashes" created="2018-10-15 15:59:16" updated="2018-10-15 15:59:16" _doctext_mode="strict"><type id="11">note</type>
<author id="830549">haukex</author>
<doctext field_name="doctext">&lt;p&gt;Glad to help, that's what we're here for :-)&lt;/p&gt;
&lt;p&gt;Like I said, don't worry too much about the formatting - it's much more important that you apply whatever formatting style you choose consistently, because no matter what formatting, if indentation isn't applied consistently, it's much easier to make mistakes. Using one of the more common styles might make your code a little easier to read to others, but inconsistent indentation is much more problematic.&lt;/p&gt;
&lt;blockquote&gt;&lt;i&gt;$h-&gt;{"3p-tRFs"} . How was it possible to replace the variable call from my version with " -&gt; " ?&lt;/i&gt;&lt;/blockquote&gt;
&lt;p&gt;[doc://perlop#The-Arrow-Operator] is both the method call operator and the &lt;i&gt;dereferencing&lt;/i&gt; operator. For example, I can say:&lt;/p&gt;
&lt;c&gt;
my %hash = ( hello =&gt; "foo", world =&gt; "bar");
my $hashref = \%hash;       # store a reference to the %hash
print $hashref-&gt;{hello};    # prints "foo"
$hashref-&gt;{world} = "quz";  # change "bar" to "quz" in orig. hash
&lt;/c&gt;
&lt;p&gt;References are explained quite nicely in [doc://perlreftut] - if you've heard of the concept of pointers, references are kind of like "safer" pointers, and therefore less scary ;-) Two advantages of references are that (a) instead of copying data structures when they are passed as arguments to functions*, you can just pass a reference instead, which saves memory and allows the function to modify the original data if desired, and (b) you can build complex data structures out of them, for example an array can contain a list of references to hashes, then you have an AoH (array of hashes); hash values can be references to arrays (HoA, hash of arrays), and all sorts of complex data structures. You can see plenty of examples of the latter in [doc://perldsc], and references are explained in detail in [doc://perlref].&lt;/p&gt;
&lt;p&gt;An "anonymous" hash or array is called that because it doesn't have a name. In "&lt;c&gt;my $hashref = \%hash;&lt;/c&gt;", the hash being referenced by &lt;c&gt;$hashref&lt;/c&gt; has a name, &lt;c&gt;%hash&lt;/c&gt;. In "&lt;c&gt;my $hashref = {};&lt;/c&gt;", this does basically the same as the previous piece of code, but now the hash referenced by &lt;c&gt;$hashref&lt;/c&gt; is newly created and does not have a name (quite useful when building nested data structures).&lt;/p&gt;
&lt;blockquote&gt;&lt;i&gt;... Data:Dumper. Isn't it contra-productive to use it when you are working with large data-sets and would end up printing a lot of content into the terminal?&lt;/i&gt;&lt;/blockquote&gt;
&lt;p&gt;It's just intended for debugging output - I figured that's what you wanted because you were using &lt;c&gt;print&lt;/c&gt;s in your original code. It's no problem to comment them out, or probably even better to do something like:&lt;/p&gt;
&lt;c&gt;
my $DEBUG = 0;  # at the top of the program
...
$DEBUG and print Dumper(...);
# or
print Dumper(...) if $DEBUG;
&lt;/c&gt;
&lt;p&gt;I personally prefer the former because it's visually a bit easier to skip those lines when you're skimming the code. Or, if performance is a concern, the following will be optimized away (the disadvantage being it's a bit trickier to change a constant via a command-line option):&lt;/p&gt;
&lt;c&gt;
use constant DEBUG =&gt; 0;
...
DEBUG and print Dumper(...);
&lt;/c&gt;
&lt;p&gt;&lt;small&gt;&lt;i&gt;Minor edits for clarity.&lt;/i&gt;&lt;/small&gt;&lt;/p&gt;
&lt;p&gt;* &lt;i&gt;Update 2:&lt;/i&gt; This statement applies when using the common ways to access arguments: &lt;c&gt;sub abc { my ($foo,$bar,...) = @_ }&lt;/c&gt; and &lt;c&gt;sub abc { my $foo=shift; my $bar=shift; ... }&lt;/c&gt;. Don't worry about accessing the elements of &lt;c&gt;@_&lt;/c&gt; directly just yet (&lt;c&gt;$_[0]&lt;/c&gt;, &lt;c&gt;$_[1]&lt;/c&gt;, ...), that's a topic for another day.&lt;/p&gt;</doctext>
<root_node field_name="root_node">1223791</root_node>
<parent_node field_name="parent_node">1224047</parent_node>
<thread id="1223791">
<node id="1223793"/>
<node id="1223794"><node id="1223795"><node id="1223796"/>
</node>
</node>
<node id="1223798"/>
<node id="1223816"><node id="1223839"/>
</node>
<node id="1223825"/>
<node id="1223879"><node id="1223882"><node id="1223889"/>
</node>
<node id="1223883"/>
<node id="1223913"><node id="1223917"><node id="1223919"/>
</node>
</node>
</node>
<node id="1224047"><node id="1224060"/>
<node id="1224086"><node id="1224087"><node id="1224090"/>
</node>
</node>
</node>
</thread></node>
