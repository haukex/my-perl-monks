<?xml version="1.0" encoding="UTF-8"?>
<node id="1230600" title="rectangularizing input to become array" created="2019-02-26 23:47:00" updated="2019-02-26 23:47:00" _doctext_mode="lenient"><type id="115">perlquestion</type>
<author id="947695">Aldebaran</author>
<doctext field_name="doctext">&lt;p&gt;I'm attempting to present an SSCCE. I realized that I can use directories on github, and I read somewhere that a good name for a default directory in perl development is 'lib'. I have a calling script which uses [Path::Tiny] to find an input file in 'lib'. The following link contains the entire source and input file: [https://github.com/TBlazer66/SSCCE|SSCCE] (1.1 kB).&lt;/p&gt;&lt;p&gt;&lt;b&gt;Update:&lt;/b&gt;In the time between when I posted the problem and when I have solutions, laden with tests, this workspace has grown to just under 5k. The first script 1.sscce.pl should still "work," but one can see how much distance we traveled by looking at 5.sscce.pl .&lt;/p&gt;
&lt;p&gt;But one need not chase an external link to comment on the substance of my question. I'm a fortran then a C guy, so I'm alway tempted to go to external loops to set values in arrays. Likewise, the c technique of doing one char at a time is effective, but not extensible the way I desire. I do not seek solutions here that look like fortran or c. The perl solution is elegant and attainable.&lt;/p&gt;

&lt;p&gt;Caller is pretty plain, but I'll put code and initial output between readmore tags:&lt;/p&gt;

&lt;readmore&gt;
&lt;c&gt;$ pwd
/home/bob/Desktop/SSCCE-master/sscce
$ ls
1.sscce.pl  lib
$ cat 1.sscce.pl 
#!/usr/bin/perl -w
use 5.011;
use Path::Tiny;
use lib "lib";
use Data::Dump;

my $path_to_file = path( "lib", "1.txt" );
my $guts = $path_to_file-&gt;slurp;
say "return1 is ";
say "$guts";
my @lines = $path_to_file-&gt;slurp;
say "return2 is ";
say "@lines";
my $ref_lines = \@lines;
my $width     = 6;
my $length    = 4;
my $ref_array = make_rectangular1( $guts, $width, $length );

#dd $ref_array;

sub make_rectangular1 {
  my ( $guts, $width, $length ) = @_;

  my $ref_array = "nothing yet";
  return $ref_array;
}

sub make_rectangular2 {
  my ( $ref_array, $width, $length ) = @_;
  my @new_array = @$ref_array;
  say "new array is ";
  say "@new_array";

  #my $ref_array = "nothing yet";
  return $ref_array;
}

__END__ 
$
&lt;/c&gt;
&lt;p&gt;Output is pretty clear:&lt;/p&gt;
&lt;c&gt;$ ./1.sscce.pl 
return1 is 
abcdef
abcdefg
abcde
 bcdefgh
 bcd	




return2 is 
abcdef
abcdefg
abcde
 bcdefgh
 bcd	




$ 
&lt;/c&gt;
&lt;/readmore&gt;
&lt;p&gt;What I would like is for these inputs to get trimmed to an array of the size specified by $width and $length . Spaces added/substituted on right if necessary to pad each vector to the same size. There's a sub stubbed out for each of the slurp possibilities. &lt;/p&gt;

&lt;p&gt;Also, what character is it that renders an entire space dark?&lt;/p&gt;

&lt;p&gt;Thanks for your comment,&lt;/p&gt;





</doctext>
<thread id="1230600">
<node id="1230601"><node id="1230699"><node id="1230700"><node id="1230703"/>
<node id="1230769"><node id="1230781"/>
</node>
</node>
</node>
</node>
<node id="1230602"><node id="1230684"/>
</node>
<node id="1230610"><node id="1230701"><node id="1230707"/>
<node id="1230710"/>
</node>
</node>
</thread></node>
