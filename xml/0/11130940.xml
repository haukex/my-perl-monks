<?xml version="1.0" encoding="UTF-8"?>
<node id="11130940" title="Re: use alternate module if pm is not installed" created="2021-04-07 05:15:04" updated="2021-04-07 05:15:04" _doctext_mode="strict"><type id="11">note</type>
<author id="830549">haukex</author>
<doctext field_name="doctext">&lt;p&gt;One way to do this is:&lt;/p&gt;
&lt;c&gt;
BEGIN {
	unless ( eval 'use DesiredModule; 1' ) {
		require AlternateModule;
		AlternateModule-&gt;import();
	}
}
&lt;/c&gt;
&lt;p&gt;However, I've also [id://11126640|made the argument] that using two different modules is not necessarily the best approach. There's ways to get modules onto systems where you don't have permissions ([mod://local::lib] or even [mod://App::FatPacker]), plus if you're going to be writing your own module anyway, why not just always use that?&lt;/p&gt;
&lt;p&gt;&lt;i&gt;Updates:&lt;/i&gt; Added the &lt;c&gt;-&gt;import&lt;/c&gt; call, as per [choroba]'s post. It's often not required for OO modules. If you need to pass a specific import list, you can do &lt;c&gt;eval 'use DesiredModule qw/some_func other_func/; 1'&lt;/c&gt; and &lt;c&gt;AlternateModule-&gt;import(qw/some_func other_func/);&lt;/c&gt;. Another possible variation is that you can replace the inner block &lt;c&gt;require ... -&gt;import();&lt;/c&gt; with &lt;c&gt;eval 'use AlternateModule; 1' or die $@||'unknown error';&lt;/c&gt;. Stringy [doc://eval] is often the wrong choice, but in this case it's fine because it's using a fixed string of code.&lt;/p&gt;</doctext>
<root_node field_name="root_node">11130939</root_node>
<parent_node field_name="parent_node">11130939</parent_node>
<thread id="11130939">
<node id="11130940"/>
<node id="11130952"/>
<node id="11130943"/>
<node id="11130942"/>
<node id="11130954"/>
<node id="11130941"/>
<node id="11130951"/>
<node id="11130978"/>
</thread></node>
