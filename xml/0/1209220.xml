<?xml version="1.0" encoding="UTF-8"?>
<node id="1209220" title="Running a perl script with root, changing to another user and then back to root?" created="2018-02-15 09:13:05" updated="2018-02-15 09:13:05" _doctext_mode="lenient"><type id="115">perlquestion</type>
<author id="1209216">morthed</author>
<doctext field_name="doctext">&lt;p&gt; I've tried to search a lot around the web for this question, to no avail. &lt;\p&gt; &lt;p&gt;Currently I'm building a perl script that needs to do a specific job for each line in some database. The database contains several arguements including a "user" field. The script runs as root, and in each line iteration in the db, it will need to switch to the user provided and operate as the user, finishing the round and exiting back to root and then again, running as a different user. &lt;\p&gt;&lt;p&gt; So far I've tried: &lt;\p&gt; 
&lt;p&gt; using POSIX::setuid($User) in order to switch to the user within the same script, meaning:&lt;\p&gt;&lt;code&gt;while (my $line = &lt;$DB&gt;) .. some code .. POSIX::setuid($User); #switch to user ... do stuff POSIX::setuid(0); #return to root &lt;/code&gt; &lt;p&gt;But this solution didn't work so well, after changing once it was stuck in this user through all iterations of the loop, and couldn't return to root, which is obviously logical because it doesn't have the correct permissions.&lt;\p&gt;&lt;p&gt;Since this didn't work, I thought to myself maybe I should split the job into 2 scripts. One script will run as root, whereas the other script will be called to by root in the first script and then use POSIX::setuid. I thought, that maybe because it's a subproc\shell it'll just return itself back to root once the job is done on the second script, but it doesn't work as well... I'm kinda out of ideas, anything you guys think'll help?&lt;\p&gt; &lt;p&gt; ANOTHER THING: I know this is probably incorrect, so that's why I ask this around... please keep mean comments off or stuff like "you don't know what you're doing", if I did I wouldn't have asked... I basically look for a safe way to change the eid\uid of the SECOND SCRIPT only and keep root as the user for the main one... is there a way for that? Thanks! &lt;\p&gt;</doctext>
<thread id="1209220">
<node id="1209223"><node id="1209224"><node id="1209228"/>
</node>
</node>
<node id="1209225"/>
<node id="1209230"/>
<node id="1209424"/>
</thread></node>
