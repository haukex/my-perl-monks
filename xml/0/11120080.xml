<?xml version="1.0" encoding="UTF-8"?>
<node id="11120080" title="Find Photos Taken During A Certain Time Range" created="2020-07-30 14:51:47" updated="2020-07-30 14:51:47" _doctext_mode="lenient"><type id="115">perlquestion</type>
<author id="180961">Limbic~Region</author>
<doctext field_name="doctext">All,
&lt;br /&gt;
I am looking for a little help with a [mod://File::Find::Rule] and [mod://Image::ExifTool] script.  My environment is Windows but I only have Perl available via Cygwin.  I have a bunch of hard drives in this machine collected over the years so a manual search has proven to be untenable.
&lt;ul&gt;
&lt;li&gt;Search all files with a .jpg or .jpeg extension (case insensitive)&lt;/li&gt;
&lt;li&gt;Once found, check to see when the photo was taken.&lt;/li&gt;
&lt;li&gt;If taken in 2015, give me the path to the file and the date the picture was taken&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;
I haven't really coded in years so while this once seemed like a trivial task, I am having issues getting the expected results.
&lt;/p&gt;
&lt;code&gt;
#!/usr/bin/perl
use strict;
use warnings;
use File::Find::Rule;
use Image::ExifTool qw(:Public);

my $tool = Image::ExifTool-&gt;new();


for my $file (File::Find::Rule-&gt;file()-&gt;name( qr/\.(jpeg|jpg)$/i)-&gt;in('/')) {
    my $info = ImageInfo($file, 'DateTimeOriginal');
    my $date = $info-&gt;{DateTimeOriginal};
    next if ! $date || $date !~ /^2015/;
    print "$file\t$date\n";
}
&lt;/code&gt;
&lt;p&gt;
When I tried it in my home directory as a test, it did exactly what I expected but when I changed '.' to '/' it started generating every file on the system (directories, text files, etc.) without the date.
&lt;/p&gt;

&lt;div class="pmsig"&gt;&lt;div class="pmsig-180961"&gt;
&lt;p&gt;
Cheers - [Limbic~Region|L~R]
&lt;/p&gt;
&lt;/div&gt;&lt;/div&gt;</doctext>
<thread id="11120080">
<node id="11120082"/>
<node id="11120083"/>
<node id="11120084"><node id="11126630"/>
</node>
</thread></node>
