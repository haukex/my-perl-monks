<?xml version="1.0" encoding="UTF-8"?>
<node id="11144617" title="Re: Heredoc Inconsistency: Bareword Filehandle vs Lexical Filehandle" created="2022-06-09 15:55:44" updated="2022-06-09 15:55:44" _doctext_mode="strict"><type id="11">note</type>
<author id="830549">haukex</author>
<doctext field_name="doctext">&lt;c&gt;
$ cat 11144614.pl 
#!/usr/bin/env perl
use warnings;
use strict;

my $hfile = 'foo.txt';

# GOOD
open(HFILE, '&gt;', $hfile) or die "$hfile: $!";
print HFILE &lt;&lt; "#EOT";
#Usage: foo.pl
#EOT

# BAD
open(my $HFILE, '&gt;', $hfile) or die "$hfile: $!";
print $HFILE &lt;&lt; "#EOT";
#Usage: foo.pl
#EOT

# GOOD
open($HFILE, '&gt;', $hfile) or die "$hfile: $!";
print {$HFILE} &lt;&lt; "#EOT";
#Usage: foo.pl
#EOT

$ perl 11144614.pl 
Argument "#EOT" isn't numeric in left bitshift (&lt;&lt;) at 11144614.pl line 15.
10294240

$ perl -MO=Deparse,-p 11144614.pl 
use warnings;
use strict;
(my $hfile = 'foo.txt');
(open(HFILE, '&gt;', $hfile) or die("${hfile}: $!"));
print(HFILE "#Usage: foo.pl\n");
(open(my $HFILE, '&gt;', $hfile) or die("${hfile}: $!"));
print(($HFILE &lt;&lt; '#EOT'));
(open($HFILE, '&gt;', $hfile) or die("${hfile}: $!"));
print({$HFILE;} "#Usage: foo.pl\n");
11144614.pl syntax OK
&lt;/c&gt;
&lt;p&gt;[doc://perlobj#Indirect-Object-Syntax] strikes again... and from [doc://print]:&lt;/p&gt;
&lt;blockquote&gt;NOTE: If FILEHANDLE is a variable and the next token is a term, it may be misinterpreted as an operator unless you interpose a &lt;c&gt;+&lt;/c&gt; or put parentheses around the arguments.&lt;/blockquote&gt;
&lt;p&gt;&lt;small&gt;&lt;b&gt;Update:&lt;/b&gt; &lt;tt&gt;-MO=Deparse&lt;ins&gt;,-p&lt;/ins&gt;&lt;/tt&gt; makes it even more obvious.&lt;/small&gt;&lt;/p&gt;</doctext>
<root_node field_name="root_node">11144614</root_node>
<parent_node field_name="parent_node">11144614</parent_node>
<thread id="11144614">
<node id="11144617"><node id="11144621"/>
</node>
</thread></node>
