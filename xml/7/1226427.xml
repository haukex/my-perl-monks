<?xml version="1.0" encoding="UTF-8"?>
<node id="1226427" title="Re^2: Easing cross-platform scripting" created="2018-11-27 16:12:41" updated="2018-11-27 16:12:41" _doctext_mode="lenient"><type id="11">note</type>
<author id="49324">kschwab</author>
<doctext field_name="doctext">&lt;blockquote&gt;&lt;i&gt;module that loads either Win32::SerialPort or Device::SerialPort, although they both provide a very similar API&lt;/i&gt;&lt;/blockquote&gt;

I've done something like that...not an "Any" module, but conditionally loading one or the other.  You do need to wrap it in a BEGIN block and eval, due to the way "use" works:

&lt;code&gt;
#
# Use Win32::Serial port on Windows otherwise, use Device::SerialPort
#;
BEGIN 
{
   my $IS_WINDOWS = ($^O eq "MSWin32" or $^O eq "cygwin") ? 1 : 0;
   #
   if ($IS_WINDOWS) {
      eval "use Win32::SerialPort";
      die "$@\n" if ($@);
   } else {
      eval "use Device::SerialPort";
      die "$@\n" if ($@);
   }
}
&lt;/code&gt;

Then something like this when using it:

&lt;code&gt;
my $IS_WINDOWS = ($^O eq "MSWin32" or $^O eq "cygwin") ? 1 : 0;
if ($IS_WINDOWS) {
   $serial = new Win32::SerialPort ($port, 1);
} else {
   $serial = new Device::SerialPort ($port, 1);
}
&lt;/code&gt;

Somewhat ugly, but handy for anyone using my module.   There's likely some more elegant way to do it.</doctext>
<root_node field_name="root_node">1226402</root_node>
<parent_node field_name="parent_node">1226410</parent_node>
<thread id="1226402">
<node id="1226405"><node id="1226411"/>
</node>
<node id="1226407"/>
<node id="1226408"><node id="1226409"><node id="1226424"/>
</node>
</node>
<node id="1226410"><node id="1226413"/>
<node id="1226427"><node id="1226430"/>
<node id="1226452"/>
</node>
<node id="1226428"/>
</node>
</thread></node>
