<?xml version="1.0" encoding="UTF-8"?>
<node id="1183127" title="declaring a common loop variable" created="2017-02-28 06:48:05" updated="2017-02-28 06:48:05" _doctext_mode="lenient"><type id="115">perlquestion</type>
<author id="1182149">pritesh_ugrankar</author>
<doctext field_name="doctext">&lt;p&gt;Hi,&lt;/p&gt;

&lt;p&gt;Is it ok to declare a common loop variable if I want to iterate through multiple arrays?&lt;/p&gt;

&lt;code&gt;
use warnings;
use strict;

my @arr1 = qw (this will print for sure);
my @arr2 = qw (this is second array);
my $things = "nothing";
foreach $things (@arr1) {
	print "$things\n";
};
	print "$things\n";

foreach $things (@arr2) {
	print "$things\n";
};
	print "$things\n";
                                     
&lt;/code&gt;
&lt;p&gt; I am using "nothing" just to verify if it gets reset to it's original value and do understand that "nothing" is a string, and has "nothing" to do (pun unintended) with undef or empty.&lt;/p&gt;
&lt;p&gt;So this will be a single copy of loop variable used for multiple arrays. My question is, is this considered ok in Perl? or am I to have a seperate loop variable for each of the arrays? Even if I have the same name for the loop variable, the following two $things are different right?
&lt;/p&gt;
&lt;code&gt;
use warnings;
use strict;

my @arr1 = qw (this will print for sure);
my @arr2 = qw (this is second array);
foreach my $things (@arr1) {
	print "$things\n";
};

foreach my $things (@arr2) {
	print "$things\n";
};
&lt;/code&gt;</doctext>
<thread id="1183127">
<node id="1183129"/>
<node id="1183130"/>
<node id="1183135"/>
<node id="1183139"/>
<node id="1183144"/>
<node id="1183206"/>
</thread></node>
