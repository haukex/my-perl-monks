<?xml version="1.0" encoding="UTF-8"?>
<node id="11102267" title="Re^2: Question about base64 encoded attributes with Net::LDAP::LDIF" created="2019-07-01 15:45:44" updated="2019-07-01 15:45:44" _doctext_mode="lenient"><type id="11">note</type>
<author id="11102002">steiner</author>
<doctext field_name="doctext">&lt;p&gt;Sorry, "encryption" was not what I meant, but "encoded".  Here's example code:&lt;/p&gt;

&lt;code&gt;
use Net::LDAP::LDIF;
our $old_ldif = Net::LDAP::LDIF-&gt;new( "dump.ldif", "r", onerror =&gt; 'undef' );
our $new_ldif = Net::LDAP::LDIF-&gt;new( "dump_new.ldif", "w", onerror =&gt; 'undef', wrap =&gt; 76, encode =&gt; 'base64' );
while (not $old_ldif-&gt;eof()) {
    my $entry = $old_ldif-&gt;read_entry();
    if ($old_ldif-&gt;error()) {
        warn "Error msg: ", $old_ldif-&gt;error(), "\n";
        warn "Error lines:\n", $old_ldif-&gt;error_lines(), "\n";
    } else {
        # delete attributes no longer needed
        foreach my $a ($entry-&gt;attributes()) {
            if ($a =~ /^attrMatch/) {
                $entry-&gt;delete($a);
            }
        }
        $new_ldif-&gt;write_entry($entry);
    }
}
$old_ldif-&gt;done();
$new_ldif-&gt;done();
&lt;/code&gt;
&lt;p&gt;
Assuming userPassword is not the attribute to remove, I would expect a diff between dump.ldap and dump_new.ldap to only show the attribute(s) that were deleted.  But in dump.ldif (from an OpenLDAP slapcat command) the userPassword field is base64 encoded.  In the dump_new.ldif file, it's not base64 encoded.  Makes the diff harder to read... nothing incorrect with either version though.
&lt;/p&gt;

</doctext>
<root_node field_name="root_node">11102005</root_node>
<parent_node field_name="parent_node">11102157</parent_node>
<thread id="11102005">
<node id="11102011"><node id="11102034"/>
</node>
<node id="11102157"><node id="11102267"><node id="11102269"><node id="11102314"><node id="11102492"><node id="11103250"/>
</node>
</node>
</node>
</node>
</node>
</thread></node>
