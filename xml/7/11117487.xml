<?xml version="1.0" encoding="UTF-8"?>
<node id="11117487" title="matching lines in a long string" created="2020-05-29 20:29:04" updated="2020-05-29 20:29:04" _doctext_mode="lenient"><type id="115">perlquestion</type>
<author id="421781">Cristoforo</author>
<doctext field_name="doctext">Hello monks!&lt;p&gt;

Been working on this problem and can't find my error. I'm trying to use the &lt;c&gt;m&lt;/c&gt; modifier to read a large text in line by line. I want to use the inline form, &lt;c&gt;(?m)&lt;/c&gt; rather than following the regex slash with m.

&lt;c&gt;
#!/usr/bin/perl
use strict;
use warnings;
use feature 'say';

my $s = &lt;&lt;'EOF';
int t; //variable t
t-&gt;a=0;  //t-&gt;a does;; something
printf("\nEnter the Employee ID : ");
scanf("%d", ptrx-&gt;eid);  //employee id ptrx-&gt;eid
printf("\nEnter the Employee Name : ");
scanf("%s", ptr-&gt;name);
return 0;
EOF

for my $line (split/(?m)\n(?=$)/, $s) {
	say '&lt;' . $line . '&gt;';	
}
&lt;/c&gt;

I wonder if anyone can see why it isn't working. The output is:
&lt;p&gt;

&lt;p&gt;
&lt;c&gt;
C:\Old_Data\perlp&gt;perl test3.pl
&lt;int t; //variable t
t-&gt;a=0;  //t-&gt;a does;; something
printf("\nEnter the Employee ID : ");
scanf("%d", ptrx-&gt;eid);  //employee id ptrx-&gt;eid
printf("\nEnter the Employee Name : ");
scanf("%s", ptr-&gt;name);
return 0;&gt;
&lt;/c&gt;

It isn't reading 'line by line'.

Thanks for any help you might offer!

&lt;p&gt;
&lt;b&gt;Update&lt;/b&gt;: It works ok without the extra specifiers if i just do &lt;c&gt;for my $line (split/\n/, $s)&lt;/c&gt;&lt;p&gt;
&lt;p&gt;But wondering why.</doctext>
<thread id="11117487">
<node id="11117491"/>
<node id="11117493"><node id="11117504"/>
</node>
<node id="11117512"/>
</thread></node>
