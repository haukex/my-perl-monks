<?xml version="1.0" encoding="UTF-8"?>
<node id="11145607" title="Testing legacy CGI scripts using the &quot;main() unless caller()&quot; technique" created="2022-07-20 15:58:37" updated="2022-07-20 15:58:37" _doctext_mode="lenient"><type id="115">perlquestion</type>
<author id="200239">davebaker</author>
<doctext field_name="doctext">&lt;p&gt;After reading the excellent &lt;i&gt;&lt;a href="https://www.amazon.com/Perl-Testing-Developers-Ian-Langworth/dp/0596100922"&gt;Perl Testing: A Developer's Notebook&lt;/a&gt;&lt;/i&gt; by Ian Langworth and chromatic, I decided to implement a strategy described therein (pp. 163-166), to do some first-time testing on the many subroutines that are in a slew of cgi scripts (some of which are many hundreds of lines long). Most of them have many subroutines. One approach would be to put some or all of the subroutines into new modules, which makes for much easier testing of those subroutines. But the other strategy is to wrap the operational guts of the cgi script in a new "&lt;code&gt;sub main() { # guts here }&lt;/code&gt;" that stops just before the subroutines are defined (which are always at the end of my cgi scripts). Then a testing script using Test::More can do a
&lt;p&gt;&lt;code&gt;   require_ok('path-to-cgi-script.cgi');&lt;/code&gt;&lt;/p&gt;
... followed on the next line by a call to one of its subroutines, e.g.,
&lt;p&gt;&lt;code&gt;   ok( add_two_numbers(2,3) == 5, "2 plus 3 yields 5");&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;... even though "add_two_numbers" isn't being exported by a module and is just a subroutine defined somewhere in the cgi script. Because the guts are embedded in a new "&lt;code&gt;main()&lt;/code&gt;" subroutine, they don't actually get run (e.g., it doesn't generate the usual web page to STDOUT) when the testing script is run despite a &lt;code&gt;require_ok()&lt;/code&gt; in the testing script, due to my adding a magic line to the top of the cgi script, as prescribed in the Perl Testing book:&lt;/p&gt;
&lt;code&gt;
   main() unless caller();
&lt;/code&gt;
&lt;p&gt;&lt;a href="https://perldoc.perl.org/functions/caller"&gt;&lt;code&gt;caller()&lt;/code&gt;&lt;/a&gt; returns the caller's package name when invoked in scalar context. There always will be a caller when the testing script does a "&lt;code&gt;require_ok('path-to-cgi-script.cgi');&lt;/code&gt;" because, of course, the cgi script is being called by the testing script. Unless the testing script has done something fancy in the way of giving itself a package name, &lt;code&gt;caller()&lt;/code&gt; returns the string 'main' in scalar context. In boolean context, it returns "true" (if the script is being called).&lt;/p&gt;
&lt;p&gt;Now here's the interesting thing -- the book says it's necessary to have the script being tested (a cgi script, in my example) end with the statement "1;" so that the &lt;code&gt;require_ok()&lt;/code&gt; in the testing script will always work (unless, of course, &lt;code&gt;require_ok&lt;/code&gt; can't find the file or encounters a compilation error in the script). (Actually, the example in the book uses &lt;code&gt;require()&lt;/code&gt; rather than &lt;code&gt;require_ok()&lt;/code&gt;, but it's the same idea, that the script being called needs a "1;" at the end.)&lt;/p&gt;
&lt;p&gt;The first cgi script I've modified for testing does NOT in fact have a "1;" at the end, and does not have any other reason to be returning "true" as the value of its last expression. It's just the guts (now embedded in &lt;code&gt;main()&lt;/code&gt;) and a bunch of subroutines. But &lt;code&gt;require_ok()&lt;/code&gt; passes! So what's the deal? The hunt began!&lt;/p&gt;
&lt;p&gt;To make a long story short, it seems to me that, whenever &lt;code&gt;require_ok()&lt;/code&gt; is run in a testing script, there is by necessity a caller from the perspective of the cgi script being tested, and what happens when the "&lt;code&gt;main() unless caller();&lt;/code&gt;" line is encountered is that the "&lt;code&gt;caller()&lt;/code&gt;" part/expression of the "&lt;code&gt;main() unless caller();&lt;/code&gt;" statement is evaluated to "true", which turns out to be the value of the last expression evaluated by require_ok() -- the remaining lines of the tested script are just more subroutines that aren't being invoked. So now I think I know why &lt;code&gt;require_ok()&lt;/code&gt; always gets a "true" value even without any final "1;", if a "&lt;code&gt;main() unless caller();&lt;/code&gt;" statement is used at the "top" of the script being tested.&lt;/p&gt;
&lt;p&gt;Did I find a "mistake" in the wonderful Perl Testing book?&lt;/p&gt;
&lt;p&gt;I know that adding a "1;" to the end of the script is no big deal, but it rattled me that the book was so definite in this regard while my results differed.&lt;/p&gt;
&lt;p&gt;And, to make it more interesting (but consistent with my conclusion), what happens if the first line is written with this syntax instead?&lt;/p&gt;
&lt;code&gt;
   main() if (! caller());
&lt;/code&gt;
&lt;p&gt;Answer: the &lt;code&gt;require_ok()&lt;/code&gt; in the test script FAILS this time, unless there's a "1;" at the end of the script being tested, despite the logic in the foregoing alternative first line being identical to the logic of the "&lt;code&gt;main() unless caller();&lt;/code&gt;" statement. The reason, I think: the boolean test of "&lt;code&gt;(! caller())&lt;/code&gt;" will return "false" any time there is a caller (i.e., whenever the script is being tested). So &lt;code&gt;require_ok()&lt;/code&gt; gets no "true" love from that line. Then it continues down the tested script slurping up the lines and looking for an expression that returns true, but when it finds no such expression after passing over the last of the subroutines (none of which are invoked), it causes the &lt;code&gt;require_ok()&lt;/code&gt; test to fail -- there was no "true" value returned from the file, which is required (ahem) for success by &lt;code&gt;require_ok()&lt;/code&gt;.&lt;/p&gt;

&lt;p&gt;(Edited in an effort to improve clarity and brevity.)&lt;/p&gt;</doctext>
<thread id="11145607">
<node id="11145608"><node id="11145611"/>
</node>
<node id="11145610"/>
<node id="11145613"/>
</thread></node>
