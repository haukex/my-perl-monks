<?xml version="1.0" encoding="UTF-8"?>
<node id="1092477" title="Using die() in methods" created="2014-07-06 12:37:02" updated="2014-07-06 12:37:02" _doctext_mode="lenient"><type id="115">perlquestion</type>
<author id="1087432">v_melnik</author>
<doctext field_name="doctext">&lt;p&gt;I have the error attribute for every class for explaining to the caller what's gone wrong. And the caller should do:&lt;/p&gt;
&lt;code&gt;my $result = $object-&gt;method();
unless(defined($result)) {
    $log-&gt;error("Something's gone wrong when called $object-&gt;method(): " . $object-&gt;error_message);
    # ...doing something with that...
}&lt;/code&gt;
&lt;p&gt;When I need to return undef as a result (for example, if the method hasn't found the result - not because of an error occuried), I have to do something like that:&lt;/p&gt;
&lt;code&gt;my $result = $object-&gt;method();
if($object-&gt;has_error) {
    $log-&gt;error("Something's gone wrong when called $object-&gt;method(): " . $object-&gt;error_message);
    # ...doing something with that...
}
# ...doing something else, understanding that the method just doesn't have anything to pass to us...&lt;/code&gt;
&lt;p&gt;Are there people who prefer to pre-suppose all their methods are dying every time when something goes wrong, so all methods should be called through eval{}/try{}/...?&lt;/p&gt;
&lt;p&gt;Sometimes I'm thinking about it as about a better way. For example it would allow me to return undef when nothing wrong have happened, so I'd prefer to die() with some explaination that would be accessible as the $@ variable.&lt;/p&gt;
&lt;p&gt;So I consider about something that would look like that:&lt;/p&gt;
&lt;code&gt;my $result = eval { $object-&gt;method() };
if($@) {
    $log-&gt;error("Something's gone wrong when called $object-&gt;method(): $@");
    # ...doing something with that...
}
# ...doing something else, understanding that the method just doesn't have anything to pass to us...&lt;/code&gt;
&lt;p&gt;Maybe it would be better to try some of TryCatch mechanisms from CPAN.&lt;/p&gt;
&lt;p&gt;Anyway, using die() from the method really looks a bit odd. Who really does that? Have you been always doing it that way or started to do it that way some time ago?&lt;/p&gt;
&lt;p&gt;Thank you!&lt;/p&gt;
&lt;!-- Node text goes above. Div tags should contain sig only --&gt;
&lt;div class="pmsig"&gt;&lt;div class="pmsig-1087432"&gt;
V.Melnik
&lt;/div&gt;&lt;/div&gt;
</doctext>
<thread id="1092477">
<node id="1092478"/>
<node id="1092483"/>
<node id="1092485"><node id="1092498"/>
</node>
<node id="1092501"/>
<node id="1092559"/>
<node id="1092576"><node id="1092617"><node id="1092662"/>
<node id="1092979"/>
</node>
</node>
</thread></node>
