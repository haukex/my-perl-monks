<?xml version="1.0" encoding="UTF-8"?>
<node id="11100317" title="Using a factory class to return objects of the same class" created="2019-05-21 10:35:49" updated="2019-05-21 10:35:49" _doctext_mode="lenient"><type id="115">perlquestion</type>
<author id="1010915">Amblikai</author>
<doctext field_name="doctext">&lt;p&gt;Hi Monks!&lt;/p&gt;
&lt;p&gt;I have a question which might be considered a "stylistic" issue but i thought i would get some insight.&lt;/p&gt;

&lt;p&gt;Essentially i have a class which requires some configuration (read an XML file to decide how to handle incoming data)&lt;/p&gt;
&lt;p&gt;Lots of objects are then created of that class using a data structure which can be fairly arbitrary&lt;/p&gt;

&lt;p&gt;So basically, i'm musing over the problem of seperating the incoming data with configuration options. Something like&lt;/p&gt;
&lt;code&gt;
my $obj=class-&gt;new({config_file =&gt; "file.xml", data =&gt; \%some_yummy_data});
&lt;/code&gt;

&lt;p&gt;Which doesn't seem very elegant to me. So i was thinking of handling it slightly differently:&lt;/p&gt;
&lt;code&gt;
my $factory=class-&gt;new({config_file =&gt; "file.xml"});
my $obj=$factory-&gt;give_me_my_object(\%data);
&lt;/code&gt;

&lt;p&gt;But then THAT seems weird since its essentially a factory returning objects of it's own class. And on top of that, it raises questions like what would happen if i tried to access "factory only" methods through the produced objects etc.&lt;/p&gt;

&lt;p&gt;Any thoughts? Am i over complicating this?&lt;/p&gt;

&lt;p&gt;Thanks as always for any help!&lt;/p&gt;

</doctext>
<thread id="11100317">
<node id="11100325"><node id="11100330"><node id="11100334"/>
<node id="11100338"/>
</node>
</node>
<node id="11100326"><node id="11100333"><node id="11100341"/>
</node>
</node>
</thread></node>
