<?xml version="1.0" encoding="UTF-8"?>
<node id="1233097" title="Capture user input AND read after it has been modified" created="2019-04-28 20:54:00" updated="2019-04-28 20:54:00" _doctext_mode="lenient"><type id="115">perlquestion</type>
<author id="836704">Allasso</author>
<doctext field_name="doctext">I am working on a small editor that runs inside a terminal window. I am currently capturing user input using the following construct:

&lt;code&gt;
use IO::Handle;
my $stdin = new IO::Handle;
   $stdin-&gt;fdopen( fileno( STDIN ), "r" ) || die "Cannot open STDIN";

while ( my $char = $stdin-&gt;getc() ) {
  # ... etc etc
&lt;/code&gt;

The capturing is to deal with such things as arrow keys so the user (me) can navigate and more easily edit text before submitting.

I am wondering if there is a way, AFTER the user has done their editing, and submits, can the text that is displayed in the terminal be read in its updated form from the terminal?

This would save me having to track every input event to construct the final string.

Thanks!
</doctext>
<thread id="1233097">
<node id="1233098"><node id="1233099"><node id="1233102"><node id="1233107"/>
</node>
</node>
</node>
</thread></node>
