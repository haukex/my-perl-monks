<?xml version="1.0" encoding="UTF-8"?>
<node id="1180917" title="ipc::run3 portability" created="2017-02-02 21:48:47" updated="2017-02-02 21:48:47" _doctext_mode="lenient"><type id="115">perlquestion</type>
<author id="943021">murrayn</author>
<doctext field_name="doctext">&lt;p&gt;I have a Perl script which should run on Windows, Linux or proprietary Unix systems (subject to the proprietary OS vendors providing a moderately current Perl environment!).
&lt;/p&gt;
&lt;p&gt;I am building a command ARRAY comprising the command name followed by a series of -keyword value pairs:
&lt;/p&gt;
&lt;p&gt;&lt;code&gt;@cmd = (@cmd, "-key1", "value", "-key2", "value2");&lt;/code&gt;&lt;/p&gt;

&lt;p&gt;This process works happily when I run it on Windows (10, Server 2012) and on Linux (CentOS 7) UNLESS one of the values is blank (a literal space character).  If I code &lt;/p&gt;
&lt;p&gt;&lt;code&gt;@cmd = (@cmd, "-key1", " ", "-key2", "value2");&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;Windows handles this correctly and passes the space as the value of key1 but Linux attempts to pass "-key2" as the value of key1 and the invoked command suffers fatal conniptions!&lt;/p&gt;
&lt;p&gt;If I place single or double quotes around the space character, both platforms object to receiving "' '" since the application expects either an X or a space (without enclosing quotation marks).&lt;/p&gt;
&lt;p&gt;When I copy the failing command line to the Linux shell prompt it fails unless I manually insert a pair of quotes around the space character.&lt;/p&gt;
&lt;p&gt;Can anybody advise on how to pass an unquoted space character via ipc::run3 please?&lt;/p&gt;</doctext>
<thread id="1180917">
<node id="1180928"/>
</thread></node>
