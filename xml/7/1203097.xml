<?xml version="1.0" encoding="UTF-8"?>
<node id="1203097" title="&quot;uninitialized value in subroutine exit&quot; warning from CGI.pm" created="2017-11-09 21:53:46" updated="2017-11-09 21:53:46" _doctext_mode="lenient"><type id="115">perlquestion</type>
<author id="1203093">dwmcewan</author>
<doctext field_name="doctext">&lt;p&gt;Hi&lt;/p&gt;

&lt;p&gt;I noticed some "uninitialized value in subroutine exit at /usr/pkg/lib/perl5/vendor_perl/5.26.0/CGI.pm line 472" warnings in our web server logs.&lt;/p&gt;

&lt;p&gt;This was with CGI.pm version 4.36 running on "perl 5, version 26, subversion 0 (v5.26.0) built for x86_64-linux-thread-multi"&lt;/p&gt;

&lt;p&gt;This warning was being generated by the following code in CGI.pm&lt;/p&gt;

&lt;code&gt;
sub self_or_default {
    return @_ if defined($_[0]) &amp;&amp; (!ref($_[0])) &amp;&amp;($_[0] eq 'CGI');
    unless (defined($_[0]) &amp;&amp; 
            (ref($_[0]) eq 'CGI' || UNIVERSAL::isa($_[0],'CGI')) # slightly optimized for common case
            ) {
        $Q = $CGI::DefaultClass-&gt;new unless defined($Q);
        unshift(@_,$Q);
    }
    return wantarray ? @_ : $Q;     # &lt;---- Line 472 of CGI.pm v4.36
}
&lt;/code&gt;

&lt;p&gt;I managed to track this down to some calls our code was making that were essentially:&lt;/p&gt;

&lt;code&gt;
   print $cgi-&gt;textfield(-name=&gt;"end_date",
                         -default=&gt;substr($pa-&gt;get_end_date(),0,10));
&lt;/code&gt;

&lt;p&gt;The $pa-&gt;get_end_date() call is part of a locally developed system that essentially queries a MySQL database.  In the cases where the warning was appearing, the returned date was undefined (it was NULL in the DB)&lt;/p&gt;

&lt;p&gt;Now, in retrospect I realise that this code was buggy and it has now been rewritten to handle undef dates.  But what I don't understand, and was hoping to have explained here, is why the warning was coming out from somewhere deep within CGI.pm, rather than from the call to substr with an undefined first parameter&lt;/p&gt;

&lt;p&gt;If I rewrite the above code to do the substr before the call to $cgi-textfield() (without also protecting it from an undef return value) I get the expected "Use of uninitialized value in substr at ..." warning.&lt;/p&gt;

&lt;p&gt;I'd guess that its something to do with the differing contexts in which the substr is evaluated in, but a more definitive explanation would be greatly appreciated&lt;/p&gt;

&lt;p&gt;Thanks,&lt;/p&gt;

&lt;p&gt;Duncan&lt;/p&gt;
</doctext>
<thread id="1203097">
<node id="1203098"><node id="1203106"/>
</node>
<node id="1203099"/>
<node id="1203113"><node id="1203123"/>
<node id="1203124"><node id="1203125"/>
</node>
</node>
</thread></node>
