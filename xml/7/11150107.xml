<?xml version="1.0" encoding="UTF-8"?>
<node id="11150107" title="Why does system(&quot;mv $x $y&quot;) not work when it works at the command line?" created="2023-02-02 20:18:12" updated="2023-02-02 20:18:12" _doctext_mode="lenient"><type id="115">perlquestion</type>
<author id="785151">Cody Fendant</author>
<doctext field_name="doctext">&lt;p&gt;This is a simple script which sorts TV show episodes into the right folders, it comes down to this basic example:&lt;/p&gt;

&lt;code&gt;
### I run this from the command line:
/usr/bin/perl mover.pl The.Rookie.S05E14.720p.HEVC.x265-MeGusta[eztv.re].mkv
&lt;/code&gt;

&lt;p&gt;The script dies here:&lt;/p&gt;

&lt;code&gt;
    unless ( -e $ARGV[0] ) {
        die("$ARGV[0] doesn't exist");
    }
&lt;/code&gt;

&lt;p&gt;But:&lt;/p&gt;

&lt;ul&gt;
&lt;li&gt;The file does exist: it shows up if I do &lt;code&gt;ls The.Rookie.S05E14.720p.HEVC.x265-MeGusta[eztv.re].mkv&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;The script is operating in the right folder, I double checked
&lt;/ul&gt;

&lt;p&gt;If I skip the file-exists test and go right to moving the file to the correct folder, I can do this: &lt;/p&gt;

&lt;code&gt;
    say "move command: 'mv $ARGV[0] ./r/'";
    system("mv $ARGV[0] ./r/");
&lt;/code&gt;

&lt;p&gt;The &lt;code&gt;mv&lt;/code&gt; command in the &lt;code&gt;system()&lt;/code&gt; call fails with "No such file or directory", but the exact same command, if I copy and paste it from the perl script's output and hit enter, works just fine.
&lt;/p&gt;
&lt;p&gt;Is it the brackets in the filename? I tried escaping them and it make no difference. The only wrinkle I can think of is that this is happening on MacOS, but the files in question are on a share from a Linux NAS, could that be causing issues?&lt;/p&gt;</doctext>
<thread id="11150107">
<node id="11150110"/>
<node id="11150111"><node id="11150124"/>
</node>
<node id="11150114"/>
<node id="11150115"/>
<node id="11150121"/>
<node id="11150141"/>
</thread></node>
