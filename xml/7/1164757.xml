<?xml version="1.0" encoding="UTF-8"?>
<node id="1164757" title="Hash key &amp; foreach" created="2016-06-02 08:47:44" updated="2016-06-02 08:47:44" _doctext_mode="lenient"><type id="115">perlquestion</type>
<author id="961">Anonymous Monk</author>
<doctext field_name="doctext">&lt;code&gt;
my %interfaces =  (
    R1R0  =&gt; 1,
    R1R6  =&gt; 2,
    R2R0  =&gt; 3,
    R2R6  =&gt; 1,
    R3R61 =&gt; 2,
    R3R62 =&gt; 3,
);

push (@routers, $r[1],$r[2],$r[3]);

foreach my $rh (@routers) {
		
  $rh-&gt;config(" deactivate  $interfaces{$R1R0}");
}
&lt;/code&gt;

&lt;p&gt; In the above code, there are 6 interfaces, 2 from each router, (R1, R2, R3). I want to deactivate each interface as defined in hash from all the routers. For that, am doing a foreach (to get the router handles separately),  and execute the action. The problem with this is, when foreach executes the first router, say $r[0], only the first interface (defined in hash/key as R1R0) is executed. I want to execute  both (first and second hash/key pair for R1), when foreach is executed for $r[0], similarly for R2 and R3.
I found the only way is defined a seperate hash  for all the three routers as below &lt;/p&gt;

&lt;code&gt;
%interfaces_R1 = (
R1R0 =&gt;
R1R6 =&gt;
);

%interfaces_R2 =(
...

&lt;/code&gt;

&lt;p&gt; Is there a better way to do it &lt;/p&gt;</doctext>
<reputation field_name="reputation">5</reputation>
<thread id="1164757">
<node id="1164760"><node id="1164834"/>
</node>
</thread></node>
