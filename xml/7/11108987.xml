<?xml version="1.0" encoding="UTF-8"?>
<node id="11108987" title="Re: Add optional modules to TEST_REQUIRES? (updated)" created="2019-11-21 10:03:29" updated="2019-11-21 10:03:29" _doctext_mode="strict"><type id="11">note</type>
<author id="830549">haukex</author>
<doctext field_name="doctext">&lt;p&gt;Personally, I am no longer a fan of optional dependencies*, because they tend to complicate things in exactly the way you describe. I would suggest thinking about alternatives, such as just deciding to either declare the module as a dependency, or removing it, perhaps a replacement is possible with core modules - for example, if I'm only using [mod://Path::Class] in one or two places, I might as well use [doc://File::Spec] instead. Or, if this optional functionality is sufficiently large, consider splitting it out into a separate release in the same namespace.&lt;/p&gt;
&lt;p&gt;Having said that, of course there are exceptions, for example if there are different backends that are OS-dependent. I have a module on CPAN myself with optional dependencies. My suggestion would be: Don't include the optional dependencies in &lt;c&gt;TEST_REQUIRES&lt;/c&gt;, use &lt;c&gt;META_MERGE =&gt; { prereqs =&gt; { runtime =&gt; { recommends =&gt; { ... } } } }&lt;/c&gt; ([https://metacpan.org/source/HAUKEX/Shell-Tools-0.04/Makefile.PL|example]). Then &lt;c&gt;skip&lt;/c&gt; the corresponding tests if the modules aren't installed, but when you do your author tests before release, verify that the modules are installed and the tests pass. (There are of course other possible approaches, this is just one suggestion.)&lt;/p&gt;
&lt;p&gt;* &lt;b&gt;Update:&lt;/b&gt; Just to clarify: I mean when writing new modules, I wasn't trying to hate on modules that already have them &lt;c&gt;:-)&lt;/c&gt; And even when writing new modules, as I said, there are exceptions. But optional dependencies add a bunch complexity, especially in testing: it really would be best to run the entire test suite once when the optional dependency isn't installed, and once when it is, and AFAIK there's no way to do that on CPAN Testers, which means you have to rely on your own local testing, on multiple platforms. And if there's more than one dependency, the number of times one &lt;i&gt;should&lt;/i&gt; run the test suite grows quickly.&lt;/p&gt;</doctext>
<root_node field_name="root_node">11108983</root_node>
<parent_node field_name="parent_node">11108983</parent_node>
<thread id="11108983">
<node id="11108984"/>
<node id="11108987"/>
<node id="11108990"/>
<node id="11109015"/>
<node id="11109024"><node id="11109028"/>
</node>
<node id="11109056"/>
<node id="11109175"><node id="11109202"/>
</node>
</thread></node>
