<?xml version="1.0" encoding="UTF-8"?>
<node id="1176227" title="Re^5: Error Handling in Selenium" created="2016-11-21 01:44:06" updated="2016-11-21 01:44:06" _doctext_mode="lenient"><type id="11">note</type>
<author id="1169560">9mohit2</author>
<doctext field_name="doctext">&lt;P&gt;Hi Everyone, As you all suggested here is a small snippet &lt;/P&gt;
&lt;code&gt;
use strict;
use warnings FATAL =&gt; 'all';
use Selenium::Remote::Driver;
use Selenium::Chrome;
use Selenium::Remote::WebElement;


my $driver = Selenium::Chrome-&gt;new(binary=&gt;"D:\\chromedriver_win32\\chromedriver.exe");
$driver-&gt;maximize_window();
$driver-&gt;set_implicit_wait_timeout(1000);
$driver-&gt;get('http://google.com');
$driver-&gt;pause(2000);
$driver-&gt;find_element_by_xpath(".//*[\@id='lst-ib']")-&gt;send_keys('perlmonks');
$driver-&gt;pause(5000);

print "1";

#The below line is just for testing that what happens if element is not present.And it even gives no warning or anything.
$driver-&gt;find_element_by_xpath("abc");
$driver-&gt;pause(5000);
print "2";

#The below line is just for testing that what happens if element is not present. This gives a warning as stated in below para.
$driver-&gt;find_element_by_xpath("def")-&gt;click;
$driver-&gt;pause(5000);
$driver-&gt;find_element_by_xpath(".//*[\@id='sblsbb']/button")-&gt;click;
$driver-&gt;pause(5000);
$driver-&gt;shutdown_binary;
&lt;/code&gt; 
&lt;P&gt;
So in the above snippet which is for searching something in Google. The lines &lt;code&gt;$driver-&gt;find_element_by_xpath("abc"); and $driver-&gt;find_element_by_xpath("def")-&gt;click;&lt;/code&gt; are just added to check what happens for elements not present.&lt;br&gt; The command prompt shows the error &lt;i&gt;Use of uninitialized value in substitution iterator at C:/Perl/site/lib/Selenium/Remote/Commands.pm line 437.&lt;/i&gt;. That too is only for the one on which click is also defined. How can I catch these to call another sub for sending notification mails. Also please let me know how can I catch error for the line with no click element as it is not even giving any warning in the Prompt. &lt;br&gt; One more thing the Output for above throws the warning first then 1 and 2. Is it always that warnings are thrown first not depending on the line by line execution. &lt;/P&gt;</doctext>
<root_node field_name="root_node">1173344</root_node>
<parent_node field_name="parent_node">1176079</parent_node>
<thread id="1173344">
<node id="1173358"><node id="1176040"><node id="1176051"><node id="1176079"><node id="1176080"/>
<node id="1176091"/>
<node id="1176221"/>
<node id="1176227"><node id="1176263"><node id="1176308"><node id="1176311"/>
</node>
</node>
</node>
</node>
</node>
</node>
</node>
</thread></node>
