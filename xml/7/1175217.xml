<?xml version="1.0" encoding="UTF-8"?>
<node id="1175217" title="splitting csv file and saving data" created="2016-11-03 05:48:34" updated="2016-11-03 05:48:34" _doctext_mode="lenient"><type id="115">perlquestion</type>
<author id="1151301">Ganesh Bharadwaj1</author>
<doctext field_name="doctext">Hi Monks,

I have a query. I have an input CSV file that looks like this.

&lt;code&gt; NPLUS,32,0,
NW,41,0,1
NWER,51,"0,1,2",12 &lt;/code&gt;

This is the format

&lt;code&gt;Layer name, layer number, data type, text type&lt;/code&gt;. some times there are more than 1 data type, in which case they are grouped using "". 
&lt;p&gt;For the first line NPLUS is the layer name,32 is layer number and 0 is the data type. There is no text type for this line&lt;/p&gt;
&lt;p&gt;For the second line NW is the layer name,41 is layer number and 0 is the data type and 1 is the text type,&lt;/p&gt;
&lt;p&gt;For the last line NWER is the layer name, 51 is the layer number, 0,1,2 is the data type and 12 is the text type. &lt;/p&gt;

I want to check the fields and print the following. 

&lt;code&gt;NPLUS layernumber 32 datatype 0,
NW layernumber 41 datatype 0 text 1,
NWER layernumber 51 datatype 0 datatype 1 datatype 2 text 12;&lt;/code&gt;

I have written a sample code, but I am not able to figure out how to split based on two delimiters and then get the required result. the last line also should have a semicolon, where as other lines should have a coma at the end only. Here is the sample code.

&lt;code&gt;open(my $in, '&lt;', 'Text_File.csv')
    or die "Cannot open Text_File.csv: $!";

open(my $out, '&gt;', 'mask.spec')
    or die "Cannot open mask.spec: $!";

while (my $line = &lt;$in&gt;) { 
#my @fields = split (/(?&lt;="),(?=")/, $line);
    my @fields = split(/,/, $line); 
    if (@fields == 4) { 
        printf {$out} "%s layernum %s datatype %s,%s", @fields; 
    } 
    elsif (@fields == 5) { 
        printf {$out} "%s layernum %s datatype %s text %s,%s", @fields;
    } 
}&lt;/code&gt;

Any help would be greatly appreciated.
  </doctext>
<thread id="1175217">
<node id="1175218"><node id="1175221"><node id="1175222"><node id="1175224"><node id="1175225"/>
</node>
</node>
<node id="1175223"/>
<node id="1175262"><node id="1175265"/>
</node>
</node>
</node>
<node id="1175220"/>
<node id="1175267"/>
</thread></node>
