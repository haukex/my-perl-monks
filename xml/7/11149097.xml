<?xml version="1.0" encoding="UTF-8"?>
<node id="11149097" title="Splitting a string on commas except when inside quotes" created="2022-12-26 19:58:21" updated="2022-12-26 19:58:21" _doctext_mode="lenient"><type id="115">perlquestion</type>
<author id="319942">anadem</author>
<doctext field_name="doctext">My trusty code splits a string on commas:
&lt;code&gt;
( $fDate, $fType, $fDetails, $fRef, $fCurrency, 
  $fAmount, $fPaidOut, $fFees, $fTax )
      = split( /,/, $line );
&lt;/code&gt;
Unfortunately the provider of the data has newly added a comma in the fDetails field and delimited the field by double-quotes:
&lt;code&gt;
11/21/2022,Payment,"Transfer to Smith, account 2",,USD,,123.60,,,
&lt;/code&gt;
&lt;p&gt;where previously there was neither a comma following Smith nor quotes around the details; split now erroneously gives: &lt;/p&gt;
&lt;code&gt;$fDetails = "Transfer to Smith
$fRef =  Savings account"&lt;/code&gt;
&lt;p&gt;with subsequent fields also getting the wrong data of course.&lt;/p&gt;

What's a clean way to handle this, such that the comma within the double-quotes is ignored by &lt;i&gt;split&lt;/i&gt;? (I don't mind losing the comma within the details if that's easiest.) Kindly excuse my ignorance here, I retired some years ago and my brain has rusted.&lt;p&gt;
Here's a runnable fragment: &lt;code&gt;
$line = 
  '11/21/2022,Payment,"Transfer to Smith, account 2",,USD,,123.60,';
print "  $line\n";

( $fDate, $fType, $fDetails, $fRef, $fCurrency,
  $fAmount, $fPaidOut, $fFees )
      = split( /,/, $line );

printf("\n  Date: %s;  Type: %s;  \n"
  ."  Details: %s\n  Reference: %s\n"
  ."  Currency: %s;  Amount: %s;  PaidOut: %s; Fees: %s;\n",
  $fDate, $fType, $fDetails,
  $fRef, $fCurrency, $fAmount, $fPaidOut, $fFees );


&lt;/code&gt;
</doctext>
<thread id="11149097">
<node id="11149099"><node id="11149293"><node id="11149307"/>
</node>
</node>
<node id="11149104"/>
<node id="11149113"><node id="11149294"/>
</node>
</thread></node>
