<?xml version="1.0" encoding="UTF-8"?>
<node id="1195297" title="references - proper use of infix (arrow) and deprecated hash references" created="2017-07-18 04:23:26" updated="2017-07-18 04:23:26" _doctext_mode="lenient"><type id="115">perlquestion</type>
<author id="961">Anonymous Monk</author>
<doctext field_name="doctext">&lt;a&gt;Hello Monks,&lt;/a&gt;
&lt;p&gt;
I just learned how to use References, but I am still confused.
Currently, I try to write a script that reads a txt file that has 5 columns, each separated by some whitespace. Each Row corresponds to a gene and the columns are the 1. Gene identifyer 2. sequence 3. sequence length 4. GC content 5. CAI. It's not important what that is.&lt;/p&gt;

&lt;p&gt; I want to store the individual attributes into a hash with the identifyer as keys, and a reference to an array containing the remaining attributes as the values. When I try to print the hash, perl complains about my infix operator, except when I write the % symbol instead of the $ symbol. But I learned it the other way. In addition, Perl complains that hash references are &lt;i&gt;deprecated&lt;/i&gt;. I'm confused. &lt;/p&gt;
&lt;a&gt; Here's my code:&lt;/a&gt;

&lt;code&gt;
my $file = 'data.txt';
open READ, $file || die "Cannot open $file: $!\n";
my %hash;
while (&lt;READ&gt;){
	chomp;
	my ($id, $seq, $length, $gc, $cai) = split /\s+/, $_;
	$hash{$id} = [ $seq, $length, $gc, $cai ];
}
close (READ);

foreach (keys %hash){
	print $_, "\t", %hash-&gt;{$_}[1], "\t", %hash-&gt;{$_}[2], "\n";
}
&lt;/code&gt;

&lt;p&gt; this code only leads to the warning: Using a hash as a reference is deprecated!
When I change the last line to &lt;c&gt; print $_, "\t", $hash-&gt;{$_}[1], ... &lt;/c&gt; it also complains: &lt;i&gt;Global symbol $hash requires explicit ... &lt;/i&gt;&lt;/p&gt;
&lt;a&gt; Can anyone clarify the situation? &lt;/a&gt;
&lt;a&gt; Thanks in advance! &lt;/a&gt;</doctext>
<reputation field_name="reputation">10</reputation>
<thread id="1195297">
<node id="1195299"><node id="1195308"/>
</node>
<node id="1195302"><node id="1195307"/>
<node id="1195351"><node id="1195497"/>
</node>
</node>
<node id="1195314"/>
<node id="1195338"/>
</thread></node>
