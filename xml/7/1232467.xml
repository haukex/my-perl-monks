<?xml version="1.0" encoding="UTF-8"?>
<node id="1232467" title="Re: perlcritic on Linux" created="2019-04-11 15:10:14" updated="2019-04-11 15:10:14" _doctext_mode="strict"><type id="11">note</type>
<author id="830549">haukex</author>
<doctext field_name="doctext">&lt;p&gt;Welcome to Perl and the Monastery, [ymakkapati].&lt;/p&gt;
&lt;p&gt;Based on those paths, it sounds like you're trying to install a module into the system Perl using &lt;c&gt;cpan&lt;/c&gt;, which is generally not recommended, because it can create conflicts with the packages installed into the system Perl using the package manager, and in case of problems, cleaning this up can become quite difficult. Also, Perl modules should always be installed using their recommended installation procedure, not by copying files into the [doc://@INC] directories.&lt;/p&gt;
&lt;p&gt;Although I am not a CentOS expert ([hippo] is our resident expert, I believe), I think you should be able to install [mod://Perl::Critic] (which includes the &lt;c&gt;perlcritic&lt;/c&gt; command) by installing the RPM package &lt;c&gt;perl-Perl-Critic&lt;/c&gt; using your system's package manager, and I'd also install &lt;c&gt;perl-core&lt;/c&gt; to get all the Perl core modules installed as well. Assuming the RPM's dependencies are configured correctly, you should get all the required dependencies installed automatically as well.&lt;/p&gt;
&lt;p&gt;If you don't want to modify the system Perl, then the recommended alternatives are: You can install a copy of Perl into your home directory using e.g. [https://perlbrew.pl/|perlbrew], into which you can install any CPAN modules you like using [dist://App-cpanminus|&lt;c&gt;cpanm&lt;/c&gt;] (which I'd recommend) or &lt;c&gt;cpan&lt;/c&gt;. The other alternative is [mod://local::lib], which lets you install modules to your home directory, but still allows you to use the system Perl (I believe the RPM package for that is &lt;c&gt;perl-local-lib&lt;/c&gt;).&lt;/p&gt;
&lt;p&gt;By the way, you said &lt;i&gt;"I ... tried to run ./perlcritic"&lt;/i&gt;. That probably won't work, since the &lt;c&gt;./&lt;/c&gt; means "run the executable that is in the current directory", which won't find the &lt;c&gt;perlcritic&lt;/c&gt; installed in your &lt;c&gt;PATH&lt;/c&gt; - you should run the command as simply "&lt;c&gt;perlcritic&lt;/c&gt;" (without the &lt;c&gt;./&lt;/c&gt;).&lt;/p&gt;</doctext>
<root_node field_name="root_node">1232466</root_node>
<parent_node field_name="parent_node">1232466</parent_node>
<thread id="1232466">
<node id="1232467"><node id="1232468"/>
</node>
</thread></node>
