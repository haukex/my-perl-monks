<?xml version="1.0" encoding="UTF-8"?>
<node id="1217467" title="Threaded 5.10.0 + ?PATTERN? = Modification of a read-only value attempted" created="2018-06-26 17:14:14" updated="2018-06-26 17:14:14" _doctext_mode="lenient"><type id="115">perlquestion</type>
<author id="832495">choroba</author>
<doctext field_name="doctext">Dear Monks,&lt;P&gt;

as some of you have noticed from my grumbling in the ChatterBox, I'm getting weird CPAN Testers' reports. With [Tux]'s help, I was able to identify the trigger: the problem happens in threaded 5.10.0 only.&lt;P&gt;

The minimal example is here:
&lt;c&gt;
eval ' "x" =~ ?x? '
&lt;/c&gt;&lt;P&gt;

(in fact, &lt;c&gt;eval '??'&lt;/c&gt; is even shorter, but probably harder to decipher, and doesn't show so clearly that it has to match to trigger the exception).&lt;P&gt;

In threaded 5.10.0 (compiled with a few bugs in &lt;c&gt;make test&lt;/c&gt; even with the latest [metamod://Devel::PatchPerl] applied), it throws
&lt;c&gt;
Modification of a read-only value attempted at ...
&lt;/c&gt;&lt;P&gt;

Interestingly, the exception goes away if I add [doc://reset] after the match:
&lt;c&gt;
eval ' "x" =~ ?x?; reset '
&lt;/c&gt;&lt;P&gt;

It's clearly a Perl bug, and it's been fixed, as later versions don't show the same behaviour. Nevertheless, I'd be interested in any explanation why "match-only-once" in stringy eval caused this particular error (normal match or the same code outside of eval don't fail).&lt;P&gt;

&lt;!-- Node text goes above. Div tags should contain sig only --&gt;
&lt;div class="pmsig"&gt;&lt;div class="pmsig-832495"&gt;
&lt;c&gt;($q=q:Sq=~/;[c](.)(.)/;chr(-||-|5+lengthSq)`"S|oS2"`map{chr |+ord
}map{substrSq`S_+|`|}3E|-|`7**2-3:)=~y+S|`+$1,++print+eval$q,q,a,&lt;/c&gt;
&lt;/div&gt;&lt;/div&gt;&lt;!-- Wiki2Monks {"version":1.142} --&gt;</doctext>
<thread id="1217467">
<node id="1217469"/>
</thread></node>
