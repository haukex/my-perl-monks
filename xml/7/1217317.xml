<?xml version="1.0" encoding="UTF-8"?>
<node id="1217317" title="Test:: to match multiple lines against corresponding regular expressions" created="2018-06-24 08:05:10" updated="2018-06-24 08:05:10" _doctext_mode="lenient"><type id="115">perlquestion</type>
<author id="826278">Dallaylaen</author>
<doctext field_name="doctext">&lt;p&gt;Hello dear esteemed monks!&lt;/p&gt;

&lt;p&gt;I'm looking for a way to test multiple lines to match corresponding regular expressions: &lt;/p&gt;

&lt;code&gt;
multi_like \@array, [
    qr/regex1/,
    qr/regex2/,
    ....
], "Test name";
&lt;/code&gt;

&lt;p&gt;Now obviously I can do it with a subtest and like statements, plus one test for length:&lt;/p&gt;

&lt;code&gt;
subtest "All lines are like expected" =&gt; sub {
    like $array[0], qr/regex1/;
    like $array[1], qr/regex2/;
    ....
    is scalar @array, $nnn, "No extra lines";
};
&lt;/code&gt;

&lt;p&gt;And I can do it with one go with [metamod://Test::Deep], although I haven't yet found out how to make it report the full difference and not just the first mismatch.&lt;/p&gt;

&lt;p&gt;But maybe there's a module that does exactly that (matches a list of strings against a list of regexps)?&lt;/p&gt;

&lt;p&gt;Thanks!&lt;/p&gt;</doctext>
<thread id="1217317">
<node id="1217318"/>
<node id="1217327"><node id="1217444"/>
</node>
</thread></node>
