<?xml version="1.0" encoding="UTF-8"?>
<node id="11151807" title="Finding unique elements in inputs" created="2023-04-20 23:45:16" updated="2023-04-20 23:45:16" _doctext_mode="lenient"><type id="115">perlquestion</type>
<author id="961">Anonymous Monk</author>
<doctext field_name="doctext">I want to take a list of inputs and simply print out each element in the order in which they occur.
&lt;p&gt;
This sounds simple, but my code doesn't work (prints a blank line).
&lt;p&gt;
&lt;code&gt;
#!/usr/bin/perl -w
use strict;

my @inputs = ();
my @values = ();

@inputs = @ARGV;

foreach my $elemnt (@inputs)
    {
    if (grep {$_ ne $elemnt} @values)
        { push @values, $elemnt; }
    }

print "@values\n";
&lt;/code&gt;
&lt;p&gt;
Suspecting an initialization problem, I tried an alternate version where I pre-seeded one element in @values, but that printed every input value (not removing duplicates).</doctext>
<reputation field_name="reputation">10</reputation>
<thread id="11151807">
<node id="11151808"/>
<node id="11151809"/>
<node id="11151812"/>
<node id="11151817"/>
<node id="11151823"/>
<node id="11151824"/>
</thread></node>
