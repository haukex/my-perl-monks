<?xml version="1.0" encoding="UTF-8"?>
<node id="1208992" title="Accessing frames with WWW::Mechanize" created="2018-02-12 08:16:10" updated="2018-02-12 08:16:10" _doctext_mode="lenient"><type id="115">perlquestion</type>
<author id="1208988">ermai</author>
<doctext field_name="doctext">I use the WWW::Mechanize module to access a URL with two frames. I enter a word into the first frame and submit it, and then download the data that is returned in the second frame. But somehow it hangs on accessing the second frame, how to link frames in url and manipulate them ?

The URL  https://imgur.com/a/eBmBG

&lt;code&gt;
&lt;html&gt;
.......

&lt;frameset rows="40%,*"&gt;
  &lt;frame name="rtop" src="123.asp" target="rbottom"&gt;
  &lt;frame name="rbottom" src="444.html" scrolling="auto"&gt;
.......
  &lt;/noframes&gt;

&lt;/frameset&gt;
&lt;/html&gt;

&lt;/code&gt;

submit tag in HTML of the end of the first frame when linking it

&lt;code&gt;
&lt;td width=28% align=center&gt;
&lt;input type="submit" name="act" value="xxx" class="cbutton"&gt;
&lt;/code&gt;

My Perl code

&lt;code&gt;
 #!c:\\perl\\bin

 use strict;

 use WWW::Mechanize;

 my $url      = "myurl";
 my $Part     = 'mykey';
 my $outfile1 = "out1.html";

 my $mech = WWW::Mechanize-&gt;new();

 $mech-&gt;get($url);
 $mech-&gt;follow_link(tag =&gt; "frame"); # open first frame in document
 $mech-&gt;field ('' =&gt; $Part);
 $mech-&gt;submit();
 $mech-&gt;follow_link(tag =&gt; "frame"); # open 2nd frame in document, where it hangs

 my $output_page = $mech-&gt;content();

 open(OUTFILE, "&gt;$outfile1");
 print OUTFILE "$output_page";
 close(OUTFILE);
&lt;/code&gt;</doctext>
<thread id="1208992">
<node id="1209008"/>
</thread></node>
