<?xml version="1.0" encoding="UTF-8"?>
<node id="1225852" title="play mp3 stream (music) portably" created="2018-11-15 06:07:58" updated="2018-11-15 06:07:58" _doctext_mode="lenient"><type id="115">perlquestion</type>
<author id="174111">Discipulus</author>
<doctext field_name="doctext">Hello nuns and monks,&lt;P&gt;

As you can see in my [id://1225392] i was able to succesfully read an mp3 stream cutting it at &lt;C&gt;icy-mataint&lt;/C&gt; occurences
and also able to (possibly) skip advertisements.&lt;P&gt;


Now, and was my original goal, I want to reproduce this stream and I found that all modules I investigated need a file while
I have a stream.&lt;P&gt;

If you take a look at my above mentioned code, I just read froma a socket by chunk and, in the &lt;C&gt;read_stream&lt;/C&gt; sub, I get &lt;C&gt;$buf&lt;/C&gt; populated
with just the music (no metadata) and I pass &lt;C&gt;$buf&lt;/C&gt; to the &lt;C&gt;write_stream&lt;/C&gt; sub.&lt;P&gt;

So my original idea was to push all these &lt;C&gt;$buf&lt;/C&gt; into something like a big cache and have some other sub shifting such cache and play each chunk..&lt;P&gt;

Some help on how to implement it? ;=) I'd like a perl solution, portable as much as it is possible (you know I mainly work on win32..): so using external programs as'mpeg123' and similar are not what i'm searching for. Until now I have mumbled the following:&lt;P&gt;


&lt;big&gt;1 - tempfile)&lt;/big&gt;As all modules (and programs too) I investigated, needs to be feed with a file, it will be possible to have a temp file and append to it. It's 
not a optimal solution: how to know how much of the the neverending file was already played? how to rotate it? &lt;P&gt;


&lt;big&gt;2 - reproxing)&lt;/big&gt; Once i have &lt;C&gt;$buf&lt;/C&gt; i can re-stream them using a local server listening on 127.0.0.1  and launch an external program (like vlc or you preferred one) to connect to such stream to play it. This is an overcomplicated solution..&lt;P&gt;


The only module I found that seems able to play music contained in a variable is [METAMOD://SDL::Mixer::Music] that can receive data via [METAMOD://SDL::RWOps] but I was not able to make it to run. Examples in this direction will be appreciated. I suppose SDL has some limitation playing mp3 on windows (missing libraries?) I had success only playing wav files (not varibles with wav inside).&lt;P&gt;

Form the [METAMOD://SDL::Mixer::Music] documentation&lt;P&gt;

&lt;c&gt;
use SDL;
use SDL::Mixer;
use SDL::Mixer::Music;
use SDL::RWOps;
 
 
my $rwops = SDL::RWOps-&gt;new_const_mem( $scalar_holding_music );
my $music = SDL::Mixer::Music::load_MUS_RW( $rwops );
&lt;/c&gt;&lt;P&gt;

Any help and suggestion will be very much appreciated&lt;P&gt;


L*&lt;P&gt;

&lt;div class="pmsig"&gt;&lt;div class="pmsig-174111"&gt;
There are no rules, there are no thumbs..&lt;BR&gt;
Reinvent the wheel, then learn The Wheel; may be one day you reinvent one of THE WHEELS.
&lt;/div&gt;&lt;/div&gt;&lt;!-- Wiki2Monks {"version":1.142} --&gt;</doctext>
<thread id="1225852">
<node id="1225870"/>
<node id="1225895"/>
</thread></node>
