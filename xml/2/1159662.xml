<?xml version="1.0" encoding="UTF-8"?>
<node id="1159662" title="Re^3: print stack of &quot;do&quot; files on error" created="2016-04-05 18:43:44" updated="2016-04-05 18:43:44" _doctext_mode="strict"><type id="11">note</type>
<author id="830549">haukex</author>
<doctext field_name="doctext">&lt;p&gt;Hi [kir],&lt;/p&gt;
&lt;p&gt;Out of curiosity I played around with this a bit more (using [doc://Carp], [mod://Carp::Always], and [mod://Carp::Clan]; I didn't get around to [mod://Devel::SimpleTrace] or [mod://Acme::JavaTrace]), but I discovered that none of these modules gave me enough control, there was always something off about the error messages. So I came to the same conclusion as you, that I'd have to build my own stack trace, and I found [mod://Devel::StackTrace]. Here's what I came up with:&lt;/p&gt;
&lt;c&gt;
package L;
use warnings;
use strict;

use Devel::StackTrace;
our $DYING = 0;

sub include {
	my $file = shift;
	unless (my $return = do $file) {
		my $e;
		if ($@) { $e = "Couldn't parse $file: $@" }
		elsif (!defined $return) { $e = "Couldn't do $file: $!\n" }
		elsif (!$return) { $e = "Couldn't run $file\n" }
		if ($e) {
			# don't add a new stack trace if we're already dying
			die $@ if $DYING++;
			die join '', $e, map {"\t".$_-&gt;as_string."\n"}
				Devel::StackTrace-&gt;new(ignore_package=&gt;__PACKAGE__)-&gt;frames;
		}
	}
}

1;
&lt;/c&gt;
&lt;p&gt;Output:&lt;/p&gt;
&lt;c&gt;
Couldn't parse X.pm: Blammo at X.pm line 9.
	L::include('X.pm') called at B.pm line 8
	L::include('B.pm') called at A.pm line 9
	A::load_b('foo') called at A.pm line 11
	L::include('A.pm') called at test.pl line 8
&lt;/c&gt;
&lt;p&gt;(If you wanted to get a stack trace from inside the failing file, you'd have to install a handler there, too.)&lt;/p&gt;
&lt;blockquote&gt;&lt;i&gt;Because with "do" there is no way to distinguish an error (like file not found) from the script just returning undef (is this right?).&lt;/i&gt;&lt;/blockquote&gt;
&lt;p&gt;The normal Perl convention is that files that are [doc://require]d/[doc://use]d end on a true value (often &lt;c&gt;1;&lt;/c&gt;). If your config files return a true value the same way, then you'll be able to distinguish those cases with the above code.&lt;/p&gt;
&lt;p&gt;Hope this helps,&lt;br&gt;-- Hauke D&lt;/p&gt;</doctext>
<root_node field_name="root_node">1159456</root_node>
<parent_node field_name="parent_node">1159475</parent_node>
<thread id="1159456">
<node id="1159458"/>
<node id="1159464"><node id="1159475"><node id="1159662"/>
</node>
</node>
</thread></node>
