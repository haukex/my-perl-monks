<?xml version="1.0" encoding="UTF-8"?>
<node id="1154672" title="How to ask better questions using Test::More and sample data" created="2016-02-08 15:24:53" updated="2016-02-08 15:24:53" _doctext_mode="lenient"><type id="120">perlmeditation</type>
<author id="104468">neilwatson</author>
<doctext field_name="doctext">&lt;p&gt;I encourage wisdom seekers to present sample data and use [mod://Test::More] in the example code of their question. Let's look at some examples.&lt;/p&gt;

&lt;h3&gt;How do I make the regex match?&lt;/h3&gt;

&lt;code&gt;
#!/usr/bin/perl

use strict;
use warnings;
use Test::More;

my $data  = "Some string here";
my $regex = qr/ fancy regex here /mxis;

like( $data, $regex, "Matching my regex" );

done_testing;
&lt;/code&gt;

&lt;p&gt;Your code fails, but readers can read this code and run it and make changes that will make it pass.&lt;/p&gt;

&lt;h3&gt;Why does my sub return an error?&lt;/h3&gt;

&lt;code&gt;
#!/usr/bin/perl

use strict;
use warnings;
use Test::More;

sub mysub {
   return;
}

ok( mysub(), "Should return true" );

done_testing;
&lt;/code&gt;

&lt;h3&gt;Presenting larger sample data as if you were reading a file line by line.&lt;/h3&gt;

&lt;p&gt;Use __DATA__.&lt;/p&gt;

&lt;code&gt;
#!/usr/bin/perl

use strict;
use warnings;
use Test::More;

my $wanted_matches = 2;
my $actual_matches = 0;
my $regex          = qr/ fancy regex here /mxis;

while ( my $line = &lt;DATA&gt; ) {
   chomp $line;

   if ( $line =~ $regex ){
      $actual_matches++;
   }
}

ok( $wanted_matches == $actual_matches, "Correct number of matches" );

done_testing;

__DATA__
line one..... 
line two..... 
....
line ten..... 
&lt;/code&gt;

&lt;!-- Node text goes above. Div tags should contain sig only --&gt;
&lt;div class="pmsig"&gt;&lt;div class="pmsig-104468"&gt;
&lt;p&gt;
Neil Watson&lt;br&gt;
&lt;a href="http://watson-wilson.ca"&gt;watson-wilson.ca&lt;/a&gt;
&lt;/div&gt;&lt;/div&gt;</doctext>
<thread id="1154672">
<node id="1154695"><node id="1154696"/>
</node>
<node id="1201201"/>
</thread></node>
