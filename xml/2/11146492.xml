<?xml version="1.0" encoding="UTF-8"?>
<node id="11146492" title="Re: Sort a matrix by row" created="2022-08-30 06:21:35" updated="2022-08-30 06:21:35" _doctext_mode="strict"><type id="11">note</type>
<author id="830549">haukex</author>
<doctext field_name="doctext">&lt;p&gt;&lt;strike&gt;Please see [id://17557] and use &lt;c&gt;&lt;code&gt;&lt;/c&gt; tags to format code, sample input, and expected output.&lt;/strike&gt; &lt;small&gt;&lt;i&gt;Fixed by [GrandFather].&lt;/i&gt;&lt;/small&gt;&lt;/p&gt;
&lt;p&gt;Anyway, here's a pure-Perl solution. It doesn't actually sort the matrix, it just gives you the sorted indicies of the rows, but that's enough to get your expected output, and hopefully you can see how to use the output to reorder the matrix if you like*.&lt;/p&gt;
&lt;c&gt;
#!/usr/bin/env perl
use warnings;
use strict;
use Data::Dump;

my @matrix = (
	[qw/ t1 t1 t2 t2 t1 t2 /],
	[qw/ a1 a2 a1 a2 a3 a3 /],
	[qw/ mis mis mis mis del del /],
);

my @idx = sort { $matrix[0][$a] cmp $matrix[0][$b]
	or $matrix[1][$a] cmp $matrix[1][$b] } 0..$#{$matrix[0]};
dd @idx;  # (0, 1, 4, 2, 3, 5)
my @out = map { $matrix[2][$_] } @idx;
dd @out;  # ("mis", "mis", "del", "mis", "mis", "del")
&lt;/c&gt;
&lt;p&gt;* &lt;i&gt;Update:&lt;/i&gt; &lt;spoiler&gt;&lt;c&gt;@matrix = map { my $r=$_; [ map { $matrix[$r][$_] } @idx ] } 0..$#matrix;&lt;/c&gt; &lt;i&gt;Update 2:&lt;/i&gt; or the slightly shorter &lt;c&gt;@matrix = map { [ @{$matrix[$_]}[@idx] ] } 0..$#matrix;&lt;/c&gt; (the same slice trick could be used instead of the &lt;c&gt;map&lt;/c&gt; in the code above).&lt;/spoiler&gt;&lt;/p&gt;</doctext>
<root_node field_name="root_node">11146491</root_node>
<parent_node field_name="parent_node">11146491</parent_node>
<thread id="11146491">
<node id="11146492"><node id="11146501"><node id="11146502"><node id="11146562"/>
</node>
</node>
</node>
<node id="11146493"><node id="11146498"/>
</node>
<node id="11146503"/>
<node id="11146504"/>
</thread></node>
