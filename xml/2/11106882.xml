<?xml version="1.0" encoding="UTF-8"?>
<node id="11106882" title="Find range in array with indices" created="2019-09-30 13:48:13" updated="2019-09-30 13:48:13" _doctext_mode="lenient"><type id="115">perlquestion</type>
<author id="1189588">IB2017</author>
<doctext field_name="doctext">&lt;p&gt;Dear monks,&lt;/p&gt;
&lt;p&gt;&lt;b&gt;Updated&lt;/b&gt;&lt;/p&gt;
&lt;p&gt;My array contains a set of &lt;strike&gt;coordinates&lt;/strike&gt; indices in the form of "x.x" (coming from a Tk Text widget), being the first value the row and the second the character position. Any two elements of the array represents a couple, i.e. start and end points in a text (element 0 and 1, element 2 and 3, and so forth). An example is:&lt;/p&gt;
&lt;code&gt;
my @array = (1.1,1.4,5.33,6.1,7.23,7.133,10,11);
&lt;/code&gt; 
&lt;p&gt;Given a &lt;strike&gt;coordinate&lt;/strike&gt; index, I need to find the range (start end point) in which the &lt;strike&gt;coordinate&lt;/strike&gt; index falls. So, for example, given 1.3, I need to find the range 1.1-1.4, i.e. element 0 and element 1 of my array. I have a small (and verbose) script that can find a range if the array is of natural numbers, but it fails with &lt;strike&gt;coordinates&lt;/strike&gt; indices like mine since, in my example, 7.23 is considered bigger than 7.133 (however, in terms of indices the opposite is the truth). Is there a straightforward way to do comparisons of my indices?&lt;/p&gt;
&lt;code&gt;
use strict;
use warnings;

my @array = (1.1,1.4,5.33,6.1,7.23,7.133,10,11);

print "\nenter number\n";
chomp (my $cursorPosition = &lt;STDIN&gt;);

my $itr=0;
foreach my $tagBegin (@array){
  if (0 == $itr % 2) {
  }
  else{  
    last if $cursorPosition &lt;= $tagBegin;
  }
  $itr++;
}
print "Range is $array[$itr-1] - $array[$itr]";
&lt;/code&gt; 
&lt;p&gt;With the above script, given coordinate 7.25 it finds the wrong range.&lt;/p&gt;</doctext>
<thread id="11106882">
<node id="11106883"><node id="11106885"/>
</node>
<node id="11106884"><node id="11106886"><node id="11106925"/>
</node>
</node>
<node id="11106887"><node id="11106926"/>
</node>
<node id="11106888"><node id="11106917"><node id="11106918"><node id="11106924"><node id="11106940"/>
</node>
</node>
</node>
</node>
</thread></node>
