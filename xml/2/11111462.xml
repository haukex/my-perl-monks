<?xml version="1.0" encoding="UTF-8"?>
<node id="11111462" title="Architecture advice, proxy or rebroadcast websocket" created="2020-01-15 17:41:59" updated="2020-01-15 17:41:59" _doctext_mode="lenient"><type id="115">perlquestion</type>
<author id="248054">Your Mother</author>
<doctext field_name="doctext">&lt;p&gt;I got some great advice for putting together my websocket client here: [id://11110853]. And it’s working well.&lt;/p&gt;

&lt;p&gt;10,000 foot view: I am consuming a stock price stream. This is a paid service that only allows one connection and it is somewhat fragile due to a short timeout period, a dearth of documentation, and no built-in caching/reconnecting. None of my plans breach the user agreement/contract; that’s not me.&lt;/p&gt;

&lt;p&gt;I don’t want to write a cache/queue. I want to leave the stream alone and uncluttered with a bunch of synchronous calls that might throw exceptions or otherwise cause trouble; separation of concerns. I also want to test multiple versions of code against the single stream concurrently. I could record and play back data but that’s a pain, a disk hog, and it doesn’t address the fragility/complexity or any possible application of code against live data.&lt;/p&gt;

&lt;p&gt;My ideas have been–&lt;/p&gt;

&lt;ul&gt;
   &lt;li&gt;proxy the websocket through nginx so multiple “muted” client connections still only share the single permissible connection to the real service (I’m not sure this is possible without a lot of code and it’s extra plumbing),&lt;/li&gt;
   &lt;li&gt;write a websocket server, in Mojo still, around the websocket client that lets an arbitrary number of local clients receive an echo of the master client’s stream,&lt;/li&gt;
   &lt;li&gt;write a simple streaming socket server in Perl that runs the websocket client and pushes out to any local socket clients,&lt;/li&gt;
   &lt;li&gt;publish the data stream to a local DB (this would be slower than everything else if it’s relational but also perhaps the most reliable with a built-in “cache/queue” only in need of pruning),&lt;/li&gt;
   &lt;li&gt;or come beg you guys for what is probably a better idea than I can summon.&lt;/li&gt;
&lt;/ul&gt;

&lt;p&gt;So… I guess the last one. :P&lt;/p&gt;
</doctext>
<thread id="11111462">
<node id="11111469"><node id="11111470"><node id="11111484"/>
</node>
</node>
<node id="11111485"><node id="11111487"/>
</node>
<node id="11111532"><node id="11111538"/>
</node>
</thread></node>
