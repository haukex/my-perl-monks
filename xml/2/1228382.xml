<?xml version="1.0" encoding="UTF-8"?>
<node id="1228382" title="Regex not matching closing newline?" created="2019-01-11 04:33:42" updated="2019-01-11 04:33:42" _doctext_mode="lenient"><type id="115">perlquestion</type>
<author id="1228378">rverscho</author>
<doctext field_name="doctext">&lt;p&gt;I cannot figure out the following seemingly trivial regex, so I hope some wisdom can be dispensed...
&lt;/p&gt;
&lt;p&gt;
I have a 4-char string, consisting of 'abc' plus a closing newline. &lt;br&gt;
When matching until the end of the string, I get a different result when using '.*$' vs '.*?$': in the first case the closing newline is included, in the second, it is not. Note that /s is being used. &lt;br&gt;
I am mystified how the end of the string can be interpreted different in these regexes, but apparently it is.&lt;br&gt;
What am I missing here?&lt;br&gt;
When the closing char is not a newline, the results are identical, as expected.&lt;br&gt;
This in in Perl v5.14.4 (no options to use a different version). &lt;br&gt;
&lt;/p&gt;
&lt;p&gt;
Thanks much in advance for guidance!
&lt;/p&gt;

&lt;code&gt;
$s = "abc\n";
if ($s =~ /(ab.*?)$/s) {
	$p = substr($s, @-[1], (@+[1] - @-[1]));
	print("match A=[$p] length=".length($p)."\n");
}
if ($s =~ /(ab.*)$/s) {
	$q = substr($s, @-[1], (@+[1] - @-[1]));
	print("match B=[$q] length=".length($q)."\n");
}

result:

match A=[abc] length=3
match B=[abc
] length=4
&lt;/code&gt;</doctext>
<thread id="1228382">
<node id="1228384"><node id="1228385"><node id="1228389"/>
</node>
</node>
</thread></node>
