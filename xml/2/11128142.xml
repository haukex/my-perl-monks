<?xml version="1.0" encoding="UTF-8"?>
<node id="11128142" title="Can't figure out how to include LAST member of the array" created="2021-02-09 14:36:27" updated="2021-02-09 14:36:27" _doctext_mode="lenient"><type id="115">perlquestion</type>
<author id="1124341">perlynewby</author>
<doctext field_name="doctext">&lt;p&gt;This code pretends to build a layer progression as per user input&lt;b&gt; but I cannot think of a way to get the last layer into the print out&lt;/b&gt;&lt;/p&gt;
&lt;p&gt; Simple task (so I thought!)- building a hierarchical progression of layers. Say the user wants to build a metal stack from M1 to M5, ie: M1-&gt;v1-&gt;M2-&gt;v2-&gt;M3-&gt;v3-&gt;M4-&gt;v4-&gt;M5.&lt;/p&gt;
&lt;p&gt;I cannot figure out how to add the last metal &lt;b&gt;(M5)&lt;/b&gt; into my logic, any ideas on this?&lt;/p&gt;
&lt;p&gt;&lt;i&gt;In fact, I want to add some dimensions to these metals and make sure the via is laid out within those dimensions. IS there a Perl library that I can use for that??&lt;/i&gt; but that's for a later day after I complete the last array member&lt;/p&gt;
&lt;P&gt; Anyways, this is what I have so far.&lt;/p&gt;
&lt;code&gt;#!/usr/bin/perl

use 5.14.2;
use strict;
use warnings;
use Data::Dumper;
use diagnostics;

sub metal_stack_order {
	my @unsorted =@_ ;
	
	#make sure to arrange to ascending order
	my @sorted = sort { $a &lt;=&gt;$b} @unsorted;
	my ($Start,$End)=($sorted[0],$sorted[1]);
	
	for (my $i=$Start; $i &lt;= ($End-1) ; $i++){		
		print "M$i --&gt; V$i--&gt;";
	}
	
		
}

say "Enter Lowest metal layer like =&gt;  \"m1\"\n";
my $input1=&lt;&gt;;
say " Enter Highest metal like =&gt;  \"m5 \"\n";
my $input2=&lt;&gt;;


#parsing the metal layer (digits)
my ($num_1)=$input1=~ m/(\d)+/;
my ($num_2)=$input2=~ m/(\d)+/;

#Ony exception, no equal metal layers
if ($num_1 == $num_2){ 
	say "You cannot do a layout  the same layer\n";
	exit;
}


metal_stack_order($num_1,$num_2);


&lt;/code&gt;
&lt;p&gt; Thanks for the help!&lt;/p&gt;

</doctext>
<thread id="11128142">
<node id="11128143"><node id="11128151"/>
</node>
<node id="11128146"><node id="11128148"><node id="11128150"><node id="11128153"><node id="11128155"><node id="11128156"><node id="11128175"><node id="11128196"><node id="11128203"/>
</node>
</node>
</node>
</node>
</node>
</node>
</node>
<node id="11128152"/>
</node>
<node id="11128170"/>
</thread></node>
