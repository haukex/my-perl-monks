<?xml version="1.0" encoding="UTF-8"?>
<node id="1222462" title="How to install module from OS distro if available, and from CPAN otherwise ?" created="2018-09-16 08:05:04" updated="2018-09-16 08:05:04" _doctext_mode="lenient"><type id="115">perlquestion</type>
<author id="1222460">toughy</author>
<doctext field_name="doctext">&lt;p&gt;Hello,&lt;/p&gt;

&lt;p&gt;I would like to show a message for users to install the needed perl modules when my script starts and dependencies are missing.&lt;/p&gt;

&lt;p&gt;I would like to show users the command to install modules from the OS distribution packages first, if they are available there, and show a `cpanm` install command for modules that are not provided in OS repositories.&lt;/p&gt;

&lt;p&gt;However, installing OS distribution packages is different for every operating system. On Ubuntu Linux for example I need to invoke `dpkg-query --list` to see if a package like lib&lt;em&gt;Module-Name&lt;/em&gt;-perl is available in the repos.&lt;/p&gt;

&lt;p&gt;This can get complicated when I need to install one module from CPAN, but some of the dependencies are available as OS packages, in which case I should install those first.&lt;/p&gt;

&lt;p&gt;So is there a module that can help me install a package from OS distro if available, or from CPAN otherwise ?&lt;/p&gt;

&lt;p&gt;Also, do I understand correctly that it is recommended to prefer the CPAN modules instead of the OS ones ? I find this to be a real problem for any non-pure perl module, I belive the OS distro should be preferred in this case.&lt;/p&gt;</doctext>
<thread id="1222462">
<node id="1222472"/>
<node id="1222544"/>
</thread></node>
