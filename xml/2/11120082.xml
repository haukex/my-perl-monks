<?xml version="1.0" encoding="UTF-8"?>
<node id="11120082" title="Re: Find Photos Taken During A Certain Time Range" created="2020-07-30 15:18:46" updated="2020-07-30 15:18:46" _doctext_mode="strict"><type id="11">note</type>
<author id="830549">haukex</author>
<doctext field_name="doctext">&lt;blockquote&gt;&lt;i&gt;When I tried it in my home directory as a test, it did exactly what I expected but when I changed '.' to '/' it started generating every file on the system (directories, text files, etc.) without the date.&lt;/i&gt;&lt;/blockquote&gt;
&lt;p&gt;That seems strange; I don't have Cygwin but I can't reproduce the issue on Linux, so it could maybe have something to do with the interaction of Cygwin and [mod://File::Find::Rule]. Is the code you showed here &lt;i&gt;exactly&lt;/i&gt; what you're running? Unless another Monk with Cygwin can reproduce the issue and find out what's wrong, you may need to look into it yourself. As a start, perhaps reduce your code even further, since it doesn't sound like it has to do with [mod://Image::ExifTool]. The following seems to work fine on my system:&lt;/p&gt;
&lt;c&gt;
use warnings;
use strict;
use File::Find::Rule;

my $rule = File::Find::Rule-&gt;file-&gt;name(qr/\.jpe?g$/i)-&gt;start('/');
while ( defined( my $file = $rule-&gt;match ) ) {
	print "$file\n";
}
&lt;/c&gt;</doctext>
<root_node field_name="root_node">11120080</root_node>
<parent_node field_name="parent_node">11120080</parent_node>
<thread id="11120080">
<node id="11120082"/>
<node id="11120083"/>
<node id="11120084"><node id="11126630"/>
</node>
</thread></node>
