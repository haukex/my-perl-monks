<?xml version="1.0" encoding="UTF-8"?>
<node id="1222472" title="Re: How to install module from OS distro if available, and from CPAN otherwise ?" created="2018-09-16 13:47:45" updated="2018-09-16 13:47:45" _doctext_mode="strict"><type id="11">note</type>
<author id="830549">haukex</author>
<doctext field_name="doctext">&lt;p&gt;It is generally recommended to not use CPAN to install modules into the system Perl, because it's possible that if an installation fails, you might mess up your entire system, and cleaning that up can be a huge pain. Also, sometimes programs may rely on the system Perl having a specific version of a module and upgrading it may actually break those programs. Personally, I would recommend using [https://perlbrew.pl/|perlbrew] or [https://github.com/tokuhirom/plenv#readme|plenv] (or [https://github.com/stevieb9/berrybrew#readme|berrybrew] on Windows) to install a fresh copy of Perl, or to use [mod://local::lib] to install modules to the user's home directory. Both of these methods allow you to install any modules from CPAN you like, so when you look at it that way, it makes sense to just install everything from CPAN.&lt;/p&gt;
&lt;blockquote&gt;&lt;i&gt;Also, do I understand correctly that it is recommended to prefer the CPAN modules instead of the OS ones ? I find this to be a real problem for any non-pure perl module, I belive the OS distro should be preferred in this case.&lt;/i&gt;&lt;/blockquote&gt;
&lt;p&gt;Well, CPAN will always give you the latest version of a module, while the OS repositories may have quite old versions of the modules (for example, that's the case on older Ubuntu LTS versions). On the other hand, once in a while building modules from source can be a pain, and if the distro has a precompiled version, just using that can be easier, so I see your point there. (BTW, if the problem is figuring out what libraries need to be installed for a Perl module to compile cleanly, on Debian/Ubuntu often &lt;c&gt;sudo apt-get build-dep libwhatever-perl&lt;/c&gt; helps.) Another situation where I found it easier to install modules from the OS distro was on the first Raspberry Pi, where compiling stuff could take quite a bit of time. But other than that, I'd recommend using the CPAN version.&lt;/p&gt;
&lt;blockquote&gt;&lt;i&gt;However, installing OS distribution packages is different for every operating system. ... This can get complicated when I need to install one module from CPAN, but some of the dependencies are available as OS packages&lt;/i&gt;&lt;/blockquote&gt;
&lt;p&gt;That sounds fairly ambitious to me. Have you actually run into this situation in practice?&lt;/p&gt;
&lt;blockquote&gt;&lt;i&gt;I would like to show a message for users to install the needed perl modules when my script starts and dependencies are missing.&lt;/i&gt;&lt;/blockquote&gt;
&lt;p&gt;Have you had a look at [mod://lazy], or [mod://cpanfile], which can be used via &lt;c&gt;cpanm --installdeps .&lt;/c&gt;? Another option is to distribute your script as part of a CPAN distro in the &lt;c&gt;App::&lt;/c&gt; namespace. As a regular CPAN dist, its dependencies are installed via the normal CPAN mechanisms. See e.g. [mod://ccdiff] or [mod://ack]. In combination with the above suggestions of &lt;c&gt;perlbrew&lt;/c&gt; or &lt;c&gt;local::lib&lt;/c&gt;, this should allow you to easily install any CPAN dependencies.&lt;/p&gt;
&lt;blockquote&gt;&lt;i&gt;see if a package like libModule-Name-perl is available in the repos.&lt;/i&gt;&lt;/blockquote&gt;
&lt;p&gt;That naming convention isn't always kept though, for example [mod://LWP] is in [https://packages.debian.org/stretch/libwww-perl|&lt;c&gt;libwww-perl&lt;/c&gt;]. You might need to do something like &lt;c&gt;apt-file search --regex '/Module.pm$'&lt;/c&gt;, although then figuring out e.g. all the different &lt;c&gt;*/Tiny.pm&lt;/c&gt; modules might be difficult. (BTW, I recently wrote [https://github.com/haukex/webperl/blob/master/experiments/depend.pl|a script] to try and use the MetaCPAN API to figure out modules' dependencies, maybe the CPAN distro names can help in figuring out the package names.)&lt;/p&gt;
&lt;p&gt;So anyway, my personal recommendation would be to use one of the methods I mentioned above that allow you to install anything you like from CPAN. However, all of those suggestions involve compiling the dependencies. So it seems to me that this comes down to another question: Which modules specifically are you having trouble compiling?&lt;/p&gt;
</doctext>
<root_node field_name="root_node">1222462</root_node>
<parent_node field_name="parent_node">1222462</parent_node>
<thread id="1222462">
<node id="1222472"/>
<node id="1222544"/>
</thread></node>
