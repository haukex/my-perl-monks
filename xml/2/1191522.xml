<?xml version="1.0" encoding="UTF-8"?>
<node id="1191522" title="Add child hash to hash of hashes." created="2017-05-29 13:22:23" updated="2017-05-29 13:22:23" _doctext_mode="lenient"><type id="115">perlquestion</type>
<author id="430892">bdalzell</author>
<doctext field_name="doctext">&lt;p&gt;Recently I returned to working on a complex program that uses a hash of hashes (H0H) as a database.&lt;/p&gt; 

&lt;p&gt;There is a section that creates a new child hash that then has to be added to the large HoH. &lt;/p&gt;

&lt;p&gt;I had forgotten how to add an already created child hash to a hash of hashes.&lt;/p&gt;

&lt;p&gt;Trying to Google things did not help. All the answers had you create the child hash while adding it to the HoH.&lt;/p&gt;

&lt;p&gt;My large application needs to have a keyname which is also stored in the child hash as part of the information with in the child hash. &lt;/p&gt;

&lt;p&gt;I am not trying to start a discussion about what is in the hash or the need for the keyname.&lt;/p&gt;

&lt;p&gt;Here are two ways to add a preexisting child hash to the HoH:&lt;/p&gt;

&lt;p&gt;Add the child hash directly: &lt;/p&gt;

 &lt;code&gt; $HoH{"$key"} = ({%child_hash);&lt;/code&gt;

&lt;p&gt;add by reference to new child hash&lt;/p&gt;

&lt;code&gt;$HoH{"$key"} = \%child_hash;&lt;/code&gt;

&lt;p&gt; Here is a working sample script which contains comments&lt;/p&gt;

&lt;code&gt;
#!/usr/bin/perl

my %HoH= (
 'test1'  =&gt; { 'keyname'=&gt;'test1', 'price' =&gt; '10.00', 'desc' =&gt; 'the 1st test'},
 'test2'  =&gt; { 'keyname'=&gt;'test2', 'price' =&gt; '12.00', 'desc' =&gt; 'the 2nd test'},
 'test3'  =&gt; { 'keyname'=&gt;'test3', 'price' =&gt; '13.00', 'desc' =&gt; 'the 3rd test'},
 'test4'  =&gt; { 'keyname'=&gt;'test4', 'price' =&gt; '14.00', 'desc' =&gt; 'the 4th test'}
);

my %child_hash = &amp;new_hash;
my $key = $child_hash{'keyname'};
  ## 2 ways to add a newly made hash to the hash of hashes
  # directly 
  # $HoH{"$key"} = ({%child_hash});
  # or by reference

$HoH{"$key"} = \%child_hash;

my @keys = keys %file_tests;
@keys = sort @keys; 

foreach my $key(@keys){
 print "$key price is: $HoH{$key}{'price'}\n";
}

sub new_hash {
 my %hash = ( 'keyname' =&gt; 'test5', 
                'price' =&gt; "20.00", 
                'desc' =&gt; "the 5th test");
 return %hash;
}
&lt;/code&gt;</doctext>
<thread id="1191522">
<node id="1191523"/>
<node id="1191528"/>
</thread></node>
