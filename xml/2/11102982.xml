<?xml version="1.0" encoding="UTF-8"?>
<node id="11102982" title="Re^2: Zip script not running on cron" created="2019-07-18 12:09:30" updated="2019-07-18 12:09:30" _doctext_mode="lenient"><type id="11">note</type>
<author id="11102968">PerlMonger79</author>
<doctext field_name="doctext">&lt;p&gt;Thank you for your time and support.  I'm fairly new to perl and I'm really starting to like coding in perl but I've never used CWD nor ENV.  I have read from both perldoc and perlmaven but I'm not understanding how to fit it in my script.   Is this right?&lt;/p&gt;

&lt;code&gt;

#!/usr/bin/perl -w
use strict;
use warnings;
use Cwd qw(cwd);
use POSIX qw/strftime/;
use IO::Compress::Zip qw(:all);

# Timestamp
my $now = strftime("%d%b%Y_%H00", localtime(time - 60*60));

# Where zip file is to be stored
my $arc = "/path/to/archive/";

# Where csv files are found.
chdir "/path/to/directory";

# Zipping files found and storing into archive
zip [ glob("*-".$now.".csv") ] =&gt; $arc.$now.".zip"    
    or die "Cannot create zip file: $ZipError" ;
    
# Deleting csv files 
unlink glob("*-".$now.".csv");

&lt;/code&gt;</doctext>
<root_node field_name="root_node">11102978</root_node>
<parent_node field_name="parent_node">11102979</parent_node>
<thread id="11102978">
<node id="11102979"><node id="11102980"/>
<node id="11102982"><node id="11102983"><node id="11102990"/>
</node>
</node>
</node>
</thread></node>
