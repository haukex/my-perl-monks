<?xml version="1.0" encoding="UTF-8"?>
<node id="1200132" title="Re^2: When modules install in perl5" created="2017-09-26 12:59:53" updated="2017-09-26 12:59:53" _doctext_mode="lenient"><type id="11">note</type>
<author id="1117213">cristofayre</author>
<doctext field_name="doctext">&lt;p&gt;For anyone who's curious, the REAL problem was that the file had been uploaded as DOS (CRLF) rather than UNIX (LF only) "Filezilla" auto corrects on upload - but not so cPanel "FileManager"
&lt;/p&gt;
&lt;p&gt;Now I got to write a script to check 200+ files on my Windows7 / Strawbery PERL local machine. I will use File::Find::Rule to reiterate through all the folders and sub domains, and open the first line of each file. If it's a CRLF, THEN I will open the file. make the change, and save it out again. Problem at present is getting grep to find the linefeed. Tried escaping and /\x0A\x0D/, but won't play ball!

&lt;code&gt;
#!/usr/bin/perl 
print "content-type:  text/html\n\n";
 use CGI::Carp qw( fatalsToBrowser );
use File::Find::Rule;
$string="\x0D\x0A";

#my @files = File::Find::Rule-&gt;file()-&gt;name('*.pl')-&gt;in('/home/cristofa/public_html/'); # set -&gt;in('.') to start from current directory

my @files=('D:/home/cristofa/public_html/cgi-bin/dummy/testme.pl');
for ($x=0; $x&lt;@files; $x++){
	open THEFILE, "&lt;$files[$x]";
	$first_line = &lt;THEFILE&gt;;
	print $first_line;
	close THEFILE;

		if (grep(/$string$/,$first_line)){
		print File "$files[$x] is incorrect&lt;br&gt;";
	#	&amp;correct_file;
		}
}

sub correct_file{
open (FILE, "&lt;$files[$x]);
while(&lt;FILE&gt;){
$tmp.=$_;
}
	$tmp=~s/[\n\r][\n\r]/\n/g;
open (FILE, "&gt;$files[$x]);
print FILE $tmp;
close(FILE);
$tmp='';
}
print "All done";
&lt;/code&gt; 

It is only checking the single test file at this stage, which should print "File XYZ is incorrect" if its working</doctext>
<root_node field_name="root_node">1200039</root_node>
<parent_node field_name="parent_node">1200042</parent_node>
<thread id="1200039">
<node id="1200040"/>
<node id="1200041"/>
<node id="1200042"><node id="1200043"><node id="1200051"/>
</node>
<node id="1200132"><node id="1200142"/>
<node id="1200187"/>
</node>
</node>
<node id="1200044"><node id="1200055"><node id="1200128"/>
</node>
<node id="1200127"/>
</node>
<node id="1200045"><node id="1200129"/>
</node>
<node id="1200060"/>
<node id="11109615"/>
</thread></node>
