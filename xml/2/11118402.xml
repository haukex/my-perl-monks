<?xml version="1.0" encoding="UTF-8"?>
<node id="11118402" title="Pass local bash script to remote host via SSH" created="2020-06-24 00:16:27" updated="2020-06-24 00:16:27" _doctext_mode="lenient"><type id="115">perlquestion</type>
<author id="725401">puterboy</author>
<doctext field_name="doctext">As part of a larger Perl program, I need to execute a local bash script on a remote machine.

I would like to do the equivalent of:
&lt;code&gt; 
#!/bin/bash
ssh &lt;remotehost&gt; bash -s &lt;&lt;EOF
&lt;local bash script written here&gt;
EOF &lt;/code&gt;

but from within a perl script using either backticks or system to run the ssh command.
Something like
&lt;code&gt; #!/usr/bin/perl
my $output = `ssh &lt;remotehost&gt; bash -s` &lt;&lt;EOF
&lt;local bash script written here&gt;
EOF&lt;/code&gt;
Note that I want to load the bash script from within the same perl file where the ssh command is run -- not load it from an external separate file.

Note that I can't run Net::SSH since I don't have access to additional modules

</doctext>
<thread id="11118402">
<node id="11118404"><node id="11118407"><node id="11118410"/>
</node>
</node>
<node id="11118409"><node id="11118490"/>
</node>
<node id="11118414"/>
</thread></node>
