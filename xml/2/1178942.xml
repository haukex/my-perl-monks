<?xml version="1.0" encoding="UTF-8"?>
<node id="1178942" title="Re^3: Exporter in an OO module?" created="2017-01-04 11:58:04" updated="2017-01-04 11:58:04" _doctext_mode="strict"><type id="11">note</type>
<author id="830549">haukex</author>
<doctext field_name="doctext">&lt;p&gt;Hi [SBECK],&lt;/p&gt;
&lt;blockquote&gt;&lt;i&gt;Initially I was trying to avoid the user having to do anything special to access the constants&lt;/i&gt;&lt;/blockquote&gt;
&lt;p&gt;I was first introduced to the idea that &lt;c&gt;@EXPORT&lt;/c&gt;ing a bunch of stuff might not be a good idea by the [mod://Perl::Critic] policy "ProhibitAutomaticExportation", and have since come to mostly agree with that idea, at least to minimize automatic exportation. Quoting the module's doc:&lt;/p&gt;
&lt;blockquote&gt;
When using [mod://Exporter], symbols placed in the &lt;c&gt;@EXPORT&lt;/c&gt; variable are automatically exported into the caller's namespace. Although convenient, this practice is not polite, and may cause serious problems if the caller declares the same symbols. The best practice is to place your symbols in &lt;c&gt;@EXPORT_OK&lt;/c&gt; or &lt;c&gt;%EXPORT_TAGS&lt;/c&gt; and let the caller choose exactly which symbols to export.
&lt;/blockquote&gt;
&lt;p&gt;My take on it is this: What does the user &lt;i&gt;expect&lt;/i&gt; will happen when they write &lt;c&gt;use Module::Name;&lt;/c&gt;? If the module is an OO module, the user might not expect it to export anything, so that should probably be its default behavior. However, in the case of &lt;c&gt;use Locale::Codes::Constants;&lt;/c&gt;, the name implies that it's a module of constants, so perhaps exporting a default set of constants is a useful default behavior. I've also had in-between cases, like e.g. a module that has one central function, which I decided to export by default, but the other functions in the module had to be exported explicitly. Some more questions one might ask oneself when deciding: how much do we want to pollute the user's namespace, which of the functions will they &lt;i&gt;always&lt;/i&gt; need, and which only sometimes? Sometimes it's hard to decide on a default behavior, but in those cases I don't think it really hurts to just keep &lt;c&gt;@EXPORT&lt;/c&gt; empty, at least in the first version of the module.&lt;/p&gt;
&lt;p&gt;Regards,&lt;br&gt;-- Hauke D&lt;/p&gt;</doctext>
<root_node field_name="root_node">1178936</root_node>
<parent_node field_name="parent_node">1178939</parent_node>
<thread id="1178936">
<node id="1178938"><node id="1178939"><node id="1178942"/>
</node>
</node>
<node id="1178940"><node id="1178981"/>
</node>
<node id="1178972"/>
</thread></node>
