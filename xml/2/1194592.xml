<?xml version="1.0" encoding="UTF-8"?>
<node id="1194592" title="Read FASTA into Hash" created="2017-07-09 06:30:48" updated="2017-07-09 06:30:48" _doctext_mode="lenient"><type id="115">perlquestion</type>
<author id="1191414">ic23oluk</author>
<doctext field_name="doctext">&lt;p&gt; Hello Monks,&lt;/p&gt;
&lt;p&gt; I'd like to read a Fasta file, consisting of header1 newline sequence1 newline header2 newline... into a hash and print it out.&lt;/p&gt;
&lt;P&gt; I am currently learning hashes, and I don't understand why my program doesn't work. It reads all sequences into one element and doesn't store the headers at all&lt;/p&gt;
&lt;p&gt; all headers start with a '&gt;', so I use a regex to identify them, here's my code: &lt;/p&gt;

&lt;code&gt;
my $file = "sequences.fasta";
open (READ, "$file") || die "Cannot open $file: $!.\n";

my %seqs = ();
my $header = '';

while (my $line = &lt;READ&gt;){
	chomp $line;
	$line =~ s/^\s*$//;
	if ($line =~ m/^&gt;.*$/){
		$line = $header;
	} else {
		$seqs{"$header"} .= $line;
	}
}
close (READ);

my @count_seq = keys %seqs;
foreach (@count_seq){
	print $_, "\n", $seqs{$_}, "\n";
}
&lt;/code&gt;

&lt;p&gt; can someone indicate the problem? Thanks in advance!&lt;/p&gt;</doctext>
<thread id="1194592">
<node id="1194594"><node id="1194595"><node id="1194600"/>
</node>
</node>
</thread></node>
