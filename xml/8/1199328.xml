<?xml version="1.0" encoding="UTF-8"?>
<node id="1199328" title="Re: Finding substrings of fixed width text with graphemes" created="2017-09-13 12:18:43" updated="2017-09-13 12:18:43" _doctext_mode="strict"><type id="11">note</type>
<author id="830549">haukex</author>
<doctext field_name="doctext">&lt;p&gt;For grapheme support ("extended grapheme cluster" to be exact) built into Perl ([doc://perlrebackslash#Misc|&lt;c&gt;\X&lt;/c&gt;]), I would suggest you use a regex, for example &lt;c&gt;my ($right) = $s=~/\A\X{10}(.*)\z/s;&lt;/c&gt; works.&lt;/p&gt;
&lt;p&gt;However, your [doc://substr] example also works for me, although I did take your example data, store it in a file, making sure to use UTF-8, and then I opened it via &lt;c&gt;open my $fh, '&lt;:encoding(UTF-8)', 'input.txt' or die $!;&lt;/c&gt;. Your ligatures - at least the way you've posted them here - appear to be stored in one Unicode character each (&lt;c&gt;"\x{FB00}"&lt;/c&gt; and &lt;c&gt;"\x{FB03}"&lt;/c&gt;).&lt;/p&gt;
&lt;p&gt;This leads me to suspect your problem might be occurring earlier, i.e. that your file is stored with a different encoding than the one you expect, or you are not opening it with the proper encoding in Perl. You might want to read the following recent threads for some general advice on dealing with encodings as well as specific advice on how to find out what encoding was used to store the file: [id://1198027], [id://1198944], [id://1198912] - basically: 1. Be &lt;i&gt;certain&lt;/i&gt; what encoding the data is stored with (looking at a hex dump of the file if necessary), 2. Open it with the proper encoding, as I showed above, and 3. Inspect the data once you've gotten it into Perl to make sure it was read properly (e.g. using [mod://Data::Dump]). Only then can you properly use the facilities Perl provides to deal with Unicode.&lt;/p&gt;
&lt;p&gt;&lt;small&gt;&lt;i&gt;Update:&lt;/i&gt; Clarified wording a bit.&lt;/small&gt;&lt;/p&gt;</doctext>
<root_node field_name="root_node">1199324</root_node>
<parent_node field_name="parent_node">1199324</parent_node>
<thread id="1199324">
<node id="1199328"><node id="1199332"/>
</node>
</thread></node>
