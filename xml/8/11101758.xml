<?xml version="1.0" encoding="UTF-8"?>
<node id="11101758" title="foreach a writing open fails through not found file" created="2019-06-23 08:59:36" updated="2019-06-23 08:59:36" _doctext_mode="lenient"><type id="115">perlquestion</type>
<author id="11101754">DomX</author>
<doctext field_name="doctext">&lt;p&gt;Hello Monks!&lt;/p&gt;
This is the first time I write something here. Mostly I found solutions in documentations, existing entries here or platforms like stackoverflow.

&lt;p&gt;This time I wasn't able to find a similar problem anywhere, so I decided to ask myself.&lt;br&gt;(There may be already the question answered anywhere I have, but I didn't get it, it was the same problem.)&lt;/p&gt;

&lt;code&gt;
foreach my $file ( @files ) {                            
        open(my $fh, "&gt;&gt;", $file-&gt;{full_path}); # or return(0);
                print $fh $file-&gt;{content};              
                close($fh); # or return(0);                 
        chmod(0755, $file-&gt;{full_path});                 
        print "Created \"$file-&gt;{full_path}\" with $file-&gt;{line_counter} line" , ( $file-&gt;{line_counter} &gt; 1 ) ? "s" : "" , "\n";
        }
&lt;/code&gt;
When this part of code is executed it tells me &lt;code&gt;print() on closed filehandle $fh at ./Make.pl line 223.
Can't open perl script "../Path/to/file.pm": File or directory not found&lt;/code&gt;&lt;br&gt;
First I didn't get it, because the returns just omitted writing the next files.&lt;br&gt;
How can I do multiple open within the foreach, without &lt;code&gt;print()&lt;/code&gt; failing?

&lt;p&gt;I feel as if the &lt;code&gt;open()&lt;/code&gt; wasn't executed every time as I expected&lt;br&gt;
The loop is executed within a subroutine.&lt;/p&gt;</doctext>
<thread id="11101758">
<node id="11101759"><node id="11101762"><node id="11101763"><node id="11120357"/>
</node>
</node>
</node>
</thread></node>
