<?xml version="1.0" encoding="UTF-8"?>
<node id="1175818" title="Class::Tiny and Exporter at the same time" created="2016-11-13 11:21:49" updated="2016-11-13 11:21:49" _doctext_mode="lenient"><type id="115">perlquestion</type>
<author id="958773">jaandy</author>
<doctext field_name="doctext">&lt;p&gt;Wise Monks, I find I have written code that works fine, at least it seems.  But I cannot understand _why_ it works.  It seems like it should not, there for I'm seeking understanding&lt;/p&gt;

&lt;p&gt;I am generating a report, using PDF::API2.  I've created myself a little helper object:&lt;/p&gt;

&lt;code&gt;
package AReport;
use common::sense;
use PDF::API2;
use Exporter qw( import );
use constant mm =&gt; 25.4 / 72;
use constant in =&gt; 1 / 72;
use constant pt =&gt; 1;
our @EXPORT = qw(mm in pt);

use Class::Tiny qw(api x y pageW pageH bodyW bodyH);
&lt;etc...&gt;
&lt;/code&gt;

In my main file, I can use the constants, as well the AReport object:

&lt;code&gt;
use AReport;

my $pdf = AReport-&gt;new( file =&gt; 'junk.pdf', pageW =&gt; 8.5/in );
&lt;/code&gt;

&lt;p&gt;
It all seems to work, but there should be two import() procedures, one for Class:Tiny and one for Exporter.  Only one should get called, but seems as both do?  How?&lt;/p&gt;

&lt;p&gt;Thank you for your time,&lt;/p&gt;
-Andy
</doctext>
<thread id="1175818">
<node id="1175822"/>
<node id="1175823"><node id="1175824"/>
</node>
</thread></node>
