<?xml version="1.0" encoding="UTF-8"?>
<node id="11129708" title="Re: How is the default &quot;Input Record Separator&quot; set?" created="2021-03-15 14:49:11" updated="2021-03-15 14:49:11" _doctext_mode="lenient"><type id="11">note</type>
<author id="11129684">brettasterling</author>
<doctext field_name="doctext">&lt;p&gt;Thanks for the responses - and I did do extensive searching (i.e. that's what I found when searching - that perl should never see the CR).  However, I'm seeing something different (running as "perl cmds.pl" in a Command Prompt on Windows 10)&lt;/p&gt;
&lt;p&gt;
File 'cmds.pl':
&lt;/p&gt;
&lt;code&gt;
open my $makefileToProcess, "&lt;", "input_file.txt" or die "input_file.txt: $!";
while (my $line = &lt;$makefileToProcess&gt;)
{
print "LINE BEFORE:$line:\n";
    chomp $line;
print "LINE AFTER:$line:\n";
}
&lt;/code&gt;

And what I see is:
&lt;code&gt;
LINE BEFORE:xyzCRLF:
LINE AFTER:xyzCR:
&lt;/code&gt;
&lt;p&gt;
(NOTE that CRLF is the CRLF pair, not actual 'C''R'L'F'.  Same with CR.)
&lt;/p&gt;&lt;p&gt;
What's even more interesting is if, instead of running as "perl cmds.pl", I use the Windows ".pl" association, and run simply as "perl_cmds.pl", it works as you described (i.e. the CR is never seen).&lt;/p&gt;&lt;p&gt;
In this case, I have 2 different perl implementations installed, one being cygwin (perl version 5.26, which is the one that keeps the CR) and the other being a standalone perl (perl v5.8.8, which is the one that eliminates the CR).&lt;/p&gt;
&lt;p&gt;This is why I originally asked how the default IRS gets set, but perhaps the real question is why one perl on a Windows system is allowing the CR to pass through to the perl script.&lt;/p&gt;
</doctext>
<root_node field_name="root_node">11129690</root_node>
<parent_node field_name="parent_node">11129690</parent_node>
<thread id="11129690">
<node id="11129691"><node id="11129723"/>
</node>
<node id="11129694"><node id="11129701"><node id="11129839"/>
</node>
</node>
<node id="11129708"><node id="11129709"/>
</node>
<node id="11129717"><node id="11129869"><node id="11129907"><node id="11129969"/>
</node>
</node>
</node>
</thread></node>
