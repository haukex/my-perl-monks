<?xml version="1.0" encoding="UTF-8"?>
<node id="11141828" title="Re^2: How to pass a folder name to Windows external commands?" created="2022-03-04 13:45:19" updated="2022-03-04 13:45:19" _doctext_mode="strict"><type id="11">note</type>
<author id="830549">haukex</author>
<doctext field_name="doctext">&lt;p&gt;I created several directories and files with non-ANSI filenames, and the following code works fine on them - it iterates over the short names such as &lt;c&gt;"C:\\Temp\\testuni\\5E71~1\\BF49~1.TXT"&lt;/c&gt;.&lt;/p&gt;
&lt;c&gt;
use warnings;
use strict;
use Path::Class 'dir';
use IPC::Run3 'run3';
use Data::Dump;

my $dir = dir("C:\\Temp\\testuni");
$dir-&gt;recurse( callback =&gt; sub {
	my $path = shift;
	if (!$path-&gt;is_dir) {
		run3 ['hex', $path], undef, \my $out;
		dd "$path", $out;
	}
});
&lt;/c&gt;
&lt;p&gt;Don't use &lt;c&gt;dir&lt;/c&gt; to get a list of filenames, which is why I'm using [mod://Path::Class] (at the very least, use [doc://readdir]). Please show a runnable, representative [id://1177642|SSCCE] that demonstrates the problem you're having, including sample input (directory structure), expected output for that input, and the actual output you're getting including any error messages.&lt;/p&gt;
&lt;p&gt;And by representative I also mean to please show the actual command you're running, assuming it isn't &lt;c&gt;robocopy&lt;/c&gt; - since its &lt;c&gt;/l&lt;/c&gt; option means to just list files, I'm not sure what you're doing with that command in your sample code. One of the things discussed in [id://1205217], which is linked from my node referenced above, is that a common mistake is to shell out to commands that should be implemented in native Perl.&lt;/p&gt;</doctext>
<root_node field_name="root_node">11141819</root_node>
<parent_node field_name="parent_node">11141821</parent_node>
<thread id="11141819">
<node id="11141821"><node id="11141828"><node id="11141913"><node id="11141922"/>
</node>
</node>
</node>
<node id="11141825"><node id="11141826"><node id="11141914"><node id="11141949"/>
</node>
</node>
</node>
</thread></node>
