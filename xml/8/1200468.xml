<?xml version="1.0" encoding="UTF-8"?>
<node id="1200468" title="Re: Accessing SQLite databases within ZIP files" created="2017-10-01 11:10:51" updated="2017-10-01 11:10:51" _doctext_mode="strict"><type id="11">note</type>
<author id="830549">haukex</author>
<doctext field_name="doctext">&lt;blockquote&gt;&lt;i&gt;have DBD::SQLite read the DB directly from the ZIP file, using some kind of transparent intermediary layer&lt;/i&gt;&lt;/blockquote&gt;
&lt;p&gt;[doc://IO::Uncompress::Unzip] does support a filehandle interface, but since SQLite is implemented in C, I doubt it will be able to make use of that. Since I assume that SQLite will be doing a lot of random access on the file, I doubt that any "transparent" layer would be particularly performant (unless it happens to decide to cache the entire file in memory). So I assume that it would instead be easier and probably better to just extract the database from the archive - whether to a file or to memory would be the next point.&lt;/p&gt;
&lt;blockquote&gt;&lt;i&gt;extract the DB into memory (i.e. a Perl scalar), and then have DBD::SQLite read that&lt;/i&gt;&lt;/blockquote&gt;
&lt;p&gt;Unfortunately I don't see an API method available in [mod://DBD::SQLite] to do that, although I may have missed something. See also [id://1172585], although that is about reading a file on disk into an in-memory database, not using existing memory - the SQLite API would need to support a way to pass a pointer to that memory to it. However, I also don't see any mention of that in the [https://www.sqlite.org/inmemorydb.html|SQLite docs] on that topic. Again, I may be missing something (it is Sunday after all &lt;c&gt;;-)&lt;/c&gt;&amp;nbsp;), but I think you'll have to extract the file to disk.&lt;/p&gt;
&lt;blockquote&gt;&lt;i&gt;worry about disk space, clean-up, and all that&lt;/i&gt;&lt;/blockquote&gt;
&lt;p&gt;If those are your main concerns, I think you will find [doc://File::Temp]'s &lt;c&gt;my $tmpdir = tempdir(CLEANUP=&gt;1);&lt;/c&gt; very useful, I use it a lot and have hardly ever had problems with it not cleaning up after itself (Perl usually has to crash hard for that). Personally, I would extract the SQLite database to disk, and probably only extract the binary blobs on demand, or perhaps, if the tools you are using to process them can make use of it, transparently using [doc://IO::Uncompress::Unzip]. The JSON file you could obviously extract into memory and decode via e.g. [mod://JSON::MaybeXS].&lt;/p&gt;
</doctext>
<root_node field_name="root_node">1200461</root_node>
<parent_node field_name="parent_node">1200461</parent_node>
<thread id="1200461">
<node id="1200467"><node id="1200481"/>
</node>
<node id="1200468"><node id="1200482"><node id="1200503"><node id="1200515"/>
</node>
</node>
</node>
<node id="1200470"><node id="1200476"/>
<node id="1200483"/>
</node>
<node id="1200472"><node id="1200484"><node id="1200488"/>
<node id="1200493"/>
</node>
</node>
<node id="1200517"><node id="1200552"/>
</node>
<node id="1200561"><node id="1200627"/>
</node>
<node id="1207803"/>
</thread></node>
