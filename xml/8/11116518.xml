<?xml version="1.0" encoding="UTF-8"?>
<node id="11116518" title="PERL to AWS" created="2020-05-06 14:44:41" updated="2020-05-06 14:44:41" _doctext_mode="lenient"><type id="115">perlquestion</type>
<author id="11116516">gdmichaels</author>
<doctext field_name="doctext">&lt;p&gt;I have been struggling all day with trying to get perl to talk to AWS/S3.  It has been a rough day.  First off, I am using Strawberry Perl, and perhaps this is the root of all my issues.  I failed to install Net::Amazon::S3, and I had separate issues with Amazon::S3.  I made the most progress with AWS::S3 from LEEJO.  Assuming for a moment Strawberry Perl is not the issue, the current roadblock I am hitting is trying to use the $bucket-&gt;add_file function to an existing bucket.  The code keeps generating this message:&lt;/p&gt;

&lt;p&gt;"Use of uninitialized value $etag in substitution (s///) at C:/Strawberry/perl/site/lib/AWS/S3/File.pm line 153."&lt;/p&gt;

&lt;code&gt;use AWS::S3;

my $aws_access_key_id     = "XXX";
my $aws_secret_access_key = "XXX";
 
my $s3 = AWS::S3-&gt;new(
  access_key_id     =&gt; $aws_access_key_id,
  secret_access_key =&gt; $aws_secret_access_key,
  honor_leading_slashes =&gt; 0, # set to allow leading slashes in bucket names, defaults to 0
);
 
my @bucket_list = $s3-&gt;buckets;
my $bucket = @bucket_list[0];

my $name = $bucket-&gt;name;
my $acl = $bucket-&gt;acl;

print "Name: " . $name . ", ACL: " . acl . "\n";

# Add a file:
my $new_file = $bucket-&gt;add_file(
  key       =&gt; 'foo/bar.txt',
  contents  =&gt; \'This is the contents of the file',
);

&lt;/code&gt;

</doctext>
<thread id="11116518">
<node id="11116519"/>
<node id="11116531"/>
<node id="11116550"><node id="11116870"><node id="11116889"><node id="11116891"/>
</node>
</node>
</node>
</thread></node>
