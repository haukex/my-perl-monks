<?xml version="1.0" encoding="UTF-8"?>
<node id="1163068" title="Create n-grams from tokenized text file" created="2016-05-15 07:06:15" updated="2016-05-15 07:06:15" _doctext_mode="lenient"><type id="115">perlquestion</type>
<author id="961">Anonymous Monk</author>
<doctext field_name="doctext">&lt;p&gt;From a file containing a word every line, I need to create n-grams of 2 (nad 3 and 4).&lt;p&gt;
&lt;p&gt;Input is&lt;/p&gt;
&lt;code&gt;
Hello
I
am
happy
&lt;/code&gt;
&lt;p&gt;Output should be for n-grams of 2&lt;/p&gt;
&lt;code&gt;
Hello I
I am
am happy
&lt;/code&gt;
&lt;p&gt;I'm using the following code (this is for n-grams of 2), which works fine exept a) it doesn't look nice b) is quite slow for big input files (expecially for n-grams of 4)&lt;/p&gt;
&lt;code&gt;
my $input= "input.txt";
my $output = "output.txt";
open (OUT, "&gt;$output") || (die "Can't open $output\n");
open (IN,"$input") || (die "WARNING: $input not found\n");

	my $line = &lt;IN&gt;;
	while($line){
		chomp $line;
		push(@token, $line);
		$line = &lt;IN&gt;;
	}
	
	my $shift=2;
	my $counter=0;
	my $TokenTotal = @token;
	while($counter &lt;= ($TokenTotal-$shift)){
		print OUT "$token[$counter]\t$token[$counter+1]\n";
		$counter++;
		$line = &lt;IN&gt;;
	}
close IN;
close OUT;
}
&lt;/code&gt;
&lt;p&gt;I am aware there is a Text::ngrams available, but I'd like to have a solution which I can perfectly control. Any suggestion and improvement is appreciated.&lt;/p&gt;</doctext>
<reputation field_name="reputation">7</reputation>
<thread id="1163068">
<node id="1163069"/>
<node id="1163072"/>
<node id="1163073"/>
<node id="1163074"/>
<node id="1163075"/>
<node id="1163077"/>
<node id="1163084"/>
</thread></node>
