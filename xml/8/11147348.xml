<?xml version="1.0" encoding="UTF-8"?>
<node id="11147348" title="request logging with Mojolicious" created="2022-10-11 12:30:57" updated="2022-10-11 12:30:57" _doctext_mode="lenient"><type id="115">perlquestion</type>
<author id="441103">tomgracey</author>
<doctext field_name="doctext">&lt;p&gt;Hello Monks&lt;/p&gt;

&lt;p&gt;I have a Mojolicious API which previously was running via apache, but I recently switched it to hypnotoad due to a memory leak issue. It is a lot happier running with hypnotoad. However, I was used to the comprehensive apache logs. I am sure hypnotoad/mojolicious can also do a decent job with request logs - but I am struggling to find much in the way of documentation (though I maybe looking in the wrong place).&lt;/p&gt;

&lt;p&gt;I have added &lt;code&gt;$self-&gt;log( Mojo::Log-&gt;new( path =&gt; $log_path, level =&gt; $log_level ));&lt;/code&gt; in &lt;code&gt;startup&lt;/code&gt; and out of the box this seems to produce log lines as follows:&lt;/p&gt;
&lt;code&gt;[date] [random number] [debug] [random code] POST "/endpoint"
[date] [same random number] [debug] [same random code] Routing to a callback
&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;and that's all. The problem is I want to log the full details of the request - ie including the parameters. Now I realise that &lt;code&gt;$self-&gt;log&lt;/code&gt; can be called inside a route to explicitly log something, and so I could catch the params there and &lt;code&gt;$self-&gt;log&lt;/code&gt; at that point. However, that way if e.g. a request comes in that doesn't even match any of the registered routes it won't get logged. I am wondering if there is some way to get Mojolicious to include the parameters alongside the &lt;code&gt;POST "/endpoint"&lt;/code&gt; line?&lt;/p&gt;

&lt;p&gt;Alternatively, perhaps I am barking up the wrong tree expecting Mojolicious to do this job - after all it is global logging I'm after and Mojolicious is a framework not a webserver. Can hypnotoad do the logging independently? Again struggling to find any info on this.&lt;/p&gt;

&lt;p&gt;Any pointers in the right direction would be gladly received.... thanks!&lt;/p&gt;</doctext>
<thread id="11147348">
<node id="11147368"><node id="11148894"/>
</node>
<node id="11147375"><node id="11148895"/>
</node>
</thread></node>
