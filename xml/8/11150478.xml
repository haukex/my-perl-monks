<?xml version="1.0" encoding="UTF-8"?>
<node id="11150478" title="Re: Why does Config.pm break the rules?" created="2023-02-17 09:16:13" updated="2023-02-17 09:16:13" _doctext_mode="strict"><type id="11">note</type>
<author id="830549">haukex</author>
<doctext field_name="doctext">&lt;p&gt;These "rules" do have exceptions sometimes. In my view, the point of "use &lt;c&gt;@EXPORT_OK&lt;/c&gt; instead of &lt;c&gt;@EXPORT&lt;/c&gt;" is to avoid cluttering up the &lt;c&gt;use&lt;/c&gt;r's namespace, and also to provide a bit more readability: &lt;c&gt;use Config '%Config';&lt;/c&gt; would tell the reader of the code exactly what is being exported, &lt;c&gt;use Config;&lt;/c&gt; doesn't. I know some might disagree, but IMHO, some modules are so single-purpose that it's ok to export maybe one or two things by default, if their names won't lead to confusion (e.g. the name of the thing they export is traceable back to the name of the module). IMO, [doc://Config] is such a single-purpose module, so it's ok to export &lt;c&gt;%Config&lt;/c&gt; by default. Also, &lt;c&gt;%Config&lt;/c&gt; is read-only, so "&amp;#91;the variables&amp;#93; can change under the hood, provoking horrible effects at-a-distance that are too hard to track and to fix" doesn't apply here either.&lt;/p&gt;
&lt;p&gt;More exceptions might be: some modules exist for the explicit purpose of exporting a bunch of functions (e.g. to reduce boilerplate), and I also think it's sometimes acceptable to export certain variables on request (e.g. in a test script I might provide something like &lt;c&gt;$DEBUG&lt;/c&gt;, where the fact that this is a global variable is not a problem).&lt;/p&gt;</doctext>
<root_node field_name="root_node">11150474</root_node>
<parent_node field_name="parent_node">11150474</parent_node>
<thread id="11150474">
<node id="11150475"/>
<node id="11150478"><node id="11150483"/>
</node>
<node id="11150479"/>
<node id="11150484"><node id="11150498"/>
</node>
<node id="11150493"/>
</thread></node>
