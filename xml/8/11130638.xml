<?xml version="1.0" encoding="UTF-8"?>
<node id="11130638" title="Re^5: Getting all subpaths from a path" created="2021-03-31 14:48:08" updated="2021-03-31 14:48:08" _doctext_mode="lenient"><type id="11">note</type>
<author id="1210662">ovedpo15</author>
<doctext field_name="doctext">Hi choroba! Thank you for your input as always. I have a few questions:&lt;br&gt;
1. As I understand the first part does opendir (somewhere) on the path &lt;code&gt;my $path = path("/usr/vsa/pkgs/python3/3.6.3a/bin/python3.6");&lt;/code&gt; and insert all of the files inside the directory into &lt;code&gt;$paths&lt;/code&gt; but I'm interested only in the path itself. The input of the script is an array of paths. Each path (and only those paths) interest me. So if for example I have /usr/vsa/pkgs/python3/3.6.3a/bin/python3.6a (note the "a" at the end) and it does not exist in the array of paths, I don't want to copy it (only /usr/vsa/pkgs/python3/3.6.3a/bin/python3.6. See more info at (*)).&lt;br&gt;
2. It does opendir for some reason so it fails for files (the path /usr/vsa/pkgs/python3/3.6.3a/bin/python3.6 is a file and it fails with: Error opendir on '/usr/vsa/pkgs/python3/3.6.3a/bin/python3.6': Not a directory at line 113). Line 113 is "{ recurse =&gt; 1 }"&lt;br&gt;
3. It looks like &lt;code&gt;path&lt;/code&gt; can't handle with some symlinks. I have a link /a -&gt; /c/d/e and &lt;code&gt;path("/a")&lt;/code&gt; returns nothing, while &lt;code&gt;path("/a/b")&lt;/code&gt; returns paths under "/c/d/e/b".&lt;br&gt;
4. Also what if there is a link /a/ -&gt; /b/ -&gt; /c? Should it work?&lt;br&gt;&lt;br&gt;

The cp/mkdir/ln part I will do myself. I'm just struggling to build the arrays of paths that are involved (and only them) - splitted into categories (file-links, dir-links, files, dirs).&lt;br&gt;&lt;br&gt;

* A bit more explanation about the "involved paths": I have an array &lt;code&gt;@array=("/usr/vsa/pkgs/python3/3.6.3a/bin/python3.6", ...)&lt;/code&gt;. I want to iterate over this array and mkdir the directories that exist in the array (recursively), copy the files (not the directories but I want to have only the paths that are located in @array) and set the same links. Think of that as a container which you want to run your tool on and you know those paths are needed for running your tool (but any other path is not needed). So if your tool uses /a/b/c.file and under /a/b you also have /a/b/d.file, then it will create /a/b directory and copy /a/b/c to $target/a/b/c (and set links if needed). If I wanted to copy all of the files under directory, I would just copy the directory (instead of mkdir and cp files under directory).</doctext>
<root_node field_name="root_node">11130527</root_node>
<parent_node field_name="parent_node">11130636</parent_node>
<thread id="11130527">
<node id="11130528"/>
<node id="11130533"><node id="11130546"><node id="11130632"><node id="11130634"><node id="11130635"/>
</node>
<node id="11130636"><node id="11130638"/>
</node>
<node id="11130637"><node id="11130639"/>
</node>
</node>
<node id="11130714"><node id="11130725"><node id="11130730"><node id="11130731"><node id="11130760"/>
</node>
</node>
</node>
</node>
</node>
</node>
<node id="11130736"/>
</thread></node>
