<?xml version="1.0" encoding="UTF-8"?>
<node id="1194368" title="Re: while (&lt;FH&gt;) always ends with &quot;Couldnt read from remote file: End of file at&quot;" created="2017-07-06 06:52:44" updated="2017-07-06 06:52:44" _doctext_mode="strict"><type id="11">note</type>
<author id="830549">haukex</author>
<doctext field_name="doctext">&lt;p&gt;You haven't shown the appropriate code to reproduce the issue, and the following works fine for me:&lt;/p&gt;
&lt;c&gt;
use warnings;
use strict;
use Data::Dumper;
use Net::SFTP::Foreign;
$Data::Dumper::Useqq=1;

die "Usage: $0 SSH_HOST\n" unless @ARGV==1;
my $sftp = Net::SFTP::Foreign-&gt;new($ARGV[0]);
$sftp-&gt;die_on_error("Unable to establish SFTP connection");

my $fh1 = $sftp-&gt;open("/etc/passwd") or die $sftp-&gt;error;
while (&lt;$fh1&gt;) {
	print Dumper($_) if /root/ || /wheel/ || /www/; 
}
&lt;/c&gt;
&lt;p&gt;To get better help, [id://101|register an account] so that you can [id://17557|edit] nodes if necessary (instead of posting several disjoint anonymous nodes) and show a [id://1177642] that reproduces the issue - for example you don't show how you create the &lt;c&gt;$sftp&lt;/c&gt; object (I have to guess that it's an [mod://Net::SFTP::Foreign]), and you don't tell us which line is 96. See also [id://174051]&lt;/p&gt;
</doctext>
<root_node field_name="root_node">1194362</root_node>
<parent_node field_name="parent_node">1194362</parent_node>
<thread id="1194362">
<node id="1194363"/>
<node id="1194364"><node id="1194373"><node id="1194379"/>
</node>
</node>
<node id="1194368"/>
<node id="1194383"/>
</thread></node>
